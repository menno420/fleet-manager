# Choose the `estate` migration order

> **Status:** `owner-guidance` — DRAFT for your own edits, 2026-08-30.
>
> `OWNER` = your words, quoted · `DERIVED` = this session's inference, revisable.
> This is not yet your document: only exact quotations tagged `OWNER` are your
> words; blank `OWNER` sections are waiting for you.

## How I see it

`DERIVED` from settled decisions: `estate` must pass its acceptance test before
the absolute write cutover. Fleet-manager becomes read-only on that day.

`DERIVED`: the working plan builds the new skills, hooks, and gates before it
seeds the hub. That can make the machinery precise before the new folder design
has been tested with real material.

## What I suggest

`DERIVED`: approve folder contracts and the scored test; build only the minimum
safety gates; seed the smallest truthful current set; cold-test it; cut over;
then add deeper machinery from measured use.

`DERIVED`: the minimum gates at birth are the session hold, link check, archive
search exclusion, migration-manifest check, and one authoritative-current-state
check. The practice gates remain; only the larger build moves later.

## One guiding question

`DERIVED`: what is the smallest set of live material that would let you trust
the new hub without returning to fleet-manager to write anything?

## Your answer

`OWNER`:

