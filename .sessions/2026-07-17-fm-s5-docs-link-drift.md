# Session — fm-s5-docs-link-drift

> **Status:** `complete`

**Branch:** `claude/fm-s5-docs-link-drift-0717`

📊 Model: Opus 4.8 · medium · feature build

**About to do:** Build S5 stdlib docs link-drift checker (`scripts/check_docs_links.py`) + fix broken intra-repo markdown links surfaced by the 2026-07-17 wind-down. This card held the PR red (HOLD) until the checker landed, the drift was resolved, and the enders were filled — then flips `complete` last.

**Did:**
- **Built `scripts/check_docs_links.py`** (stdlib-only, sibling-parity with `check_owner_queue.py` / `check_roster_freshness.py`): kit provenance + kill-switch header, `--selftest` (inline good/bad tempdir fixtures), `--advisory` (`::warning::` + exit 0), `--no-anchors`, `--list`, `--root`. Parses inline `[text](target)`, bare `<target>`, and reference-style `[label]: target` links; skips code fences, external (`http(s)`/`mailto`/`tel`/`ftp`), protocol-relative, and pure same-page `#anchor` links; resolves relative + root-absolute (`/…`) intra-repo targets; validates optional `#anchor` fragments against the target file's GitHub-slugged headings (+ `<a name/id>` and `{#custom}`), reported as a separate `dead-anchor` kind.
- **Scan set = living surfaces only:** `docs/**` + `control/README.md` + root `*.md` + `projects/**` + `environments/**` + `registry/**` + `templates/**`. EXCLUDES `.substrate/` (kit machinery), `scripts/fixtures/` (deliberately-broken test fixtures), `.sessions/` + `.session-journal*.md` (append-only historical byte records — the same OUT-of-scope discipline `check_owner_queue.py` check 3 applies). 252 files in the default set.
- **Severity convention chosen = sibling-parity:** default DIRECT run exits 1 on any dead link, `--advisory` → exit 0 (the repo's established advisory pattern — `check_owner_queue.py` / `check_roster_freshness.py` both do this). S5 is specced advisory / never-merge-blocking, satisfied structurally: the checker is **standalone**, NOT wired into `bootstrap.py check`, so it can never jam the substrate-gate.
- **Ran it — repo scanned CLEAN.** Default set (252 files, 386 intra-repo links) and a broadened confirmation over ALL 452 tracked `*.md` (incl. `.sessions/`) both returned **zero** dead-link / dead-anchor / encoding findings. The wind-down (PRs #288/#291/#292) left **no** broken intra-repo links — the kit's blocking `docs/`-only `check_links` kept `docs/` clean throughout, and wind-down housekeeping did not orphan references on the other surfaces. **No fixes were manufactured** (honest CLEAN, not filler — Q-0089/Q-0102 bar).
- **Detection verified against ground truth:** `--selftest` PASS (known-bad fires all 4 expected `dead-link`/`dead-anchor` kinds, skips a fenced link + an external `.md` link; known-good clean). Also injected one broken link into a real scanned file (`docs/NEXT-TASKS.md`) → the real-repo run flagged exactly it → reverted → CLEAN again. Confirms the CLEAN is real, not a false-negative parse gap.
- **Wiring:** left standalone/advisory by design — `bootstrap.py check` runs the engine's built-in checks and does NOT auto-discover `scripts/check_*.py` (the `scripts/` checkers run via workflows). Adding it to a blocking step could red the gate on the excluded historical surfaces or on future anchor-slug edge cases, so per the S5 brief it stays a standalone forward guard; verified `bootstrap.py check --strict` is unaffected (EXIT=0, only the born-red HOLD for this card).
- **Checks green:** `check_docs_links.py --selftest` PASS exit 0; `check_owner_queue.py` CLEAN exit 0; `check_roster_freshness.py` OK exit 0; `bootstrap.py check --strict` exit 0 (born-red HOLD on this card until this flip).

⚑ Self-initiated: None — directed deliverable (NEXT-TASKS.md item **S5**, owner-sanctioned first-build set for the recreated manager). No autonomous idea promotion; no trigger created, modified, fired, or deleted.

💡 Session idea: Extend the link checker (or a sibling) to also validate **backtick inline-code doc references** — `` `docs/foo.md` ``, `` `projects/bar/meta.md` `` — not just `[text](target)` links. The kit's `check_reachable` resolves backtick `<docs>/*.md` refs but ONLY to build the `docs/` reachability graph and ONLY inside `docs/`; nothing flags a **dead** backtick ref like `` `control/gone.md` `` on the non-`docs/` surfaces. That's a real residual gap this session's scope did not cover (S5 was `[]()`/ref-style links by spec). A `--backtick-refs` mode reusing this checker's resolver would close it cheaply. (Dedup-checked `docs/ideas/` + README — no existing link/drift/anchor/backtick-ref idea; novel.)

⟲ Previous-session review: The previous session (fm-recreation-ruling / PR #292) did the recreation-ruling recording with exemplary discipline — it not only recorded the ruling but discovered mid-task that the wake-chain arming it assumed was agent-doable is itself classifier-walled, and correctly filed that as an owner-queue ask (`OQ-FM-WAKE-CHAIN-ARM`) rather than pretending to resolve I4. Strong decide-and-flag. One thing it could have done better: its own session idea proposed a `scripts/check_wake_chain.py` dead-man detector but left it as prose — given the very next session (this one) was already tasked with *building a stdlib checker*, the two could have been sequenced together. Workflow improvement: the NEXT-TASKS first-build set (S3/S5/S9) and freshly-minted session-idea checkers should share ONE index so a checker idea born in session N is visible as a candidate for session N+1's build slot instead of re-derived — i.e. route session-idea checker proposals straight into `docs/NEXT-TASKS.md`'s build set, closing the idea→build loop the same way `docs/ideas/README.md` routes product ideas.
