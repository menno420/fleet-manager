# Session — fm-apparatus-sizing

> **Status:** `complete`

**Branch:** `claude/fm-apparatus-sizing-0718`

📊 Model: Opus 4.8 · medium · docs-only

**About to do:** Record the fleet-manager apparatus-sizing recommendation (NEXT-TASKS item 3) as an owner-queue decision — keep-vs-retire per workflow; decide-and-flag, no execution. This card holds the PR red (HOLD) until the decision record lands, checks stay green, and the enders are filled — then flips `complete` last.

**Did:**
- **Recorded `OQ-FM-APPARATUS-SIZING`** in `docs/owner-queue.md` under group (D) Standing decisions — a six-field + RISK, paste-ready keep-vs-retire recommendation for NEXT-TASKS immediate item 3, framed as a **right-sizing** pass (the recreation ruling, owner C / relay event 09027052, stood the loop back up), not a wind-down.
- **Per-workflow verdict, named against the real files** (from `ls .github/workflows/` + each file's provenance header): **KEEP** `merge-on-green.yml` (server-side landing path, not classifier-gated, load-bearing) · **KEEP** `substrate-gate.yml` (kit-owned merge gate) · **KEEP** `roster-freshness.yml` (roster-stamp PR gate) · **KEEP** the S3/S5/S9 advisory checkers (`scripts/check_owner_queue.py` / `check_roster_freshness.py` / `check_docs_links.py`, stdlib, zero apparatus coupling). **HOLD/right-size** `roster-regen.yml` — its cron is `40 */2 * * *` (every 2h → ~12 regens/day); recommended **reduce the cadence** (e.g. daily, keep `workflow_dispatch`) rather than delete, so it's reversible. **HOLD** `control/` message-bus + `telemetry/` snapshots as historical (the ORDER relay is already retired — `control/inbox.md` is historical — and no live workflow regenerates them).
- **Marked NEXT-TASKS item 3** "recommendation recorded → `OQ-FM-APPARATUS-SIZING` (awaiting owner)" with the one-line KEEP/HOLD summary and the decide-and-flag / owner-hub-venue-execution note.
- **Decide-and-flag, no execution:** deleted / edited **no** workflow. The RECORD is ✅ reversible; the cadence edit / any retire touches `.github/workflows/**` and is owner/hub-venue (classifier-gated for agents) — flagged with RISK ⚠️, not self-executed.
- **Checks green:** `check_owner_queue.py` CLEAN EXIT=0 (slug well-formed) · `check_roster_freshness.py` OK EXIT=0 (1.5h old, threshold 4h) · `check_docs_links.py` CLEAN (254 files, no dead link introduced) · `bootstrap.py check --strict` EXIT=1 == the born-red HOLD ONLY (this card in-progress during the run); the `model-line` advisories are **pre-existing on OTHER `2026-07-17-*` cards**, explicitly never-exit-affecting, and out of this docs-only session's scope. Guard-fire delta (5 records) committed with the session, not reverted.

⚑ Self-initiated: None — directed deliverable (NEXT-TASKS.md immediate item 3, the flagged apparatus-sizing decision). No autonomous idea promotion; no trigger created, modified, fired, or deleted.

💡 Session idea: **A `check_apparatus_provenance.py` advisory checker** (sibling to the S3/S5/S9 trio). Every fleet-manager self-apparatus workflow carries a kit-convention `PROVENANCE` + `Reliability` header with a "delete this if it proves unreliable / no longer needed" kill-switch line — exactly the metadata this sizing decision had to read by hand out of each `.github/workflows/*.yml`. A stdlib checker could parse those headers and flag a workflow whose kill-switch condition looks met (e.g. a `roster-regen` that fires on a cadence far tighter than the roster-freshness threshold needs, or an apparatus file whose motivating plan doc is itself retired), so the next right-sizing pass starts from a machine-generated keep/retire candidate list instead of a manual `ls` + header-read. Advisory only (never merge-blocking), standalone. Dedup-checked `docs/ideas/` (16 files) and the S3/S9/current-state-freshness ideas: those age *register verdicts / wall findings / the living-ledger snapshot*; this ages *the apparatus's own provenance headers against their kill-switch conditions* — novel and distinct.

⟲ Previous-session review: **PR #300 (fm-current-state-refresh)** did the right things: it refreshed `docs/current-state.md` to a true post-recreation snapshot while **preserving the file's section shape**, and — critically — verified every correction against live GitHub + repo state before writing (Q-0120 / playbook R2) rather than propagating the stale ledger, catching the wind-down→recreation framing flip that this whole apparatus-sizing decision depends on. It also cited verified SHAs for all 7 landed slices and confirmed the two new-file adds via `git log --diff-filter=A` rather than asserting them. One thing it could have done better / a system improvement it surfaces: #300 pointed its "Next action" at exactly this apparatus-sizing decision (item 3) but left it as prose the next session had to re-derive into a queue item — the cheapest enforcing fix (Q-0194 friction→guard) is that a `current-state.md` "Next action" naming an owner-decision should carry the target `OQ-` slug inline the moment the decision is recorded, so the ledger and the owner queue point at each other by id and neither can silently drift from the other. #300's own 💡 idea (a `check_current_state_freshness.py` that ages the dated header) is the complementary half — together they'd make the current-state ↔ owner-queue linkage both fresh and machine-checkable.
