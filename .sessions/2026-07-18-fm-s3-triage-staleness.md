# Session — fm-s3-triage-staleness

> **Status:** `complete`

**Branch:** `claude/fm-s3-triage-staleness-0718`

📊 Model: Opus 4.8 · medium · feature build

**About to do:** Build S3 stdlib fleet-triage staleness flagger (`scripts/check_fleet_triage_staleness.py`) — advisory re-verify prompt for register rows whose evidence pin has aged. This card held the PR red (HOLD) until the checker landed, was run against the real file + `--selftest`, the sibling checkers + strict gate stayed green, and the enders were filled — then flips `complete` last.

**Did:**
- **Built `scripts/check_fleet_triage_staleness.py`** (stdlib-only, sibling-parity with the S9 `check_capabilities_wall_age.py` / `check_owner_queue.py` / `check_roster_freshness.py` / `check_docs_links.py`): kit provenance + kill-switch header, `--selftest` (inline fixture), `--advisory` (`::warning::` + exit 0), `--max-age-days` (default 21), `--today YYYY-MM-DD` override (testability without touching the clock), `--triage PATH`. It ages each ACTIVE register verdict in `docs/fleet-triage.md` by the newest date substantiating it and flags any unsubstantiated past the window as **re-verify-due**, mechanizing the file's own §How-to-re-verdict discipline ("a verdict is a dated judgment, not a decree").
- **Grammar parsed** (reported to the coordinator up front): only the **REGISTER** table — the 6-column rows (`| Repo | Seat | Health | Verdict | rationale | next action |`) under the `## Register (verdicts as of YYYY-MM-DD …)` heading, scoped by entering on that H2 and leaving on the next H2 so the seat-restructure table and the dated sweep-note tables are **never** mis-parsed as register rows. Per row: repo = col 0 (strip `**bold**`, text before first ` (`); verdict = col 3, first recognized token of `KEEP-SEQUENCE | KEEP-PARKED | KEEP-QUIET | KEEP unarchived | KEEP | ARCHIVE | SEED | DELETE` (may be bold + trailed by a `*(re-verdict …)*` parenthetical). The register seed date is parsed from the heading (`2026-07-11`), used as the pin fallback for a row that carries no in-row date.
- **Evidence pin (freshest-wins), the load-bearing design.** An active row's pin = max of (1) the newest `YYYY-MM-DD` embedded in the row itself (a re-verdict / SHA-note / "post-seed" date), else the register seed date; and (2) the newest dated **sweep-NOTE** (a `## YYYY-MM-DD · …` section) whose body **names** the repo — the living register is swept ~daily, so a verdict named in a fresh sweep IS freshly substantiated. Repo-name matching is word-boundary with a hyphen-variant guard (`\bsuperbot\b(?!-\w)` so the `superbot` hub is NOT credited by a `superbot-next` / `superbot-idle` mention, while `superbot-next` still matches literally) — the exact false-credit class S9's name-collision rule warns about.
- **Frozen-by-design vs. active (how "stale verdict" is distinguished from "intentionally-parked stable state," the coordinator's explicit ask).** A finished/parked repo's verdict is a **settled state, not rot**, so it is EXCLUDED from aging and listed as a `[frozen-by-design]` note: verdict in `{ARCHIVE, SEED, DELETE}`, or `KEEP-PARKED / KEEP-QUIET / KEEP-UNARCHIVED`, or a row carrying a PRECISE stability marker (`dormant | wound-down | archive-ready | frozen | stale-by-design`). Only a plain `KEEP` / `KEEP-SEQUENCE` with no such marker is aged (mirrors S9's supersession-exclusion instinct). **Caught + fixed a real false-freeze in the build:** the first marker set included a bare `by[-\s]design` alternative, which wrongly froze the ACTIVE **sim-lab** KEEP because its rationale reads "idle-by-design **not stuck**" — a non-freezing use of the phrase. Every genuinely-frozen row is ALSO caught by its verdict token, so the bare marker was dropped for the precise phrasings only; sim-lab now reads correctly as active-and-fresh (substantiated by the 2026-07-17 sweep).
- **Ran it against the real file — CLEAN at the default 21d window** (`--today 2026-07-18`, EXIT=0): zero re-verify-due active verdicts, 6 `[frozen-by-design]` notes (pokemon-mod-lab / trading-strategy KEEP-PARKED · superbot-plugin-hello KEEP-QUIET · codetool-lab-fable5 / -sonnet5 ARCHIVE · codetool-lab-opus4.8 KEEP-UNARCHIVED). Honest CLEAN — the register is actively swept (2026-07-16 + 2026-07-18 notes name nearly every active repo), not manufactured. **Flagging path demonstrated on real rows:** at `--max-age-days 0` it fires `[reverify-due]` on 7 active rows with accurate sweep-note pins (e.g. `substrate-kit` last-substantiated `2026-07-16` via sweep-note, 2d); at `--today 2026-08-20` (default 21d) all **13** active rows age out with correct pins while the **6** frozen-by-design rows stay excluded — proving the active/frozen split holds even when everything is stale.
- **Detection verified against ground truth via `--selftest`** (EXIT=0): inline fixture asserts a stale active KEEP (in-row 2020, unswept) → flagged; an active KEEP-SEQUENCE named in a 2026-07-17 sweep → NOT flagged (evidence-pin path); an ARCHIVE row → `[frozen-by-design]` note; a KEEP-PARKED row → note; a repo appearing in the seat-restructure table → NOT leaked into register aging (scope isolation); and the `superbot` hub named in a fresh sweep (word-boundary, not via a hyphen variant) → substantiated, not flagged despite a 2020 in-row date. All six invariants pass. (Found + fixed a fixture self-bug during the build: the sweep note originally contained the literal string `repo-stale` in a "Nothing about repo-stale here" line, which self-credited the row it was meant to leave stale — reworded.)
- **Severity + wiring:** sibling-parity — default DIRECT run exits 1 on any re-verify-due verdict, 0 clean; `--advisory` → exit 0. S3 is specced ADVISORY / never merge-blocking, satisfied structurally too: the checker is **standalone**, NOT wired into `bootstrap.py check`, so it can never jam the substrate-gate.
- **Checks green:** `check_fleet_triage_staleness.py --selftest` PASS EXIT=0; `check_owner_queue.py` CLEAN EXIT=0; `check_roster_freshness.py` OK EXIT=0 (1.3h old); `check_docs_links.py` CLEAN (254 files); `check_capabilities_wall_age.py` CLEAN (30d); `bootstrap.py check --strict` EXIT=0 after this flip — the only red was this card's born-red HOLD (guard-fire delta committed, not reverted). Pre-existing `model-line`/seat-digest advisories on OTHER `2026-07-17-*` cards are explicitly "never exit-affecting" and out of this session's scope.

⚑ Self-initiated: None — directed deliverable (NEXT-TASKS.md item **S3**, owner-sanctioned first-build set for the recreated manager; last of the S3/S5/S9 trio, S5/S9 already merged). No autonomous idea promotion; no trigger created, modified, fired, or deleted.

💡 Session idea: **A register ↔ owner-queue linkage checker** (`check_triage_owner_links.py`, sibling to this S3 flagger and `check_owner_queue.py`). The register's §How-to-re-verdict pt 3 already states the rule — "a verdict whose *next action* is an owner click must have (or get) an `OQ-` item; cite the slug" — but nothing enforces it, so a row whose "single next action" column reads `Owner: …` / `owner-click` can silently carry **no** owner-queue slug, or cite an `OQ-`/`OA-` slug that no longer exists in `owner-queue.md`. A stdlib checker could parse the register's action column for owner-action phrasing, require an accompanying `OQ-…`/`OA-…` slug, and cross-check each cited slug against the live owner-queue — flagging both the missing-slug and the dangling-slug classes. It closes the loop the S3 checker leaves open: S3 ages *whether a verdict is still current*; this ages *whether the verdict's owner-gated next-step is actually tracked* — the two together make the register self-auditing on both axes. (Dedup-checked `docs/ideas/` + the S9 `--stamp` idea + PR #298's cite-the-live-HEAD SHA-freshness idea — no existing register↔owner-queue linkage / next-action-slug idea; novel and distinct from the staleness/SHA/wall family.)

⟲ Previous-session review: **PR #298 (fm-overnight-oversight — the 2026-07-18 overnight oversight sweep + verified idea-routing doc + two owner-queue items)** did the right high-leverage things: it re-verified every per-repo claim live against GitHub before writing (Q-0120, and it *caught the reviewer citing stale sibling SHAs* by doing so), it kept strictly oversight-only (recorded in this repo, nothing pushed to siblings), and it left durable traces (a dated `fleet-triage.md` sweep entry + a new `idea-routing` ledger + inline-slug owner-queue items) rather than letting the findings evaporate in chat. One thing it could have done better: the sweep it wrote is *exactly the substantiating evidence* this S3 checker now consumes — yet #298 recorded the sweep as free prose with the repo names spelled inconsistently in places (hub as "superbot (hub)" vs "superbot hub"), which this checker's word-boundary matcher had to be built defensively around; a lightly more **regular** sweep-row format (one canonical repo token per row) would let any downstream consumer credit rows unambiguously. Workflow improvement it surfaces: the sweep notes and the register are now *coupled* (S3 reads one to age the other), so the sweep-writing habit should converge on a stable per-repo token — the same "make the artifact machine-readable so the next tool doesn't guess" instinct that the roster's generated format already follows but the hand-written sweep notes do not yet. Cheapest enforcement would be a doc-lint that checks each dated sweep-note names its repos with their canonical register spelling — a natural companion to this checker.
