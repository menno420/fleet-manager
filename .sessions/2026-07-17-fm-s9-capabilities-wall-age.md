# Session — fm-s9-capabilities-wall-age

> **Status:** `complete`

**Branch:** `claude/fm-s9-capabilities-wall-age-0717`

📊 Model: Opus 4.8 · medium · feature build

**About to do:** Build S9 stdlib CAPABILITIES wall-age flagger (`scripts/check_capabilities_wall_age.py`) — advisory re-probe prompt for stale wall findings. This card held the PR red (HOLD) until the checker landed, was run against the real file + `--selftest`, the sibling checkers + strict gate stayed green, and the enders were filled — then flips `complete` last.

**Did:**
- **Built `scripts/check_capabilities_wall_age.py`** (stdlib-only, sibling-parity with `check_owner_queue.py` / `check_roster_freshness.py` / `check_docs_links.py`): kit provenance + kill-switch header, `--selftest` (inline fixture), `--advisory` (`::warning::` + exit 0), `--max-age-days` (default 30), `--today YYYY-MM-DD` override (testability without touching the clock), `--capabilities PATH`. It ages every WALL finding in `docs/CAPABILITIES.md` by its most-recent verified date and flags any older than the window as **re-probe-due**, mechanizing THE DISCOVERY RULE step 5 ("an entry older than the staleness window is a CLAIM, not a fact — re-verify").
- **Grammar parsed** (the two date-token formats the file actually uses): (1) append-log / mirrored-aggregate bullets `- YYYY-MM-DD · wall · <venue> · **…**` — 2nd `·`-field is the kind token (`wall` aged, `capability` skipped, `UPDATE` = supersession notice); (2) seed / prose bullets with `— LAST-VERIFIED: YYYY-MM-DD`, classified wall-vs-capability by section heading (`## Walls` / `## WALLED` → wall; `## Capabilities` / `## CAN …` → skip). Verified-date extraction is freshest-wins by priority: `LAST-VERIFIED:` → leading `- YYYY-MM-DD ·` → `(re-)verified/appended/added/recorded YYYY-MM-DD` → any date (max). A wall with no parseable date surfaces as an `[undated-wall]` note (no freshness data = itself a re-probe candidate) and does NOT affect the exit code.
- **Supersession exclusion (the load-bearing requirement).** A self-resolved wall must never nag. Two detectors: (a) an in-block resolution marker (`supersed…` guarded against `un-superseded`, `RESOLVED`, `no longer a (blanket) wall`, `RETIRES the recorded`, `retired … wall`); (b) a SEPARATE `UPDATE (supersedes …)` / `supersed…` bullet dated on/after the wall that shares a **distinctive title keyword** (len≥5, stopword-filtered) with it. Verified live on the canonical case: today's **Self-scheduling the seat wake chain — WALL** (L116) is EXCLUDED because the L128 `UPDATE (supersedes … self-scheduling WALL …)` bullet shares `scheduling` — exactly the "a wall that has since been lifted doesn't stay recorded as permanent" case S9 exists for. The `## Walls` seed **Environment / Project creation** wall (L85) is also excluded via its in-block "NO LONGER a blanket wall" marker.
- **Ran it against the real file — CLEAN at the default 30d window** (as of `--today 2026-07-17`): zero re-probe-due walls (every dated wall in the ledger is ≤8 days old — the whole ledger post-dates the 2026-07-10 recreation), with 8 notes (2 `[superseded]` incl. the self-scheduling case, 6 `[undated-wall]` on the folded-manifest prose walls that carry no date). Honest CLEAN, not manufactured. **Flagging path demonstrated** at `--max-age-days 5`: fires `[reprobe-due]` on 8 genuinely-dated walls (2026-07-09/10, 7–8d old — branch-deletion 403, `api.github.com` direct HTTP, self-merge classifier, GraphQL quota, devkitPro installers, private-repo auto-merge toggle, …) while STILL excluding the superseded self-scheduling + env-creation walls → EXIT=1; `--advisory` → EXIT=0.
- **Detection verified against ground truth via `--selftest`** (EXIT=0): inline fixture asserts a stale seed wall (2020) → flagged; a fresh wall (1d) → not; a superseded wall (older, but a later UPDATE supersedes) → excluded as a `[superseded]` note; an old *capability* → never a wall; a genuinely stale un-superseded prose-dated wall → flagged; an undated wall → `[undated-wall]` note, never aged. All six invariants pass.
- **Severity + wiring:** sibling-parity — default DIRECT run exits 1 on any re-probe-due wall, 0 clean; `--advisory` → exit 0. S9 is specced ADVISORY / never merge-blocking, satisfied structurally too: the checker is **standalone**, NOT wired into `bootstrap.py check`, so it can never jam the substrate-gate. Verified `bootstrap.py check --strict` is unaffected by the checker (its only red is this card's born-red HOLD until the flip; guard-fire delta committed, not reverted).
- **Checks green:** `check_capabilities_wall_age.py --selftest` PASS EXIT=0; `check_owner_queue.py` CLEAN EXIT=0; `check_roster_freshness.py` OK EXIT=0; `check_docs_links.py` CLEAN (253 files); `bootstrap.py check --strict` EXIT=0 after this flip (born-red HOLD released).

⚑ Self-initiated: None — directed deliverable (NEXT-TASKS.md item **S9**, owner-sanctioned first-build set for the recreated manager). No autonomous idea promotion; no trigger created, modified, fired, or deleted.

💡 Session idea: Add a `--stamp` / append-emit mode to this checker (or a sibling) that, for each `[reprobe-due]` wall, emits the ready-to-paste APPEND-log skeleton line (`- <today> · <re-verify pending> · <venue> · **<title>** · re-probe of the <date> finding · …`) so a session prompted to re-verify a stale wall gets the correct five-field/venue-tagged form handed to it rather than re-deriving the ledger grammar. It closes the loop between "you have a stale wall" (this checker) and "APPEND the result the right way" (THE DISCOVERY RULE step 4/5 format) — the same prep-the-paste, don't-hand-directions instinct as the owner-step skill. (Dedup-checked `docs/ideas/` + this session's NEXT-TASKS build set — no existing wall-age / re-probe / capabilities-stamp idea; novel.)

⟲ Previous-session review: PR #295 (fm-anthropic-email-pack — added a capability self-knowledge section to the Anthropic email pack) did the right high-leverage thing: it turned the fleet's hard-won capability/wall findings into an *outbound* artifact for Anthropic, which is exactly the kind of durable-trace-over-chat move the workflow rewards, and it correctly kept it in the email-pack lane rather than sprawling. One thing it could have done better: the very findings it packaged (the self-scheduling wall → nondeterministic-classifier correction, the deferred-ToolSearch lesson) are the same class this S9 checker now ages — the two are complementary but were built a session apart with no cross-link, so the email pack cites walls that nothing was yet re-probing. Workflow improvement: when a session packages capability findings for an external/owner audience, it should note in its card whether each cited wall is *fresh-verified* or *stale* — the wall-age checker now gives that a mechanical answer, so the email-pack (and any future capability digest) could run `check_capabilities_wall_age.py --advisory` first and annotate "N walls re-probe-due" rather than presenting a possibly-stale snapshot as current. That makes the outbound artifact self-dating.
