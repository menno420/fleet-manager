# 2026-07-10 — kit upgrade v1.4.0 → v1.7.0 (distribution wave)

> **Status:** `complete`

## What happened

Distribution worker (kit v1.7.0 wave, owner directive Q-0261.3): upgraded the
vendored substrate-kit **v1.4.0 → v1.7.0** on PR #35.

- **Canonical release-asset path:** `bootstrap.py` + `release.json` fetched
  from the v1.7.0 GitHub release; asset sha256
  `00f4f4cd39351b17389b9abab3be88fcb0c9f4dee9ad8f1639ad1fc67fdb5238` verified
  before running `python3 bootstrap.py.new upgrade` (archive-first; old dist
  banked at `.substrate/backup/bootstrap-1.4.0.py`, marker in
  `.substrate/backup/last-upgrade.json`).
- **Live gate refreshed** from the regenerated staged copy
  (`cp .substrate/ci/substrate-gate.yml .github/workflows/substrate-gate.yml`
  — kit-owned per kit PR #130): no-card advisory sentinel, born-red
  ADDED-card advisory, MODIFIED-card locked door.
- **`upgrade --apply-docs`** re-rendered the two provably consumer-untouched
  template-improved docs (`CONSTITUTION.md`, `docs/collaboration-model.md` —
  CAPABILITIES discovery rule + OWNER-ACTION ask discipline). Diverged docs
  (`docs/AGENT_ORIENTATION.md`, `control/README.md`) left for the lane —
  template deltas recorded in `.substrate/upgrade-report.md`.
- **New plant:** `docs/CAPABILITIES.md` (v1.5.0 ORDER 006 ledger seed).
- **Verify:** `python3 bootstrap.py check --strict` green on this tree apart
  from (a) this card pre-flip (by design) and (b) one pre-existing
  advisory-only `owner-action-fields` warning on `control/status.md`
  (lane-owned, never exit-affecting).
- **Scope fence held:** `control/inbox.md` / `control/status.md` untouched.

## Lane-owed follow-ups (NOT done here, by scope fence)

1. **Heartbeat `kit:` line** in `control/status.md` still says `v1.4.0` —
   update to `kit: v1.7.0 · check: green · engaged: yes` on the next
   heartbeat overwrite (v1.3.0 ORDER 003 duty; the adopters registry
   currency checker flags DRIFT until then).
2. **Diverged docs manual merge** (optional): fold the v1.7.0 template
   deltas into `docs/AGENT_ORIENTATION.md` (CAPABILITIES read-path entry)
   and `control/README.md` (order-claiming ritual + OWNER-ACTION format) —
   exact diffs are in `.substrate/upgrade-report.md`.
3. **New conventions now live:** order claiming (`claimed-by:` before
   executing an inbox ORDER), six-field OWNER-ACTION asks (advisory nag
   already firing on the current status ⚑ list), inbox pure-append,
   born-red ADDED-card = advisory / close-out MODIFIED-card = locked door.

## 💡 Session idea

The v1.7.0 staged gate template does NOT wire `check --inbox-base` into the
live workflow (verified: `grep inbox .github/workflows/substrate-gate.yml`
matches only a comment), so the v1.7.0 "inbox append-only, CI-gated" claim
is latent until a post-v1.7.0 kit release ships the wired template. Idea:
kit-side, add a gate-template currency line to the upgrade report (template
version stamp in the workflow header) so an upgrade worker can tell at a
glance which enforcement clauses the installed gate actually carries —
worth routing to substrate-kit as a KI candidate.

## ⟲ Previous-session review

The ~19:50Z critical-finding session (PR #34) was a model of evidence
discipline — every cross-surface claim carried a panel/PR/line citation,
and the probe (create_trigger schema dump) turned an ambiguous owner report
into a bounded platform limitation. One improvement it surfaces: its
finding landed doctrine riders in four places but no single index of
"platform bugs we've filed" — a one-table `docs/findings/` index would keep
the fourth-platform-bug class discoverable. No process failure to flag; the
born-red flow worked as designed.

📊 Model: fable-5 (family-level, self-reported from this session's own harness) · kit-distribution worker (coordinator-dispatched) · start 2026-07-10T19:57Z (`date -u`)
