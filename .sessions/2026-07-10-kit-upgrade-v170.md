# 2026-07-10 — kit upgrade v1.4.0 → v1.7.0 (distribution wave)

> **Status:** `in-progress`

## What is about to happen

Distribution worker (kit v1.7.0 wave, owner directive Q-0261.3): upgrade the
vendored substrate-kit from **v1.4.0 → v1.7.0** using the canonical
release-asset path (`bootstrap.py.new` + `release.json` from the v1.7.0
GitHub release, then `python3 bootstrap.py.new upgrade`), refresh the live
gate from the regenerated staged copy
(`cp .substrate/ci/substrate-gate.yml .github/workflows/substrate-gate.yml`
— kit-owned per kit PR #130), verify `python3 bootstrap.py check --strict`
green, and land the PR per this repo's conventions (born-red card, READY,
REST squash-merge on green per playbook R21).

Scope fence: kit-owned files only. No lane-owned content — `control/inbox.md`
and `control/status.md` are untouched (the lane updates its own `kit:`
heartbeat line on its next heartbeat).
