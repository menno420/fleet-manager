# 2026-07-16 — pr-audit-0716 (fleet-wide PR-landing audit)

> **Status:** `complete`
> **Branch:** claude/pr-audit-0716

Intent: land the fleet-wide PR-landing audit report — a point-in-time,
read-only snapshot of PR auto-landing health across all 19 fleet repos
(companion to fm ORDER 048), plus a current-state ledger link to it.
Records/oversight PR only — no runtime code, no trigger touched. Under fm
ORDER 048 landing discipline: decide/build/land-on-green via
merge-on-green.yml — this session does NOT self-arm or self-merge.

## What happened

- **(a) Report landed:** `docs/pr-landing-audit-2026-07-16.md` — badge token
  `audit`; 19-repo PR-landing snapshot (two landing mechanisms, per-repo
  open-PR diagnoses, summary table, recommended owner actions). Findings in
  brief: 19/19 repos have a working landing workflow at HEAD; ~250 of ~259
  48h merges automatic (merge order = per-PR CI-completion, not a queue); 2
  genuinely stuck (websites #359 auto-merge-not-armed, pokemon-mod-lab #87
  conflict); ~13 deliberate drafts (classifier-denial parks) are the main
  "why won't this merge" bucket.
- **(b) Ledger link:** `docs/current-state.md` ▶ Recently shipped (newest
  first) gained a real markdown link to the report (fm #265).
- **PR #265** opened READY; lands on green via `merge-on-green.yml` — this
  session did NOT self-arm or self-merge. Born-red card flipped to
  `complete` as the final commit.

## Enders

- **📊 Model:** Claude Opus 4.x family · high · review/verify
- **💡 Session idea:** ship `scripts/classify_open_prs.py` — a stdlib-only
  classifier that, given a directory of raw `list_pull_requests` + head-SHA
  check-run JSONs, folds each open PR's (draft? carve-out label?
  mergeable_state? all-checks-green?) into a `held-by-design | stuck |
  pending` verdict with the reason, so the "is this PR actually stuck or just
  parked?" question this audit answered by hand becomes a one-command
  repeatable readout. Turns the fleet's single hardest-to-eyeball PR
  distinction into a deterministic step; mirrors the `check_trigger_health.py`
  / `assemble_triggers_snapshot.py` "stop re-deriving it every wake" pattern.
  (Dedup-grepped `docs/ideas/` — no existing idea covers open-PR
  classification.)
- **⟲ Previous-session review:** the trigger-snapshot-pm wake (fm #264) did
  the right thing recording I8 DUPLICATE-CRON as a *report-only* sibling-lane
  call rather than deleting cutover-window duplicates it didn't own — honest
  lane discipline, not overreach. System improvement it surfaces (echoed by
  this audit): "held-by-design vs genuinely-stuck" is the recurring
  hard-to-tell distinction across the whole fleet — a draft/`blocked`/`dirty`
  PR looks stuck but usually isn't. A committed `scripts/`-side classifier
  that folds each open PR's (draft? label? mergeable_state? head-SHA checks?)
  into a `held-by-design | stuck | pending` verdict would turn this
  hand-driven per-repo audit into a repeatable one-command readout — the same
  "stop re-deriving the error-prone step every wake" theme as #264's
  assemble-snapshot idea.
