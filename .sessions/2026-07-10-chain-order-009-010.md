# 2026-07-10 — continuation-chain slice #2 (ORDER 009 generated roster v1 + ORDER 010 model matrix)

> **Status:** `complete`

📊 Model: fable-5 (family-level per Q-0262; self-report from the session harness) · start 2026-07-10T21:05Z (`date -u`)

## Declared at open (born-red)

Work-loop worker executing the coordinator's second `send_later` continuation-chain
slice (Q-0265 continuous mode; chain fire ~21:00Z). Planned slice = **ORDER 009
(generated roster v1, per `docs/proposals/generated-roster-from-heartbeats.md`) +
ORDER 010 (fleet model-matrix sweep)**, per the status.md next-chain-slice pointer.
About to land, in this PR:

1. **Evidence sweep (read-only, pre-write):** per-repo `control/status.md` (or
   equivalent) at HEAD over git transport for every fleet lane (superbot,
   superbot-next, substrate-kit, websites, trading-strategy, venture-lab,
   superbot-games, pokemon-mod-lab, gba-homebrew, product-forge, idea-engine,
   sim-lab, codetool ×3, fleet-manager) + live `list_triggers` snapshot for wake
   state; plus newest ~3–5 session cards per repo for `📊 Model:` lines (bounded).
2. **`docs/roster.md`** — generated roster v1 (generation #1): one row per lane —
   status stamp + freshness age, phase, orders state, kit version, trigger
   name/cron/last-fire — with a generated-at header, source-of-truth note, and the
   proposal's kill-switch note (stale >24h → trust the heartbeats directly).
3. **`docs/playbook.md`** — one-line regeneration duty in the manager wake doctrine
   (provenance ORDER 009).
4. **`docs/findings/model-matrix-2026-07.md`** — per-repo model matrix (ORDER 010):
   configured Project model (mostly unknown — honest nulls), card-self-reported
   families, routine-vs-manual where determinable, the websites 16:01Z
   cross-surface disagreement cited; conclusion on what the matrix can/cannot
   establish.
5. **`control/inbox.md`** — ORDERs 009 + 010 → ✅ DONE (citing this PR); phase-2
   manifest→pointer-stub decision noted decide-and-flag (NOT executed this slice).
6. **`control/status.md`** — heartbeat overwrite as the deliberate last content
   step: chain-slice record, orders footer (inbox now clear of open orders).
7. **This card**, flipped `complete` as the deliberate last commit.

Landing: born-red card holds the gate red; work commits next; gate run
(`python3 bootstrap.py check --strict`) before the flip; flip last; REST
squash-merge on the branch's substrate-gate Actions run going green. After the
merge: re-arm the coordinator's continuation chain (`send_later`, ~15 min).

## Done (close-out) · end 2026-07-10T21:45Z (`date -u`)

All seven declared items landed. Evidence chain: inbox ✅ records (ORDERs 009 + 010),
`docs/roster.md` header + per-row evidence, matrix evidence column, status.md
chain-slice record. Notes beyond the declaration:

- **Roster = 17 rows** (superbot-games split per-lane per the proposal's split-repo
  clause); trigger evidence from a **99-record** `list_triggers` sweep (16 enabled) —
  richer than expected: five seats now run failsafe wakes (fm, Builder, idea-engine,
  product-forge, sim-lab, + trading created 21:03Z), and live `send_later` chain
  one-shots confirm multiple hot chains.
- **Action-worthy staleness found:** venture-lab 16h23m stale with 3 unconsumed ORDERs
  (incl. the P0 Stripe fix) and NO trigger — its "ride the next boot" plan has no
  scheduler behind it; pokemon-mod-lab's ORDER 003 also sits unconsumed post-heartbeat.
  Both surfaced in the roster verdicts + heartbeat in-flight list.
- **Drift fixed on sight (Q-0166 class):** this repo's own status `kit:` line claimed
  v1.4.0 while `substrate.config.json`/`KIT_VERSION` are 1.7.0 (the #35 upgrade);
  corrected in the heartbeat with provenance. Also fixed mid-flight: roster.md needed a
  kit Status badge (gate finding) — `living-ledger` chosen since the file is a
  regenerated ledger.
- **Decide-and-flag calls:** (a) generation #1 executed as a wake *procedure*, not a
  script — `tools/gen_roster.py` mechanization deferred to the parallel-run wake so the
  format proves itself before it's frozen in code; (b) phase-2 manifest→pointer-stub NOT
  executed (coordinator directive, roster proves itself first); (c) matrix covers all 16
  repos, not only routine-armed lanes — the ORDER's done-when is a subset, and the extra
  rows cost one bounded sweep while making the null conventions visible.
- **Model-matrix headline:** one unchanged websites trigger produced sonnet-5 (16:01Z)
  and fable-5 (20:00Z) sessions on the same day — the Routines screen's uniform fable-5
  display is demonstrably not authoritative; per-session self-report stands as the
  least-bad basis (already doctrine).

💡 **Session idea:** the roster's "Age" column conflates two different clocks —
*heartbeat age* (when the lane last wrote status) and *consumption lag* (orders landed
in an inbox after the last heartbeat, invisible to freshness math). venture-lab and
pokemon both look merely "stale" but are actually **starving** (work waiting, no
scheduler). Generation #2 should add a derived `unconsumed-orders` marker per row —
computable from inbox HEAD vs the status `orders:` line, zero lane-side work — so
"stale but idle" and "stale and starving" stop looking identical.

⟲ **Previous-session review (chain slice #1, PR #37 — ORDERs 003/007/011):** strong
slice — the two-tier drainer design (Codex primary / manager fallback) was the right
generalization of the ORDER's "name a drainer", and re-verifying BOTH halves of the
re-arm against the full trigger listing (not just the create call) is exactly the
verify-against-registry discipline R2 wants. One miss: its status heartbeat flagged
trading#36 as time-sensitive "pre-holdout" — but trading's own heartbeat (21:08Z) shows
the holdout already executed via #37, so the flag was stale within the hour it shipped.
**Workflow improvement:** time-sensitive flags in the manager heartbeat should carry
the *invalidating condition* ("until trading ORDER 008's dedicated session runs"), so
the next writer can retire them mechanically instead of re-deriving; applied in this
slice's heartbeat (the review-queue drain bullet retires the flag with the evidence).

📎 Family-level model names only throughout (Q-0262); no new owner-queue asks added
(net: venture-lab starvation folded into the existing boot-click ask).
