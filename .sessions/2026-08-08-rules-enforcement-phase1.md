# 2026-08-08 · hub — Phase 1: make the corpus trustworthy enough to retrieve from

> **Status:** `in-progress`

- **📊 Model:** fable-5 · high · feature build — retrieval & orientation repair, plus the roadmap that frames it

Time: 2026-08-08 · venue: owner-live hub chat · branch
`claude/fleet-manager-rules-enforcement-18o8t1`

💡 Session idea: **an orientation surface that is wrong costs more than one that
is missing.** A missing doc produces a search; a stale router produces confident
action in the wrong direction, and nothing downstream can tell the difference.
That is why this session's largest single change is not an addition — it is a
supersession banner on the one line of the program doc that still tells every
off-path reader the next step is a task the owner reserved twelve days ago.

## What is about to happen

Phase 1 of the agent-operating-environment roadmap: repair the retrieval and
orientation layer so the corpus is trustworthy enough to support intent
resolution (Phase 2). Boot-file surgery at net-zero words · session-close
carrying its DECIDED Layer-2 step · one honest prompt route + the admission bar
· live misdirections fixed at source and stale routers flagged · the three-phase
roadmap committed so the direction outlives this chat. Then the Layer-2
re-verification, with the falsification the acceptance doc admits was never run.

## Previous-session review

⟲ The 2026-08-08 index/instruments sessions (#817–#825) built the five injection
mechanisms and the finding that organises them, and stopped at exactly the right
place: they mechanised the *action* moment and left the *orientation* moment
alone. This session is the other half, and it inherits their central result —
rules bind only when they arrive — which is why almost nothing here is new prose
and most of it is deleting or correcting text that already arrives.

## Verification

(to be filled at close — real exit codes, each command run on its own)
