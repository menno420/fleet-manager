# Session — the phase-3 + gba-bypass review: four self-flagged items, settled

> **Status:** `in-progress` — branch `claude/v1-21-0-phase3-gba-tcvym8`, PR #858.
> Born-red hold: this card flips only when the review corrections, the
> `[D-0017]` capture and the §7 row are landed and verified.

- **📊 Model:** fable-5 · high · review/verify

## previous-session review — the four self-flagged items, each settled

Adversarial, per the wave's own flags; full evidence in
[`docs/findings/2026-08-14-v1210-phase3-review.md`](../docs/findings/2026-08-14-v1210-phase3-review.md):

1. **mineverse gate-regen residue: NONE, MEASURED.** The merged #144 diff
   touches no `.github/workflows/` path; both live workflows at `main` are
   blob-identical to the pre-#144 parent (`a354a27af6` / `9feb8bcb13`), the
   live gate read directly carries zero pytest/requirements references, and
   `main` == the #144 squash with 0 commits after. The round-2 revert held.
2. **The gba turn's confident claims: all siblings of the corrected one
   re-measured true** — ruleset `['NDS ROM build', 'ROM builds']` active;
   #215 merged 05:58:15Z inside the recorded 3-second bypass window; mirror
   release exactly 1 asset; branch `claude/nds-toolchain-1-22-3` retained;
   tree at dist `8807a00e…` / pin 1.21.0; and `rom-builds.yml` verified in
   the tree boot-running the NDS titles under py-desmume (twenty-eight + twelve
   headless proofs — the harness that caught the regression).
3. **Underroot half-rate: scope discipline holds everywhere.** Every committed
   record says "under this SDK in the emulator"; nothing rounds it up to "the
   game is broken" or down to "goldens drifted". Hardware behaviour LEFT OPEN
   with the reason: no hardware venue exists; the owner play test is the
   migration step where that answer lives.
4. **idea-engine: gate MEASURED green at `main`** via the squash-tree method
   (#899 head tree `c3862e0a67` == `main` `320acabd9`, substrate-gate success
   on that head; push-runs on `main` are GITHUB_TOKEN-suppressed — another
   sighting of the known mechanism). `ideas/shiftlife/` exists. The
   required-check-vs-retired-roster question stays **flagged, not decided**
   (⚑ below).

Also re-derived rather than inherited: the registry at HEAD (stamp
`2026-08-14T05:59:49Z`) — 9 current · 3 stale, with superbot-games' 3-file
DRIFT re-measured at its source heartbeats and pokemon-mod-lab's private tree
read over direct PAT; the 24-repo sweep — 2 open `claude/*` fleet-wide
(sim-lab #344 · trading-strategy #160, both July parks), fm 0 open at
`108b452`, one post-snapshot delta (superbot #2444, its own dashboard
automation, created 09:19:01Z).

## Shipped

- `docs/findings/2026-08-14-v1210-phase3-review.md` — the four verdicts +
  the re-derivations, mirroring the phase-2 review method.
- `docs/decisions.md` — **`[D-0017]`**: nothing waits in an open PR — open
  means being driven to a terminal state now; future work lives in records
  and branches. The ruling existed only in chat + the gba #216 close comment.
- `.sessions/2026-08-14-gba-bypass-merge.md` — dated ⟲ annotation at the
  superseded "#216 holds open as the migration vehicle" claim (closed-unmerged
  06:41:13Z, branch retained).
- Program §7: the review row appended (fm #858); the phase-3-completes row's
  "#216 (open)" corrected by the new row, append-only. NOW unchanged.

## ⚑ Flagged for the owner — decisions not taken

- **Which adopter next** — superbot-games (v1.20.1 hop + reconcile its 3-file
  self-report DRIFT at the source heartbeats) or pokemon-mod-lab (v1.15.0,
  private, biggest jump). Posed at session end; neither assumed.
- **sim-lab #344 and trading-strategy #160** — the two July parks now sit
  against `[D-0017]`; #344's own record already says "land or close".
  Land-or-close is the owner's call, posed alongside the adopter question.
- **idea-engine's required gate consults a roster frozen at retirement**
  (`check_sections`; `ideas/shiftlife/` exists to satisfy it). Re-point,
  prune, or keep — an idea-engine design call, recorded in the finding.
- Carried unchanged from the wave card: substrate-gate required on
  superbot-next (costed MEASURED) · the orientation-budget restructure (still
  freezing `current-state.md`) · the gba 1.22.3 migration on its retained
  branch (first question: WHY Underroot halves under libnds 1.22; gba's
  required NDS check reds every future PR there until it lands — that urgency
  is the owner's to weigh) · mineverse's capability-seed fence surgery
  (deferred with reason on its card).

## Verify

- fm gate: `python3 bootstrap.py check --strict` → exit 1 on exactly the
  designed born-red hold naming this card (`in-progress`), verified against
  the findings header not the tail; `tools/check_no_false_walls.py --strict`
  → exit 0 CLEAN. (Both real exit codes, no pipes.)
- Every cross-repo claim above was measured this session over the direct-PAT
  path (raw contents, blob SHAs, check-runs, rulesets, PR states) — never a
  PR-body read. PR #858's own substrate-gate red on the born-red heads is the
  designed hold (job log: absent-card/in-progress sentinel), not a failure.

Layer-2 handoff: null (fleet-manager itself; no `docs/repos/<name>/` folder
exists for the reviewed wave repos — their own cards and PR threads carry it)

- Capability delta: none new — pokemon-mod-lab's private-tree read over the
  direct-PAT API reconfirms the existing egress-scoping entry in
  `docs/CAPABILITIES.md`'s append log; nothing appended, no wall recorded.

💡 **The registry's DRIFT lines are a ready-made rider for the next hop:** the
three stale `kit:` heartbeat lines this review measured at source
(superbot-games `status.md` v1.15.0 · `status-mining.md` v1.7.1 ·
`status-exploration.md` v1.7.1) are exactly what its upgrade session should
reconcile in the same PR — one commit clears the registry's only DRIFT row,
and the wave precedent (fm #855's per-adopter Codex rounds) already covers
the review cost.
