# Session — fm-owner-steps

> **Status:** `complete`

**Branch:** `claude/fm-owner-steps-0718`

📊 Model: Opus 4.8 · medium · docs-only

**About to do:** consolidated fleet-wide owner-steps deliverable (owner order 210e7bb8) — write `docs/owner-steps-2026-07-18.md`, a single payoff-ordered, one-sitting-batched, paste-ready list of every open owner-only step across the fleet. HOLD (born-red) until the doc lands, is reachable + badged, checks are green, and the enders are filled — then flips `complete` last.

**Did:**
- **Wrote `docs/owner-steps-2026-07-18.md`** — a single consolidated, payoff-ordered deliverable covering every open owner-only step across the whole fleet, in the prep-owner-steps grammar (plain language; grouped into one-sitting BATCHES; each item = WHAT → WHY/UNBLOCKS → STEPS (every click) → LINKS (full absolute URLs, file refs as `/blob/main/`) → VERIFY; exact paste-blocks inline). Badge `audit` (copied from the sibling `owner-actions-2026-07-17.md`), NOT `complete` so the reachability gate stays green.
- **8 batches + appendix:** B1 five-minute unblocks (GBA #165 merge, 10 GBA draft ready-flips, curious-research slicer word, Lumen Drift release, product-forge Pages, superbot-idle `pytest` required); B2 GitHub settings/rulesets (gba required-checks, pokemon-mod-lab protect main, substrate-kit check swap, superbot-next retention/merge-queue); B3 secrets & tokens (one websites PAT → GITHUB_TOKEN + BAKE_PAT, Railway Postgres/SITE_PASSWORD, PayPal Payouts, mineverse ingest/write/OAuth vars, superbot-next prod env, fleet-manager ROSTER_READ_TOKEN); B4 money/publish (Stripe test keys, 10 Gumroad kits with exact prices, Amazon KDP, park the $1.2k–$5.6k commissions, SWTK T+14 kill-check 2026-07-26, websites Q-0004 control decision); B5 standing one-word decisions (delete-vs-archive=A gate, apparatus-sizing, substrate-kit ×3, games ×4, venture D1–D19, trading-strategy, idea-engine #481, curious-research drybox); B6 gated releases (cfgdiff, envdrift, product-forge archive, sim-lab); B7 optional/cosmetic (stale branches, veto passes, triggers); B8 time-gated (after EAP read-only 2026-07-21). Appendix lists the verified-done items removed from the list.
- **Reachability + badge:** linked the new doc from `docs/current-state.md` (Recently-shipped list, above the 07-17 line, noting it supersedes the older list). `check_docs_links.py` CLEAN (255 files, every intra-repo link resolves).
- **Checks green:** `check_owner_queue.py` CLEAN EXIT=0 · `check_roster_freshness.py` OK EXIT=0 (0.9h old) · `check_docs_links.py` CLEAN · `bootstrap.py check --strict` EXIT=1 == the born-red HOLD ONLY (this card in-progress at run time); the seat-digest + 8 model-line advisories are **pre-existing on OTHER `2026-07-17-*` cards**, explicitly never-exit-affecting, and out of this docs-only session's scope. This card's own model-line is taxonomy-clean. Guard-fire delta (14 records) committed with the session, not reverted.
- **Born-red discipline:** card pushed FIRST (commit `d515f55`), PR #306 opened READY immediately, doc + link committed (`00bea42`), card flipped `complete` LAST.

⚑ Self-initiated: None — directed deliverable (owner order 210e7bb8, the consolidated fleet-wide owner-steps doc). No autonomous idea promotion; no trigger created, modified, fired, or deleted.

💡 Session idea: **A `check_owner_steps_freshness.py` advisory checker** that reads the newest `docs/owner-steps-*.md` and, for every item that cites a specific PR/release URL as still-open (e.g. "merge PR #165", "release 404"), re-queries live state and flags any that have since resolved — so the next consolidation starts from a machine-generated "these moved to done" candidate list instead of the manual per-PR re-harvest this doc required. Advisory only, stdlib, standalone. Dedup-checked `docs/ideas/` and the apparatus-provenance / current-state-freshness ideas: those age *the apparatus's own workflow headers* and *the dated current-state snapshot*; this ages *owner-step citations against live GitHub PR/release state* — the exact drift this deliverable had to close by hand — and is distinct.

⟲ Previous-session review: **PR #303 / the apparatus-sizing session** did the right things: it recorded a keep-vs-retire recommendation as a decide-and-flag queue item (`OQ-FM-APPARATUS-SIZING`) named against the real `.github/workflows/*.yml` files rather than from memory, and correctly refused to self-execute the cadence edit (which touches `.github/workflows/**` and is classifier-gated for agents) — flagging it with RISK instead. One thing it could have done better / a system improvement it surfaces: the apparatus-sizing recommendation and the delete-vs-archive standing decision are now **two separate owner-queue items that gate overlapping work** (this deliverable had to cross-reference both to order Batch 5 vs Batch 6), yet neither points at the other by slug. The cheapest enforcing fix (Q-0194 friction→guard) is that when an owner-queue decision *gates* another queued item, the record should carry the gated item's `OQ-`/batch id inline — so a consolidation pass can order them mechanically instead of re-deriving the dependency each time. The apparatus session's own 💡 (a `check_apparatus_provenance.py`) is the complementary half; together they'd make the queue both self-provenanced and self-ordered.
