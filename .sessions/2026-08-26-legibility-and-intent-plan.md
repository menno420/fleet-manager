# 2026-08-26 — the legibility plan: why the kit's next-agent contract is not being kept

> **Status:** `in-progress` — born red on purpose. About to happen: measure the
> owner's own hypothesis that substrate-kit is not doing its job in the repos,
> then write the plan he asked for into `docs/planning/`. Flips to `complete`
> only after `python3 bootstrap.py check --strict` returns a real exit 0 read
> from the process, and after `@codex` answers at the exact head.

- **📊 Model:** opus-5 · high · idea/planning
- **📍 Venue:** cloud-container

## 💡 Session idea

The owner said he suspected the kit was not doing its job — that every agent
should be contributing ideas and journals for the next agent, and is not. That
is a measurable claim about 14 repositories, and measuring it first is what
turns a suspicion into a plan with a number in it.
