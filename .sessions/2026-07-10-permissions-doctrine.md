# 2026-07-10 — Permissions & authority doctrine fold (owner directive ~22:3xZ)

> **Status:** `in-progress`

📊 Model: fable-5 (family-level per Q-0262 policy 1) · start 2026-07-10T22:17Z (`date -u`)

## Declared at open (born-red)

Worker executing the owner's ~22:3xZ bulk-doctrine directive: every Project's
Custom Instructions must carry an explicit full-permissions section naming
permissions by name (prevents the stall class where seats refuse to merge
their own green PRs or park reversible work as "owner-required"). Plus the
~22:1xZ scope addition: the owner's in-session grant cleared the product-forge
classifier merge wall (PRs #12/#13 merged) — one grant-recipe sentence folds
into the canonical block. About to land, in this PR:

1. **Canonical `## Permissions & authority` block** (verbatim-identical) into
   all 13 `projects/<repo>/instructions.md` paste bodies, near the top; each
   file version-bumped (v1→v2); char budgets held ≤~7,000 per paste body by
   tightening redundant prose (per-file trims recorded in this card).
2. **One Permissions line** into each of the 13
   `projects/<repo>/coordinator-prompt.md` (version-bumped).
3. **`projects/UNIVERSAL.md`** — fleet-canonical wording home for the block
   (v2→v3).
4. **`projects/README.md`** — registry doctrine note: block mandatory in every
   instructions.md; paste wave carries the NEW v2s.
5. **`docs/owner-queue.md`** — new click item: product-forge GitHub Pages
   enable (games-web publish; deploy workflow already on forge main, PR #13).
6. **`projects/venture-lab/meta.md` + instructions** — its documented merge
   wall is the same classifier class; grant recipe applies.
7. **`control/status.md`** heartbeat (deliberate last content step).
8. This card, flipped `complete` as the deliberate last commit.

Landing: born-red card holds the gate red; work commits next; `python3
bootstrap.py check --strict` before the flip; flip last; REST squash-merge on
the branch's substrate-gate run going green (no auto-merge arm — R21).
