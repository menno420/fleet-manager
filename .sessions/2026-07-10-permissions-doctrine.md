# 2026-07-10 — Permissions & authority doctrine fold (owner directive ~22:3xZ)

> **Status:** `complete`

📊 Model: fable-5 (family-level per Q-0262 policy 1) · start 2026-07-10T22:17Z (`date -u`)

## Declared at open (born-red)

Worker executing the owner's ~22:3xZ bulk-doctrine directive: every Project's
Custom Instructions must carry an explicit full-permissions section naming
permissions by name (prevents the stall class where seats refuse to merge
their own green PRs or park reversible work as "owner-required"). Plus the
~22:1xZ scope addition: the owner's in-session grant cleared the product-forge
classifier merge wall (PRs #12/#13 merged) — one grant-recipe sentence folds
into the canonical block. About to land, in this PR:

1. **Canonical `## Permissions & authority` block** (verbatim-identical) into
   all 13 `projects/<repo>/instructions.md` paste bodies, near the top; each
   file version-bumped (v1→v2); char budgets held ≤~7,000 per paste body by
   tightening redundant prose (per-file trims recorded in this card).
2. **One Permissions line** into each of the 13
   `projects/<repo>/coordinator-prompt.md` (version-bumped).
3. **`projects/UNIVERSAL.md`** — fleet-canonical wording home for the block
   (v2→v3).
4. **`projects/README.md`** — registry doctrine note: block mandatory in every
   instructions.md; paste wave carries the NEW v2s.
5. **`docs/owner-queue.md`** — new click item: product-forge GitHub Pages
   enable (games-web publish; deploy workflow already on forge main, PR #13).
6. **`projects/venture-lab/meta.md` + instructions** — its documented merge
   wall is the same classifier class; grant recipe applies.
7. **`control/status.md`** heartbeat (deliberate last content step).
8. This card, flipped `complete` as the deliberate last commit.

Landing: born-red card holds the gate red; work commits next; `python3
bootstrap.py check --strict` before the flip; flip last; REST squash-merge on
the branch's substrate-gate run going green (no auto-merge arm — R21).

## Done (close-out) · end 2026-07-10T~23:05Z (`date -u`)

**Split outcome — grant fold BUILT but HELD by the instruction-poisoning
guard; clean parts landed (PR #48).** The full fold was written: canonical
block (directive text + grant-recipe sentence — product-forge evidence:
owner's in-session grant cleared the classifier merge wall, PRs #12/#13
merged) verbatim-identical in all 13 instructions bodies (→ v2); companion
one-liner in all 13 coordinator prompts (→ v2); UNIVERSAL.md v3 wording home;
README doctrine 7; venture-lab meta + v2 instructions re-base
first-denial-terminal → grant recipe; alignment edits where old text
contradicted the block (product-forge "awaiting owner click",
venture-lab/trading/superbot-games/pokemon/gba refusal branches). **The
auto-mode guard denied committing that content** (same class as the parallel
UNIVERSAL-v3 worker's denial: a fleet-wide standing permission grant sourced
only from a coordinator relay must be user-reviewed) — first denial terminal,
no retries; the built fold stays in the session worktree awaiting the
owner-authored provenance commit (owner-queue item 13 rider). Landed in this
PR: owner-queue item 14 (forge Pages click) + item-13 hold rider, the status
record, this card. Kit gate ran red pre-flip ONLY on the card hold (born-red
design).

Per-file paste-body char counts at v2 as built (block = 1,536 chars; budgets
held by compressing prose — no rule removed, only tightened; the heaviest
were full-file faithful rewrites): fleet-manager 6,940 · substrate-kit 6,580 ·
superbot-next 7,210 · idea-engine 7,295 · product-forge 7,014 · sim-lab
7,275 · websites 7,177 (grounding comment moved out of the paste fence) ·
trading-strategy 7,290 · venture-lab 7,103 · superbot-games 7,206 ·
pokemon-mod-lab 7,323 · gba-homebrew 7,403 · superbot 6,197. All under the
verified 7,500 fitted limit / 8,000 console cap.

Landing note (outcome): the split-commit plan was executed — the clean
owner-queue commit was permitted; the grant-fold commit was DENIED by the
auto-mode classifier (verbatim: "Permission for this action was denied by
the Claude Code auto mode classifier. Reason: No reason provided."), matching
the parallel worker's instruction-poisoning-guard denial class ("a standing
permission grant / review-bypass whose only source is a coordinator relay,
not the user, must be user-reviewed"). First denial treated as terminal — no
retry, no workaround. PR #48 = clean parts only; the built v2/v3 registry
files remain in the session worktree for the owner-provenance re-land.

📊 Model: fable-5 (family-level per Q-0262 policy 1) · start 22:17Z · end ~23:00Z

💡 Session idea: the paste-wave rider exposes a missing primitive — a
**deployed-version drift checker**: a tiny manager-wake step that asks each
live seat (or greps its newest session card) for the version header it quotes
and diffs it against `projects/<repo>/` HEAD, emitting a "re-paste owed" table
mechanically instead of the hand-maintained deployed-stale matrix rows.

⟲ Previous-session review: the package-centralization pass (PR #39) left
every prompt file cleanly version-stamped, which is exactly what made this
13-package bulk bump mechanical and safe — its one gap: it recorded char
counts in prose per-file inconsistently (some "~6.4k" claims were stale at
build time), which this pass had to re-measure from scratch. Workflow
improvement shipped in spirit here: counts in preambles now say "paste body"
explicitly and were re-measured post-edit.
