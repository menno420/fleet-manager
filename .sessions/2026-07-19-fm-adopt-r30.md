# 2026-07-19 · fm R30 adoption — workflow-PR merges are agent work (records slice)

> **Status:** `complete`

About to happen (as declared born-red): docs-only records slice adopting
playbook **R30** (landed on main today by a sibling session, PR #367 —
`docs/workflow-pr-merge-policy.md`, owner-live provenance quoted therein) into
the seat's own records: (a) `docs/owner-queue.md` — retire/annotate the
"carve-out confirmation awaited from owner" framing (ANSWERED by R30/#367),
keep the two live hub items, add the websites label-machinery cleanup note to
`OQ-LABEL-DEFS-DELETE`; (b) `docs/fleet-triage.md` — dated R30-landing entry
incl. parallel-writer coordination note + the two dated classifier denials on
manager-relayed guardrail-removal dispatches; (c) `control/status.md` — bump,
R30-adopted section, refreshed baton (next snapshot ~18:00Z).

- **📊 Model:** fable-5 · high · docs-only — records slice (R30 adoption)

## What shipped (PR #368)

- `docs/owner-queue.md` — the "carve-out confirmation awaited from owner"
  thread is **ANSWERED by R30/#367**, recorded with citations in three spots:
  section (A) standing note (workflow-diff carve-outs no longer route to the
  owner queue at all — only a policy STOP does); `OQ-LABEL-DEFS-DELETE`
  R30-UPDATE (merge-policy half answered; the websites label-machinery
  cleanup — `host-automerge-extras.yml` auto-re-create/auto-apply — remains
  pending and **rides the owner's live venue**: a manager-relayed removal
  dispatch was classifier-gated twice on 2026-07-19 by the auto-mode
  guardrail-removal provenance check — dated, factual, transient venue state
  per doctrine); `OQ-FM-APPARATUS-SIZING` RISK note (once decided, the
  workflow-diff PR is agent work end-to-end under R30). The two live hub
  items stand unchanged: `OQ-SBW-DUP-FAILSAFE` · `OQ-LABEL-DEFS-DELETE`.
- `docs/fleet-triage.md` — dated entry "R30 landed — workflow-PR merges are
  agent work (~15:3xZ)": R30 landed via sibling session (PR #367, merged
  14:41:37Z, commit `234303e`; first attempt #362 closed after Codex found
  ~7 bypasses in the label-gate design); parallel-writer coordination note
  (sibling active 12:01–14:41Z, zero collisions with this seat's #364/#365 —
  per-file claims + separate branches held); the two classifier denials
  recorded dated + factual.
- `control/status.md` — `updated:` bumped to 15:30Z; "R30 ADOPTED" section
  (operating change: future workflow-diff PRs in tended repos agent-merge
  after the policy's 3-point head-SHA check); Baton (15:3xZ): (1) hub/owner —
  SBW dup delete · label defs · websites label-machinery cleanup (owner
  venue); (2) next slice — `check_label_hygiene.py` or I8-reads-lane-fence;
  (3) next I6 snapshot ~18:00Z.

## Enders

- 💡 **Session idea (dedup-checked — `docs/ideas/`, `docs/planning/2026-07-19-
  next-slices.md` both sections, playbook R30 text; nothing mechanizes the
  merge-side check):** **`scripts/r30_merge_check.py` — mechanize the R30
  3-point verification.** R30 puts a safety-critical, multi-step checklist on
  the merge-side agent (Codex review state at the exact head SHA incl. P1/P2
  scan of inline + summary; check runs AND legacy statuses; whole-file
  secret+egress scan with interpreter/`/dev/tcp` patterns; STOP on patch-less
  diffs). That is exactly the improvised-procedure shape the friction→guard
  doctrine says to convert into a checker: one stdlib script,
  `r30_merge_check.py <owner>/<repo> <pr>`, direct-egress PAT path, printing
  PASS/STOP per point with the evidence lines — the agent still decides, the
  script just makes the checklist undroppable and its evidence quotable in
  the merge record. Advisory tier, Q-0105 unverified header, selfcheck with a
  synthetic secret+egress workflow fixture.
- ⟲ **Previous-session review (PR #365, volatile-field drift check in
  `verify_routine_state.py`):** strong slice — it fixed the exact rot
  observed that same day (fence two firings stale while the read side said
  OK), pinned the founding shape in `--selfcheck`, and kept the exit contract
  frozen (V1 is INFO, never DRIFT) so advisory stayed advisory. Improvement
  it surfaces: its ground-truth run quoted the synthetic 10Z-era fixture
  inline in the session card but didn't commit it as a reusable fixture file
  — pinning real-incident fixtures under `tests/` (or a `fixtures/` dir)
  would let later refactors replay today's exact rot instead of re-deriving
  it from prose.
- **Doc-audit:** everything durable is homed — the R30 resolution in
  `docs/owner-queue.md` (three annotated spots, citations to #367 + the
  policy doc), the dated evidence in `docs/fleet-triage.md`, the operating
  change + baton in `control/status.md`; no drift found elsewhere (playbook
  R30 + policy doc landed by the sibling need no edits from this slice; RAW
  final report carries PR/merge facts). Claim
  `control/claims/claude-fm-adopt-r30.md` deleted in this flip commit.
- **Guard-fires:** 4 record(s) appended to `.substrate/guard-fires.jsonl` by
  `bootstrap check --strict` (allowlisted, reason-carrying) — delta committed
  per the ledger rule.
