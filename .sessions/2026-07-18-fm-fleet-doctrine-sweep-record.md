# Session — fm-fleet-doctrine-sweep-record

> **Status:** `complete`

**Branch:** `claude/fm-fleet-doctrine-sweep-record-0718`

📊 Model: Opus 4.8 · medium · docs-only

**About to do:** Append a dated oversight-sweep record to `docs/fleet-triage.md` capturing the fleet-wide stale-merge-doctrine sweep (read-only, verified 2026-07-18 vs each repo's origin/main): 10 of 14 siblings already reconciled the pre-#308 "agents can't merge" wording, 3 remain STALE (gba-homebrew, pokemon-mod-lab, superbot-idle) and 1 kit residual (substrate-kit) — all routed to the hub as cross-repo living-doc edits. This card holds the PR born-red until the entry lands and the checks pass, then flips `complete` LAST.

**Did:**
- **Appended `## 2026-07-18 · fleet-wide stale merge-doctrine sweep`** to `docs/fleet-triage.md` (newest-at-bottom, before `## How to re-verdict`), in the file's dated sweep-note format (italic source line + purpose + result + per-repo findings + disposition). The record captures which sibling repos' living/binding docs still carry the pre-#308/#309 "agents can't merge" doctrine after the owner reversed it.
- **Framed strictly as drift-records, not standing walls.** Every finding reads "repo X's status doc still carries the pre-#308 doctrine" (a dated observation of stale wording), never a present-tense standing capability-denial claim — and the dated `## 2026-07-18` heading is itself the DATED exemption `check_no_false_walls.py` recognizes. `docs/fleet-triage.md` is not in that checker's narrow living/binding scan set, and the guard stayed CLEAN across 5 scanned docs.
- **Findings recorded:** 10 of 14 already reconciled (superbot, superbot-next, superbot-games, superbot-mineverse, websites, venture-lab, idea-engine, sim-lab, trading-strategy, curious-research); 3 STALE routed to hub — gba-homebrew `control/status.md` (~5 lines, contradicts its own already-corrected `CAPABILITIES.md:89`), pokemon-mod-lab `docs/CAPABILITIES.md:97-100` seed + `current-state.md` + `control/status.md` (workflow-diff carve-out partly real but agent-mergeable via MCP/REST, not an owner blocker), superbot-idle `control/status.md:84` (borderline past-tense, one-line pointer suffices); plus a HISTORICAL residual in substrate-kit's `CAPABILITIES.md` append-log (:105 phrases a now-false standing rule, recommend a dated correction on top). Disposition: cross-repo living-doc edits routed to the hub chat; fleet-manager's own docs already reconciled (#313/#316).
- **Checks green:** `check_no_false_walls.py` CLEAN EXIT=0 (5 living/binding docs, none added); `check_owner_queue.py` CLEAN EXIT=0; `check_docs_links.py` CLEAN (256 files, every intra-repo link resolves); `bootstrap.py check --strict` EXIT=0 after this flip (the only red was this card's born-red HOLD — guard-fire delta staged, not reverted). The pre-existing `seat-digest-stale` advisory is explicitly "never exit-affecting" and out of this docs-only session's scope.

⚑ Self-initiated: None — directed deliverable (coordinator-dispatched oversight-sweep record). Oversight-only: recorded in this repo, nothing pushed to sibling repos. No autonomous idea promotion; no trigger created, modified, fired, or deleted.

💡 Session idea: **A `check_reconcile_routing.py` sibling checker that ages routed-to-hub cross-repo reconciles.** This sweep routes 3 stale doc-reconciles + 1 kit pointer to the hub chat as living-doc edits — but nothing tracks whether those reconciles actually land in the named sibling repos. A stdlib checker could parse dated sweep-notes for "routed to hub" / "cross-repo … edits" dispositions naming a sibling repo + a file:line, then (given standing read-auth to every repo) fetch that repo's origin/main and re-check whether the cited stale wording still exists — flagging a routed reconcile that has aged past a window without the target doc changing. It closes the loop this sweep opens: the sweep *finds* drift and hands it off, but a hand-off with no landing-verification is exactly how stale doctrine survives a reversal. Dedup-checked `docs/ideas/` + the S3 register-staleness flagger + the prior session's register↔owner-queue linkage idea — this ages *cross-repo hand-off completion*, a distinct axis from register-verdict staleness (S3) and owner-queue slug integrity; novel.

⟲ Previous-session review: **PR #316 (fm-no-false-walls-guard — building `tools/check_no_false_walls.py` and fixing the CONSTITUTION false-wall line)** was exactly the right high-leverage move for this moment in the fleet's doctrine reversal: it turned #309's *stated* ban on present-tense standing capability-denial claims into an *enforced* guard (the "enforce, don't exhort" instinct), carried a full provenance + reliability + kill-switch header, failed SAFE (advisory, never wired into a blocking gate), and split the labor cleanly with S9's dated-ledger ager — the prose-guard vs the ledger-ager. It also made THIS session's job trivially safe: I could frame drift-records confidently knowing a checker backs the fleet-standard. One thing it could have done better: the guard's scan set is a narrow allowlist (CONSTITUTION, current-state, owner-queue, CLAUDE.md, CAPABILITIES active region) — which is correct for *living/binding* docs, but this very sweep shows the pre-#308 wording actually survives in **sibling repos' `control/status.md`** files, a surface no fleet-manager-local checker can see. Workflow improvement it surfaces: the false-wall guard is per-repo, but the doctrine is fleet-wide — the natural next step (the `💡` idea above generalizes it) is a hub-run cross-repo sweep that fetches each sibling's living docs over the standing read-auth and greps for the banned shape, so the reversal is *verified landed everywhere*, not just *guarded locally*. #316 built the local guard correctly; the fleet just needs its hub-level twin.
