# 2026-08-10 · hub — salvage fm #838's residue, close it as superseded

> **Status:** `complete`

- **📊 Model:** opus-5 family · high · docs-only
- Time: 2026-08-10 · venue: owner-live hub chat · branch
  `claude/fleet-manager-full-audit-lty31q` (restarted from merged `main`)

💡 Session idea: two sessions fixing the same defect in parallel is not waste —
the waste is the second one merging first and leaving the first as an
un-mergeable stub nobody reads again.

Layer-2 handoff: null (fleet-manager itself)

## What is about to happen

The owner asked why a PR was still open. **fm #838 is not this session's** — a
prior session opened it 2026-08-10T17:59Z, an hour before this session began,
against the same root cause (D2's stale `shiftlife` pointer; the owner's
2026-08-08 roadmap unreachable from the front door). This session's #839/#840/#841
landed on the same files and left #838 `dirty`.

Measured overlap against merged `main`: D2 repointed ✅ (OD-15), roadmap
reachable from `README.md` ✅ — but **the boot file carried 0 mentions of the
roadmap**, so the surface a Claude Code session actually auto-loads still could
not reach the owner's current plan. That residue is salvaged here, together with
#838's `derive-don't-state-counts` fix in the ChatGPT instructions. #838 is then
closed as superseded, with this PR named.

## Previous-session review

⟲ fm #840 built the browsable layer (map, six-read order, area READMEs) and
closed 17 Codex findings. It did not check for *other* open PRs touching the same
files — which is how a sibling session's work was left stranded rather than
merged or closed.

## Close-out

### Shipped

- `.claude/CLAUDE.md` — read-path entry **1b** for the owner's 2026-08-08
  roadmap (the boot file carried **0** mentions before this; `README.md` already
  had it, so the gap was on the surface Claude Code actually auto-loads), and a
  **dated reconciliation** of the OD table against the NOW pointer: both OD-13
  and OD-15 are conditional, they expire independently, and on either expiry the
  instruction is to **re-read the pointer**, not to infer the lettered step.
- `README.md` — read 4 is the consolidation program **paired with** the roadmap,
  so the roadmap is inside the mandatory order rather than the conditional deep
  path.
- `docs/prompts/chatgpt-project-instructions.md` — the Work surface's own cold
  contract now defers to README's numbered order (it still named a three-file
  route, so a Work session would have skipped the roadmap regardless of the two
  fixes above), plus #838's `derive-don't-state-counts` fix: the frozen
  *"five files"* replaced by a description of **both** guards — the standalone
  tool's short allowlist and the engine guard inside `check --strict`
  (`iter_adopter_files`: live `docs/**/*.md` + `.claude/**/*.md`).

### Verify — real exit codes

```
python3 bootstrap.py check --strict   → 1  (sole finding: this card's designed born-red hold)
added-card lane, run direct           → session-card-hold only, no masked finding
link scan over the 3 changed docs     → 0 dead
```

### Review

Codex, two rounds, both on exact heads. Round 1 (`cdadc6e`): 3 findings, 3
conceded — the roadmap still sat in the *conditional* deep path so the six-read
contract could skip it; the OD/NOW precedence was written as permanent when both
directives are conditional; and `--list` was offered as CI's coverage when the
engine guard scans a wider set (verified in source). Round 2 (`cc7d151`): 3 new
findings, 3 conceded — including one this session **introduced** while fixing
round 1: calling the program "the older track" demoted a `living-ledger` holding
OD-13/14/15 dated 08-08/10. Corrected: the program is THE plan, the roadmap is
the methods subplan OD-13 prioritises ahead of the lettered steps. 6 findings, 6
conceded, 0 declined; landed at the two-round cap.

### Capability delta

Two measurements worth the ledger, both from answering owner questions rather
than probing: **`gh pr list` is 403-walled here** — *"This GraphQL query
(PullRequestList) is not enabled for this session — only the pinned set of
PR-review operations is served. Use REST via `gh api repos/{owner}/{repo}/...`"*
— while the same listing over direct-PAT REST returns **200**. And
`python3 bootstrap.py hook session-start` runs clean but emits **nothing** here,
so SessionStart's injection path is **unverified**; `UserPromptSubmit` injection
is `MEASURED` by direct observation (route_docs.py fired on this session's first
turn). Neither is a wall — both are route facts.

### ⚑ Owner-facing

- `OQ-FM-D2-TARGET` — unchanged; still his call.
- **New, and the reason this PR exists:** the estate mandates *raising* the
  open-PR claim signal (`session-close/SKILL.md:45`) and mandates nobody
  *reading* it. That asymmetry stranded fm #838 for an hour while this session
  fixed the same defect. The mechanism follows as its own PR, on
  `UserPromptSubmit` (observed working) using direct REST (`gh` is 403-walled),
  not on the assumed `SessionStart` path.

### Ideas

💡 A one-directional protocol is invisible to its own gate: every checker here
verifies that a session *emitted* its signals, none verifies that a session
*consumed* anyone else's. That is the shape behind this PR's existence.

### ⟲ Previous-session review

⟲ fm #840 built the browsable layer and closed 17 findings, but never checked
whether another session held an open PR on the same files — which is how #838's
work was left stranded rather than merged or closed.
