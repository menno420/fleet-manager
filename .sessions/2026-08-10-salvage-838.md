# 2026-08-10 · hub — salvage fm #838's residue, close it as superseded

> **Status:** `in-progress`

- **📊 Model:** opus-5 family · high · docs-only
- Time: 2026-08-10 · venue: owner-live hub chat · branch
  `claude/fleet-manager-full-audit-lty31q` (restarted from merged `main`)

💡 Session idea: two sessions fixing the same defect in parallel is not waste —
the waste is the second one merging first and leaving the first as an
un-mergeable stub nobody reads again.

Layer-2 handoff: null (fleet-manager itself)

## What is about to happen

The owner asked why a PR was still open. **fm #838 is not this session's** — a
prior session opened it 2026-08-10T17:59Z, an hour before this session began,
against the same root cause (D2's stale `shiftlife` pointer; the owner's
2026-08-08 roadmap unreachable from the front door). This session's #839/#840/#841
landed on the same files and left #838 `dirty`.

Measured overlap against merged `main`: D2 repointed ✅ (OD-15), roadmap
reachable from `README.md` ✅ — but **the boot file carried 0 mentions of the
roadmap**, so the surface a Claude Code session actually auto-loads still could
not reach the owner's current plan. That residue is salvaged here, together with
#838's `derive-don't-state-counts` fix in the ChatGPT instructions. #838 is then
closed as superseded, with this PR named.

## Previous-session review

⟲ fm #840 built the browsable layer (map, six-read order, area READMEs) and
closed 17 Codex findings. It did not check for *other* open PRs touching the same
files — which is how a sibling session's work was left stranded rather than
merged or closed.

## Close-out

_pending — this card is born red and flips last._
