# 2026-07-12 — kit upgrade v1.12.1 → v1.13.0 (distribution wave B)

> **Status:** `complete`

Upgraded the vendored substrate-kit from v1.12.1 to v1.13.0 (verified release asset, sha256 `982b2667…0959`, three-way match: release facts / release.json / `sha256sum`); distribution-only — no lane/domain files touched.

## What shipped

- `bootstrap.py` v1.12.1 → v1.13.0; old dist banked at `.substrate/backup/bootstrap-1.12.1.py` (sha256 `1055ca2cfd32a83e3dab7a978b05fbec2a82932a3375de0b1034f2519c16e4aa`).
- Planted `docs/SKILLS.md` (new in this release) and applied the minimal boot-wiring hunk to `docs/AGENT_ORIENTATION.md` — required by the strict reachability gate.
- Staged kit assets refreshed (`.substrate/claude/CLAUDE.md`, skills incl. new `release/` + `upgrade-distribution/`, agents, hooks, CI examples).
- Carve-outs respected (see `.substrate/upgrade-report.md`): 5 consumer-edited docs untouched; `docs/AGENT_ORIENTATION.md` diverged — its `.claude/CLAUDE.md` → `CONSTITUTION.md` pointer hunks (ORDER 015 payload) left **lane-owed**; `CONSTITUTION.md` template-improved (apply via `upgrade --apply-docs`, lane-owed).
- No hardcoded `1.12.1` pins in tests/config (grep verified; matches were historical prose in research/roster docs only).
- Heartbeat `kit:` line in `control/status.md` is lane-owed — deliberately untouched.

- **📊 Model:** fable-5 · medium · distribution-wave

## 💡 Session idea

The strict reachability gate forces newly planted docs (like `docs/SKILLS.md`) to be linked from a read-path doc, but when the linking doc is *diverged*, the upgrade can't auto-wire it — every distribution session re-solves this by hand. The kit's upgrade could emit a ready-to-paste minimal wiring hunk (just the new-doc lines, no other template deltas) in `upgrade-report.md`, so distribution sessions apply reachability wiring mechanically without touching the diverged doc's lane-owed content.

## ⟲ Previous-session review

Previous session (#112, heartbeat v3.3 landing) landed cleanly and kept the one-writer-per-file discipline. One improvement it surfaces for the system: the heartbeat's `kit:` line has been re-flagged as stale across three consecutive waves (per `docs/owner-queue-candidates.md`) — the lane isn't picking it up from status prose; graduating it into an explicit `control/inbox` ORDER (as that doc already suggests) would close the loop rather than re-flagging a fourth time.
