# 2026-07-11 — kit: upgrade v1.12.0 → v1.12.1

> **Status:** `complete`

📊 Model: fable-5 · start 2026-07-11T~22:2xZ ·
distribution worker (owner directive Q-0261.3)

## Declared at open (born-red)

About to upgrade kit v1.12.0 → v1.12.1. Scope: substrate-kit upgrade only,
kit-owned files (bootstrap.py, .github/workflows/substrate-gate.yml,
.substrate/*, kit-planted docs) — no lane work, no control/inbox.md or
control/status.md edits (heartbeat `kit:` bump is lane-owed). Plan: stage the
sha256-verified release asset
(`1055ca2cfd32a83e3dab7a978b05fbec2a82932a3375de0b1034f2519c16e4aa`, 704108
bytes; release run 29170017074, tag v1.12.1) as `bootstrap.py.new` +
`release.json` adjacent → `python3 bootstrap.py.new upgrade` (self-verifies
sha256+version, self-cleans staged inputs) → expect exactly one new
`.substrate/backup/bootstrap-1.12.0.py` bank (sha256 `77c00b81…`, the
outgoing v1.12.0), all pre-existing banks byte-identical, carve-out scan
"ran, 0 found" → `check --strict` red only on this card's designed HOLD →
flip this card `complete` as the deliberate LAST step, deleting the claim in
the same commit. REST squash-merge on green (R21).

## Shipped (close-out)

- **Upgrade landed (commit ebde89c):** `bootstrap.py` v1.12.0 (sha256
  77c00b81…) → v1.12.1 (sha256 1055ca2c…, byte-identical to the verified
  release asset, 704108 bytes); `kit_version: 1.12.1` recorded in
  `bootstrap.py`, `substrate.config.json`, `.substrate/state.json`. Engine
  self-verified sha256+version against the adjacent `release.json` and
  self-cleaned both staged inputs.
- **Carve-out scan clean:** verbatim `upgrade: carve-out scan:
  .github/workflows/substrate-gate.yml — ran, 0 found (live matched the
  previous kit template byte-for-byte; the delta is kit-side template
  evolution)`. No pre-regen bank created (only the historical
  `substrate-gate.pre-regen-4f50eb4d.yml` remains); live gate regenerated
  from template@new (+23/−4 lines).
- **Backups verified:** exactly ONE new archive
  `.substrate/backup/bootstrap-1.12.0.py` (sha256 77c00b81…, byte-identical
  to the outgoing v1.12.0 and to the v1.12.0 release asset); all EIGHT
  pre-existing `bootstrap-*.py` banks byte-identical before/after (full-dir
  hash compare). Engine-owned `last-upgrade.json` + `state.json` in the
  backup dir updated as designed.
- **Planted docs:** upgrade-report classifications — 15 unchanged · 6
  consumer-edited (kept) · 0 template-improved · 0 diverged. Nothing
  host-owned overwritten; no manual-merge debt added this run.
- `check --strict` = designed born-red HOLD on this card only; sole other
  output is the pre-existing never-exit-affecting `owner-action-fields`
  advisory on `control/status.md`. guard-fires.jsonl moved (tracked —
  committed in this flip).

## Walls hit

None. Both release assets downloaded clean on the first curl (byte size +
sha256 matched the coordinator's verified facts; no proxy stub bodies).

💡 Session idea: every kit-upgrade session hand-verifies "all pre-existing
banks byte-identical" by manually hashing `.substrate/backup/` before and
after the upgrade — an out-of-band scratchpad step the engine could own.
Teach `upgrade` to emit a `banks:` block in `.substrate/upgrade-report.md`
listing each pre-existing archive with its sha256 and a `preserved:
byte-identical` verdict (it already reads the dir to write the new bank), so
the report itself is the audit artifact and distribution workers stop
re-deriving it with shell hashing every wave.

⟲ Previous-session review: the v1.12.0 kit-upgrade card (#79) was exemplary
— it live-verified the three-way carve-out fix it had itself proposed
upstream, closing the loop. One improvement it surfaces: its lane-owed list
(heartbeat `kit:` bump, AGENT_ORIENTATION diverged-doc merge, no live
CLAUDE.md, owner-action-fields advisory) is re-flagged verbatim by every
kit wave including this one — the AGENT_ORIENTATION item now reads
`consumer-edited` in this run's report (template unchanged), so that one
resolved by template catch-up, but the other three persist untouched.
Chronic lane-owed items re-flagged across 3+ waves should graduate from
session-card prose into a control/inbox ORDER so the lane actually
schedules them instead of every distribution worker re-noting them.

Lane-owed (not done here, kit scope only, Q-0261.3 one-writer rule):
heartbeat `kit:` line in `control/status.md` still self-reports v1.7.0
(needs bump to v1.12.1); fleet-manager still has NO live root CLAUDE.md
(planted boot set staged only at `.substrate/claude/CLAUDE.md`); the
pre-existing `owner-action-fields` advisory on `control/status.md` remains
open. Non-kit observation, not touched: branch
`origin/claude/order-017-instruction-reissue` in flight — lane work, not
this session's.
