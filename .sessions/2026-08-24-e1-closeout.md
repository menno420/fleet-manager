# 2026-08-24 — E1 close-out: record the owner's send-day plan

> **Status:** `complete` — branch `claude/final-eap-mail-x2s9kx`, **restarted
> from `origin/main` at `6c48f9f`** because its prior PRs (#943, #944) both
> squash-merged; stacking on merged history is what the restart rule forbids.
> Landed as fm **#945**. Born red on purpose and **verified red at open** —
> `substrate-gate` returned `failure` on the first head. One review round
> requested and answered at `29b4054`: **2 findings, both conceded, one P1.**
> Flipped after `python3 bootstrap.py check --strict` returned a real exit 0 on
> this tree, read directly and never after a pipe.

- **📊 Model:** opus-5 · high · docs-only

## 💡 Session idea

The owner closed the session with a plan that exists nowhere in the repo:
*"tomorrow morning I will start fresh and create and send the email after a
revision pass and my own section added/edited."*

The queue said he writes Part 1 and sends. His sentence says **more is revised or
edited before it goes** — which changes what tomorrow's session should expect:
not "hand him the file", but "be ready to edit the assembled draft on his
direction". A fact that lives only in a closing message is not in the repo, the
lesson this session already recorded twice — once about a mailbox, once about a
handoff prompt.

**And the first draft of this card over-read him** (`@codex`, fm #945, **P1** —
the session's only one). It decomposed the sentence into *"a revision pass over
Part 2"* followed by *"Part 1 written and Part 2 edited"*. **His sentence names
no target and does not say whether those are two operations or one.** So the
record carried an inference as his ordered intent, on the very entry a session
executes against tomorrow — the exact failure the mail this session assembled
reports as FALSE-DONE's sibling. Now quoted verbatim, with what it settles and
what it does not kept apart, and *"ask him"* named as the resolution rather than
a guess.

**Both findings were ones this session asked the reviewer to attack by name, and
both came back against it.** That is the argument for writing the request as a
challenge rather than a summary: a reviewer told where the author is least
confident spends its attention there.

## previous-session review

fm #944 landed the scoped append/retract claim and hardened
`tools/check_claim_propagation.py`. Its own review round is fully addressed; the
sweep and selftest are green on `main`.

## What landed

- `docs/owner-queue.md` — `OQ-E1-FINAL-EAP-EMAIL` carries his send-day sequence.
