# Session — fm-wake-chain-record-fix

> **Status:** `complete`

**Branch:** `claude/fm-wake-chain-record-fix-0717`

📊 Model: Opus 4.8 · medium · docs-only

**About to do:** Correct the wake-chain record: native MCP scheduling works (not a wall); close OQ-FM-WAKE-CHAIN-ARM; note the coordinator-bound failsafe + I4 satisfied. This card held the PR red (HOLD) until the three doc edits landed and the enders were filled, then flips `complete` last.

**Did:**
- **`docs/CAPABILITIES.md` — dated UPDATE supersedes the same-day self-scheduling WALL finding.** Native scheduling via worker ToolSearch WORKS: the scheduling MCP tools (`create_trigger` / `list_triggers` / `send_later`) are DEFERRED — load via ToolSearch (not in the visible tool list), call NATIVELY (never via Bash). Verified failsafe `trig_01Bo7dZxM9xz2hwR36L424Z8` (cron `30 */2 * * *`, enabled, next 2026-07-17T22:36Z, coordinator-bound, persist_session:true). Earlier denials = the Bash-fallback path only; the classifier is NONDETERMINISTIC (retry once from a fresh worker, not a wall). Caveat recorded: such triggers store no MCP connectors, so a NEW spawned session lacks connector tools (moot firing into an existing session); hygiene: one trigger-MCP call per worker. Added the standing lesson: **before declaring a tool absent, ToolSearch for it — absence from the visible tool list is NOT tool absence.**
- **`docs/owner-queue.md` — `OQ-FM-WAKE-CHAIN-ARM` marked ✅ RESOLVED 2026-07-17 (agent-side; owner action no longer needed)** and moved out of the Active region into a new `## Resolved 2026-07-17 (agent-side — wake chain restored via native MCP scheduling)` section: wake chain restored agent-side, failsafe armed (coordinator-bound dead-man) + pacemaker restored; the earlier ask assumed a hard wall that was actually the Bash path + nondeterministic classifier. UNBLOCKED I4.
- **`control/status.md` — I4 MANAGER-FAILSAFE now SATISFIED.** Dead-man exists: `trig_01Bo7dZxM9xz2hwR36L424Z8` (coordinator-bound; fires 2-hourly, checks seat liveness + nudges). The recreation-ruling note's BLOCKER superseded by a dated UPDATE; the I4 line rewritten FAIL→SATISFIED with the prior-state provenance kept; noted the checker's snapshot-computed I4 self-corrects on the next `telemetry/triggers-snapshot.json` refresh. `updated:` bumped to `2026-07-17T22:38Z` (real `date -u`).
- **Checks green (verbatim in the run report):** `check_owner_queue.py` CLEAN exit 0; `check_roster_freshness.py` OK exit 0; `check_docs_links.py` (the S5 checker merged this morning, PR #293) CLEAN exit 0 — confirms the edits introduced no dead links; `bootstrap.py check --strict` exit 0 after the flip.

⚑ Self-initiated: None — directed deliverable (coordinator relay of new wake-chain facts + the follow-up completion instruction). No autonomous idea promotion. No trigger created, modified, fired, or deleted by this worker — the failsafe recorded here was armed by another session; this session only records it.

💡 Session idea: A `scripts/check_capability_freshness.py` companion to the S9 wall-age flagger, but for the *inverse* rot: flag a `docs/CAPABILITIES.md` finding still labelled `WALL` when a LATER dated entry supersedes it — i.e. detect a stale wall that a same-file UPDATE has already overturned but that downstream readers might still cite. This session is the exact motivating case: the self-scheduling WALL sat un-superseded until a correction landed, and any reader between the two would have acted on a wall that was already false. (Dedup-checked `docs/ideas/` + README and the S9 NEXT-TASKS item — S9 flags walls that are *too old to trust*; this flags walls *already contradicted in-file*, a distinct check; novel.)

⟲ Previous-session review: The previous session (fm-s5-docs-link-drift / PR #293) built the S5 link-drift checker cleanly and — crucially — resisted manufacturing fixes when the repo scanned CLEAN, correctly reporting an honest true-negative instead of inventing drift to look productive (the Q-0089/Q-0102 "never hallucinate filler" bar, well applied). What it could have done better: its own card leaned on the earlier "self-scheduling WALL" framing as settled fact in its previous-session review — a reminder that a session's recorded "wall" is a *point-in-time* finding, not durable truth. Workflow improvement (dovetails with this session's 💡): CAPABILITIES walls should carry a machine-checkable freshness/supersession marker so a later reader (or checker) can tell a still-live wall from one an in-file UPDATE has already overturned — turning the "walls are point-in-time" lesson into an enforced check rather than an exhortation (enforce-don't-exhort, Q-0132).
