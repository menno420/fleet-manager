# 2026-07-12 — prompts v3.3: seat-first keyword-dictionary CIs + expanded startups + registry sync

> **Status:** `in-progress`

📊 Model: fable-5 · start 2026-07-12 ·
integration worker (owner-directed overnight prompt rebuild, dispatched by coordinator)

## Declared at open (born-red)

v3.3 prompt rebuild — CI keyword-dictionary + expanded startups + registry
sync. Owner spec (live, 2026-07-12): per-seat Custom Instructions become a
compression-dictionary/routing layer (≤8,000 chars each, seat-first, the
owner's five-section skeleton) and the startup prompts lose their char cap
and EXPAND to carry the full doctrine verbatim. The assembled artifact IS the
seat file — the core+seat-block CI assembly step is retired; one file per
seat. This session installs the 8 drafted CIs + 8 drafted expanded startups
into `docs/prompts/v3/`, retires the assembly step in
`tools/regen_b_files.py` (keeping `--check-registry`/`--write-registry` and
adding drift checks so CI entries and startup doctrine can't silently drift),
reconciles the UNIVERSAL.md grant sha, re-syncs the `projects/<seat>/`
registry copies with PR #110-style provenance stamps, and lands the
planned-routes follow-up table.

## Close-out (written by the integration worker; badge stays in-progress —
## the verification pass flips it after review, per the dispatch order)

Shipped on fm PR #111 (head `3eafb4b`): 8 v3.3 CIs (one authored file per
seat, all ≤8,000 chars AND ≤8,000 UTF-8 bytes, checker-gated) + 8 v3.3
EXPANDED startups (doctrine verbatim; Q-0270 BOOT TRIAD as BOOT step 0) +
session-ender v3.3 (D-10 single-source, inlined copies drift-checked) +
regen_b_files.py rebuilt (assembly retired; drift checks + registry sync) +
per-project/README.md v3.3 (defects #7/#8/#9 CLOSED) +
docs/prompts/v3/planned-routes.md (new `plan` ledger) + all 24
projects/<seat>/ registry copies regenerated `@ 347abdb` (--check-registry
OK 24/24). UNIVERSAL grant sha reconciled to `16161af`. Full report,
QA-coverage map, and judgment calls: the PR #111 body.

💡 Session idea: the drift checker now proves the INLINED doctrine copies
match their canonical sources, but nothing yet proves the CI dictionary's
one-line COMPRESSIONS still match the startup text they summarize — a
"compression audit" mode (per dictionary entry, grep its keyword in the
seat startup and flag entries whose source sentence changed since the CI's
`startup@<sha>` stamp) would catch stale summaries the byte checks can't.
Dedup-checked against docs/ideas-shaped homes here (none) and
planned-routes.md (routes, not tooling) — genuinely new.

⟲ Previous-session review: the registry-sync session (PR #110,
`.sessions/2026-07-12-registry-sync-v32.md`) did clean regenerate-don't-fork
work and its `--check-registry` guard made THIS session's registry step a
two-command operation — the improvement it earned: its `instructions.md`
recipe hardcoded the core-assembly layout in prose that v3.3 had to hunt
down and retire across three files (script docstring, README, registry
headers); v3.3 therefore keeps the layer contract in ONE README section and
makes the script cite it, so the next generation swap edits one place.

📊 Model: fable-5 · integration worker session, dispatched (worker skips
heartbeat/status writes per CONTROL BUS; the flip pass owns the final
commit). Claim file `control/claims/claude-prompts-v3-3.md` — release at
flip (ender step 2).
