# 2026-09-04 · Couch Legend after phase A — B has not arrived; the rail breach settled, the next scenes written

> **Status:** `in-progress`

- **📊 Model:** fable-5 · xhigh · feature build
- **📍 Venue:** cloud-container
- **🔗 Session:** [session_01EnfPJ65QJYW6FWpKDq3bvk](https://claude.ai/code/session_01EnfPJ65QJYW6FWpKDq3bvk) · "Couch Legend phase B/C/D"

💡 Session idea: **an owner-gated phase is not a stop sign for the lanes
beside it.** The handoff said B gates C and D; it also said the art lane and
the one open rail breach were ungated. The useful move on a gate that has not
opened is to make the gate cheaper to open — one line asking for the verdict,
with the exact tab and minute it takes — and then work every lane that does
not need it.

## previous-session review

`2026-09-04-couch-legend-keepsakes-*` in couch-legend (#19) landed phase A and
left two things: a sub-2 % felt-upgrade reading it stated rather than tuned
away, and the owner's feel pass. This session re-ran its census instrument at
`4934955` before trusting the handoff's numbers (14/18 · 3/18 · 17/18, exit 0),
found 0 unconsumed owner comments, and took the ungated lanes.

## What this hub-side card covers

Records only — the product work is in couch-legend (#20, its own card
`.sessions/2026-09-04-the-glass-and-the-garden.md`). Here: the Layer-2 thread,
three owner-queue entries (phase B · the scene prompts · the empty prepay), one
capability entry (the paid key's balance is a state with an exact error, not a
wall), and this card.

## What shipped (hub side)

- `docs/repos/couch-legend/README.md` — the session's thread at the top of
  § Threads, and a one-line correction on the #19 thread's "one sub-2 %
  reading survives" (it no longer does).
- `docs/owner-queue.md` — `OQ-CL-COUCH-FEEL` · `OQ-CL-SCENES-4-6` ·
  `OQ-GEMINI-PREPAY`; `owner/README.md` regenerated by `tools/gen_owner_index.py`
  (the preflight's owner-index drift check caught the stale index before the
  push — the added-card gate, not a plain `--strict`, is what ran it).
- `docs/CAPABILITIES.md` — 2026-09-04 entry: the paid key's prepay balance
  is empty, with the exact 429; the free tier's image models at `limit: 0`;
  the route around it. A state, not a wall.

## Product side, for the reader who only sees this card

[couch-legend #20](https://github.com/menno420/couch-legend/pull/20): the
cross-wire ceiling (the one rail breach #19 stated, closed by tuning; 27 runs
regenerated, 0 of 27 below the floor), two wrong published numbers corrected,
and the arc-2 scene prompts for chapters 4–6. Its own card carries the
evidence and the verify tails.

## Verify

- `python3 scripts/preflight.py` → exit 0 after the owner-index regeneration.
- `python3 bootstrap.py check --strict --added-card .sessions/2026-09-04-couch-legend-glass-and-garden.md`
  → exit 1 on the born-red hold only (the preflight finding cleared).

## ⚑ Owner asks

All three are in `docs/owner-queue.md` with WHERE/HOW; the one that unblocks
the most is `OQ-CL-COUCH-FEEL` — one line on whether arranging the couch is
interesting.

## ⟲ Lesson

A balance at a horizon is not a measurement of an economy: it resets at
every prestige and reads whatever was unspent when the clock stopped. Two
documents quoted it from the same fixtures and disagreed by an order of
magnitude, and a change that did not touch the late game moved it 12×.
Peak-over-a-window survives the timing; the sim record § 3 now says why.
