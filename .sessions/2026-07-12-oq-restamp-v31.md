# 2026-07-12 — owner-queue re-stamp: C#34–C#36 onto prompts v3.1

> **Status:** `complete`

📊 Model: fable-5 · start 2026-07-12 ·
fleet-manager lane worker (mechanical truth-keeping)

## Declared at open (born-red)

Append dated re-stamp notes under owner-queue items C#34/C#35/C#36
(docs/owner-queue.md) pointing the restructure pastes at the prompts-v3.1
artifacts (PR #103): assembled custom-instructions per seat +
per-seat startup / universal-startup / session-ender. Original item text
stays verbatim; docs/owner-queue.md + this card are the only files touched.

## Close-out

Shipped on PR #106 — three append-only notes in `docs/owner-queue.md`,
original C#34–C#36 text untouched, each note attributed "re-stamped
2026-07-12 after prompts v3.1 (#103)":

- **C#35**: paste source superseded → the v3.1 assembled CI per seat
  (filled core `docs/prompts/v3/custom-instructions-core.md` + the seat
  block `docs/prompts/v3/per-project/<seat>-custom-instructions.md`).
  Counts re-verified on disk this session: all 8 seat-block paste bodies
  re-measured and match their in-file claims exactly (951–992 chars);
  assembled-filled arithmetic verified per header (7,943–7,998, all under
  the 8,000 hard cap); file sizes cross-checked with `wc -c`
  (2,093–2,165 bytes). Drift-check first line updated to the v3.1 stamp.
- **C#34 + C#36**: per-seat startup =
  `docs/prompts/v3/per-project/<seat>-startup.md` (generated from
  `docs/prompts/v3/universal-startup.md`); shared ender =
  `docs/prompts/v3/session-ender.md`.

`.substrate/guard-fires.jsonl` carries the check-run append (tracked kit
state, committed per repo convention). `python3 bootstrap.py check --strict`
green apart from the designed born-red hold this flip releases.

## 💡 Session idea

The owner-queue's char counts for paste artifacts go stale the moment a
prompt regen lands (C#35 drifted within a day of #103). Instead of baking
numbers into queue items, bake a pointer + a verify command (e.g. "counts:
see `docs/prompts/v3/per-project/README.md` budget table; re-measure with
the block-body rule in each file header") — the queue then can't hold a
stale count, and re-stamp slices like this one shrink to path fixes only.

## ⟲ Previous-session review

The staleness sweep (2026-07-12-staleness-sweep-8seat.md, PR #105) set the
right precedent this slice reused: measure everything yourself, cite
file@SHA, park READY+green when merge authority is denied. Miss: its
sweep covered seat heartbeats but not the owner-queue's artifact pointers —
C#34–C#36 were already drifted against the just-merged #103 and went
unflagged. Improvement: add "owner-queue asks still point at live artifact
paths/counts" as a standing check in the sweep's needs-attention pass; it
is a cheap grep against the queue's backtick paths.
