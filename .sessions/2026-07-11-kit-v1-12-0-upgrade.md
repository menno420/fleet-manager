# 2026-07-11 — kit: upgrade v1.11.0 → v1.12.0

> **Status:** `complete`

📊 Model: Claude (Fable family) · start 2026-07-11T~14:2xZ ·
distribution worker (owner directive Q-0261.3)

## Declared at open (born-red)

About to upgrade kit v1.11.0 → v1.12.0. Scope: substrate-kit upgrade only,
kit-owned files (bootstrap.py, .github/workflows/substrate-gate.yml,
.substrate/*, kit-planted docs) — no lane work, no control/inbox.md or
control/status.md edits (heartbeat `kit:` bump is lane-owed). Plan: stage the
sha256-verified release asset
(`77c00b811429e1b526ccc7e0dcf597435c11048e16a67edba6050f516ad5e1f8`, 689586
bytes; three-way verified: tag v1.12.0 → b310abaf, bump PR #225, release run
29160292286 success) as `bootstrap.py.new` + `release.json` (sha256
`617dca357c06403cbdcf274b3f1a88a48b1d4a0f282aa190b9d0a07a913fe600`, 449
bytes) → `python3 bootstrap.py.new upgrade` → verify the v1.12.0 MINOR
payload landings (substantive auto-draft: reflog commit subjects +
carried-forward pointer, skeleton 8→6 fill slots; HANDOFF/SessionStart
auto-derived evidence trail; planted-orientation boot-set trim — CLAUDE.md.tmpl
three-surface boot set, AGENT_ORIENTATION dedup, CONSTITUTION 591→488 words,
planted-doc regen diffs EXPECTED; unadopted auto-draft advisory in bare strict
lane only; carve-out scanner THREE-WAY compare fix #210 — FIRST live exercise,
expect ZERO phantom flags and NO pre-regen bank since the live gate already
carries checkout@v5/setup-python@v6) → exactly one new
`.substrate/backup/bootstrap-1.11.0.py` bank, all pre-existing banks
byte-identical → `check --strict` red only on this card's designed HOLD →
flip this card `complete` as the deliberate LAST step, deleting the claim in
the same commit. REST squash-merge on green (R21).

## Shipped (close-out)

- **Upgrade landed (commit 7811e14):** `bootstrap.py` v1.11.0 (sha256
  c339bd6a…) → v1.12.0 (sha256 77c00b81…, byte-identical to the verified
  release asset, 689586 bytes); `kit_version: 1.12.0` recorded in
  `bootstrap.py`, `substrate.config.json`, `.substrate/state.json`. Engine
  self-verified sha256+version against the adjacent `release.json` and
  self-cleaned both staged inputs.
- **Carve-out scanner three-way compare (#210) — FIRST live exercise,
  PASSED:** verbatim `upgrade: carve-out scan:
  .github/workflows/substrate-gate.yml — ran, 0 found` and `upgrade: kept:
  .github/workflows/substrate-gate.yml (kit-owned, already current)`. ZERO
  phantom carve-outs, NO pre-regen bank created (only the historical
  `substrate-gate.pre-regen-4f50eb4d.yml` remains) — exactly the fix the
  v1.11.0 card's session idea asked for; the false-positive class is dead.
- **Backups verified:** exactly ONE new archive
  `.substrate/backup/bootstrap-1.11.0.py` (sha256 c339bd6a…, byte-identical
  to the outgoing v1.11.0); all seven pre-existing `bootstrap-*.py` banks
  byte-identical before/after (full-dir hash compare). Engine-owned
  `last-upgrade.json` + `state.json` in the backup dir updated as designed.
- **Planted docs:** upgrade-report classifications — 15 unchanged · 4
  consumer-edited (kept) · 1 template-improved (`CONSTITUTION.md`,
  consumer-untouched → applied via `upgrade --apply-docs`, 591→~488-word
  condense, hash re-recorded) · 1 diverged (`docs/AGENT_ORIENTATION.md` —
  host AND template both moved; boot-set-trim delta printed in
  `.substrate/upgrade-report.md`, left for manual merge — see lane-owed).
  Nothing host-owned overwritten.
- **Staged CLAUDE.md updated** (`.substrate/claude/CLAUDE.md` — boot set
  trimmed to three surfaces, CAPABILITIES/AGENT_ORIENTATION routed not
  front-loaded). Fleet-manager remains **staged-only**: NO live root
  CLAUDE.md, so the trim reaches workers only if the lane adopts a live copy.
- `check --strict` = designed born-red HOLD on this card only; sole other
  output is the pre-existing never-exit-affecting `owner-action-fields`
  advisory on `control/status.md`. guard-fires.jsonl gained this session's
  born-red fire entries (tracked file — committed in this flip).

## Walls hit

None. Both release assets downloaded clean on the first curl (byte size +
sha256 matched the coordinator's verified facts; no proxy stub bodies).

💡 Session idea: the upgrade engine now three-way-compares the gate workflow
(live vs old-template vs new-template, #210) but a **diverged planted doc**
still dead-ends in "manual merge" prose that no session owns — this run's
`docs/AGENT_ORIENTATION.md` will be re-flagged by every future upgrade until
someone hand-merges it. The engine already banks the old template and prints
the template@old→template@new delta; teach it `upgrade --merge-doc <path>`:
a real three-way merge (base = old template, ours = live doc, theirs = new
template) that auto-applies when hunks don't collide and only then falls
back to manual. Same trick, second surface; it would have closed this run's
one open item mechanically.

⟲ Previous-session review: the v1.11.0 kit-upgrade card (#72) is the loop
working end-to-end — its 💡 idea (three-way carve-out compare) shipped
upstream as kit #210 and THIS run live-verified it: zero phantoms and no
pre-regen bank where #72 got a false-positive bank from the same detector.
Genuinely nothing to fault in its execution. Improvement it surfaces: three
consecutive kit-upgrade cards have now hand-carried the same release-facts
prose (tag→commit, run id, asset sha) — #72 itself said this should graduate
into a machine-checkable `verified:` block in the upgrade-report template;
v1.12.0's report still lacks it. Third occurrence = promote it upstream as a
kit issue rather than re-suggesting it in cards.

Lane-owed (not done here, kit scope only, Q-0261.3 one-writer rule):
heartbeat `kit:` line in `control/status.md` needs its bump to v1.12.0;
`docs/AGENT_ORIENTATION.md` diverged-doc manual merge (delta preserved in
`.substrate/upgrade-report.md`); fleet-manager still has NO live root
CLAUDE.md (planted boot-set trim staged only); the pre-existing
`owner-action-fields` advisory on `control/status.md` remains open. Next
session should know: the upgrade-report's "Template deltas for diverged
docs" section holds the exact AGENT_ORIENTATION patch to merge by hand.
