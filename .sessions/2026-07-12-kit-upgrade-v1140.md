# 2026-07-12 — kit upgrade v1.13.0 → v1.14.0

> **Status:** `complete`

📊 Model: fable-5

Distribution-only wave (owner directive Q-0261.3): upgraded substrate-kit
v1.13.0 → v1.14.0. No lane domain work; control/inbox.md and
control/status.md untouched (heartbeat staleness is lane-owed).

## What happened

- Staged v1.14.0 `bootstrap.py.new` + `release.json`; three-way sha256
  verified (asset == release.json field == substrate-kit committed dist ==
  `47c1b8b954be2f587d88f7ed5923870883deab88a8fa7fbf2bb755decc2ee581`).
- `python3 bootstrap.py.new upgrade`: old dist banked to
  `.substrate/backup/bootstrap-1.13.0.py` (sha `982b2667…0959`, matches
  pre-upgrade bootstrap.py); all 11 pre-existing banks byte-identical.
- `python3 bootstrap.py upgrade --apply-docs`: applied 5 template-improved
  docs (CONSTITUTION.md, docs/collaboration-model.md,
  docs/question-router.md, docs/CAPABILITIES.md, docs/SKILLS.md).
- capability-seed outcome: `docs/CAPABILITIES.md is consumer-untouched —
  the whole file (fence included) refreshes via upgrade --apply-docs; no
  fence-only refresh needed.` (refreshed via apply-docs = success path.)
- `check --strict`: only red is this card's designed born-red hold; no
  orphan finding on the diverged `docs/AGENT_ORIENTATION.md`, so it was
  left entirely alone (v1.13.0 #114 precedent not needed). The chronic
  owner-action-fields advisory did NOT appear this run.

## Lane-owed follow-ups (verbatim report lines)

- `| control/README.md | diverged | both the template and the doc moved — manual merge |`
- `| docs/AGENT_ORIENTATION.md | consumer-edited | template unchanged — consumer-owned, nothing to apply |`
- `| docs/current-state.md | consumer-edited | template unchanged — consumer-owned, nothing to apply |`
- `| docs/ideas/README.md | consumer-edited | template unchanged — consumer-owned, nothing to apply |`
- `| control/inbox.md | consumer-edited | template unchanged — consumer-owned, nothing to apply |`
- `| control/status.md | consumer-edited | template unchanged — consumer-owned, nothing to apply |`
- Q-0270 collapse note (leave to lane): "If this repo carries a local prose
  copy of the boot-triad/venue-posture rule (superbot Q-0270), that copy is
  now superseded by docs/CAPABILITIES.md's posture rule — collapse the
  local copy into a pointer."
- control/status.md heartbeat still reports an old kit version — chronic,
  lane-owed, deliberately not bumped by this distribution wave.

## 💡 Session idea

The upgrade-report's "Template deltas for diverged docs" section already
contains the exact diff a lane session needs to hand-merge
`control/README.md` — worth a kit follow-up where `check` (advisory)
reminds until a diverged doc's delta is resolved, so diverged docs don't
silently accumulate across releases the way this repo's control/README.md
now spans one.

## ⟲ Previous-session review

Previous kit-upgrade card (v1.13.0, PR #114) did well: it established the
hand-merge-only-on-orphan precedent for the diverged AGENT_ORIENTATION,
which this session applied as a decision rule (no orphan → no touch) with
zero re-derivation. Improvement it surfaces: the card's landing notes were
narrative; this session's card keeps lane-owed lines verbatim in a single
list so the next distribution wave can diff them mechanically.
