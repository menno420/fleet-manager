# 2026-07-13 — v3.5 stage-2: prompt-generation completion (v3.6 fold + ninth seat)

> **Status:** `complete`
> **Branch:** `claude/v35-stage2` · PR #153
> 📊 Model: fable-5

## What happened

Owner final night order, TASK 1 — completed the v3.5 prompt generation as
**v3.6** (stage-2 on top of PR #151; label choice argued in the v3.6
changelog: a body-changing re-sync bumps the generation so the DRIFT-CHECK
stamp stays meaningful). Sources verified at superbot HEAD `c65750e`.

- **Fold a — OPEN-PRs-STAY-OPEN standing:** AUTONOMY RIDER sentence reworked
  in the shared DOCTRINE of all 9 startups (land on green / never merge-chase
  / stack on open heads, base in PR body / owner sweeps via hub); CI BOOT
  TRIAD entry extended; v3.5 changelog item 5 marked SUPERSEDED.
- **Fold b — Q-0272 reading path:** new shared FLEET READ doctrine paragraph
  (standing read-only, pml DARK, fleet-reading-path.md route, fleet_status.py
  live-state command, unchanged boundaries); CI Routes-footer hook.
- **Fold c — Q-0273 venue model:** new shared VENUE MODEL paragraph (hub vs
  seats vs PM; `VENUE:hub` tag); CI six-field-ask hook.
- **Fold d — Q-0274 grounding boot-read:** shared doctrine sentence + a
  per-seat § pointer on every Orientation route (fm §2 … game-lab §9;
  curious-research routes to its founding pair).
- **Fold e — ninth seat:** `curious-research-{startup,custom-instructions}.md`
  authored from the founding pair, conformed to registry format (shared
  blocks spliced byte-exact from fleet-manager's; volatile night program NOT
  carried, D-9); failsafe `20 */2 * * *` added to the stagger table; SEATS
  registry + `seat_digest_sync.py` map extended; `projects/curious-research/`
  generated at v1 (+ authored meta.md).
- **Fold f — byte caps:** stage-2 added ~+310 bytes/CI; compensating wording
  compressions (shared identical set + per-seat) brought all 9 CIs to
  7,991–7,999 bytes — no rule dropped; the tooling's hardcoded "all 8" /
  "v3.4"/"v3.5" strings parameterized (`VERSION` const, seat-count derived).
- **Deliverables:** registry re-synced (`--write-registry`, 27 copies;
  `--check-registry` OK), per-project README v3.6 changelog + size + stagger
  tables, owner skim doc `docs/prompts/v3/CHANGES-v3.4-to-v3.5.md` (linked
  from docs/prompts/README.md), `regen_b_files.py` default run exit 0
  ("all 9 seats clean"), `bootstrap.py check --strict` green (born-red hold
  only).

Worker session (dispatched slice): no control/status.md write — the
coordinator's heartbeat owns routine state.

## 💡 Session idea

`regen_b_files.py` should drift-check each prompt file's `<!-- char-count:`
header comment against the measured paste-body counts (a one-line check in
`run_checks`). This session restated 18 stale count comments by hand-script;
the checker already computes the true numbers, so stale size headers are a
guard away from impossible. (Deduped: the tool gates the cap, but nothing
gates the header comment itself.)

## ⟲ Previous-session review

Stage-1 (PR #151) made the right D-9 call excluding open-PRs-stay-open as a
night-mode rule, and its explicit "NOT folded" changelog line is what made
this session's supersession trivially auditable — that discipline paid off
within hours. One miss it left behind: the v3.5 restamp did not touch the
registry-header strings hardcoded to "prompts v3.4" in `regen_b_files.py`, so
the regenerated projects/ copies shipped with mixed version labels. Stage-2
parameterized every such string through a single `VERSION` constant so a
future restamp cannot half-apply again — the durable fix, not just the
correction.
