> **Status:** `reference`

<!-- v3.7 · 2026-07-15 · provenance: DUTY-FORM rewrite (owner mandate, fm control/inbox.md ORDER 048, landed live 2026-07-15 in the hub working session). Body delta from v3.3: step 1 PARK becomes step 1 LAND — every session PR is driven to MERGED on green (the landing workflow merges; the step-5 flip releases it) or closed-with-reason, and the landing-path vocabulary drops owner-click-default / successor-review-merge for `lands-on-green (workflow)` (default) and `hub-venue` (owner-labelled / workflow-carve-out diffs); operational practices are phrased affirmatively throughout (what to DO), per the owner's instruction. Step order and the routine-disposition mechanics (F-1 failsafe bridge, business-cron rebind, fresh-session-per-fire keep) are UNCHANGED. This file stays the canonical single source; tools/regen_b_files.py verifies every startup's inlined copy byte-matches the step block below (drift class D-10). Edit HERE, then re-splice the 9 startups and rerun the check. Prior: v3.3 (expanded-startup rebuild) · v3.2 (stateless audit) · v3.1 (QA P0 ender fixes) · v3.0. -->
<!-- char-count: chat paste, the 8,000 console wall does not apply. -->
<!-- Artifact D — universal session ender. ONE version for every seat; no slots. Step order (QA R3-Q8: disposition precedes the heartbeat so delete outcomes land in the baton): 1 LAND → 2 RELEASE → 3 ROUTINE DISPOSITION → 4 HEARTBEAT → 5 FLIP → 6 REPORT. The FAILSAFE cron stays armed at close (F-1): it is the successor's dead-man bridge. -->

v3.7 · 2026-07-15 · universal session ender

SHUT DOWN NOW, in order. This closes your session's chain — the six steps below are the whole remaining work. (Coordinator sessions only: a dispatched/worker session skips steps 3–5 — it reports to its coordinator, and control/status.md stays the coordinator's.)

1. LAND — drive every session PR to terminal: MERGED on green (the repo's landing workflow merges once every required check on the head SHA is green — your step-5 flip is what releases it) or closed-with-reason; every PR leaves this step READY and accounted for. Checks still PENDING at ender time: spend the remaining poll budget (≤3 total); still pending → leave the PR READY with `pending @ <run-id>` named in the heartbeat's PR list — the landing workflow and the successor complete it. If anything merged this session, diff the merge commit and confirm the payload actually landed (an empty-vehicle merge is a known failure class). Name each still-open PR's landing path: `lands-on-green (workflow)` (default) · `hub-venue` (owner-labelled / workflow-carve-out diffs — queue the deep link).
2. RELEASE — delete your claim files (yours only; a possibly-live parallel claim stays its owner's).
3. ROUTINE DISPOSITION — the chain closes agent-side, in full, here: delete the pending send_later pacemaker + every session-bound WAKE trigger this session created, then VERIFY absent via list_triggers paginated to exhaustion (limit 100, next_cursor until has_more is false) — BEFORE the heartbeat, so the baton records what really closed. TWO EXCEPTIONS, kept armed by design: (a) the seat FAILSAFE cron — it stays ARMED as the successor's dead-man bridge (F-1); record its id + schedule in the heartbeat; the successor's boot cutover rebinds-then-deletes it; delete it yourself only on an explicit owner retire-this-seat order, and then only AFTER the heartbeat has landed and the card is flipped, as the true final call. (b) a BUSINESS cron (a scheduled deliverable — its prompt does real work): record id + next-fire for the successor to rebind — and a FRESH-SESSION-PER-FIRE business cron is recorded and KEPT AS-IS: it binds to no mortal seat session and outlives every seat by design. Deletion walled from this toolset → relay the SAME delete through a spawned worker (ONE trigger-MCP call per worker), verify; genuinely uncloseable → carry the id + why into the heartbeat (next step), for the successor.
4. HEARTBEAT — overwrite control/status.md as a COMMIT ON YOUR SESSION PR (main moves by PR, and a heartbeat committed on the PR travels with the merge — successors read the newest heartbeat across main + open PRs): routine disposition AS VERIFIED in step 3 (ids closed / ids uncloseable + why / ids left-for-successor / the failsafe id staying armed as the successor's bridge), the PR list with landing paths, ⚑ owner asks paste-ready, next-2-tasks baton. NEUTRAL facts + pointers ONLY — steering lines and denial quotes live in the PR body / owner-queue; durable links live in docs/current-state.md.
5. FLIP — flip the born-red session card to complete as the deliberate LAST COMMIT (after the heartbeat, so the merged PR carries its close-out) — the flip is what releases the landing workflow to merge your PR.
6. REPORT — shipped / merged / open-with-named-blockers / walls / flags, every claim citing a commit/PR/CI run; the durable copy goes IN THE SESSION PR BODY (the heartbeat's PR list points at it) — the chat message alone is invisible owner-side. Then END: the chain terminates with you — everything you armed is closed or recorded, and the failsafe bridge stands.
Confirm before ending: recite the ids you closed, the failsafe id left armed as the successor's bridge, any business-cron ids recorded (+ next fire), the ids documented uncloseable (+ why), and that the chain closed with zero new routines armed.
