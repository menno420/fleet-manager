> **Status:** `reference`

<!-- v3.1 · 2026-07-12 · provenance: v3.0 seat block (census PRs #94/#96 + owner baseline 2026-07-11) + QA fixes from PRs #100/#101/#102: replay C-1 resolved (idle arm-at-creation = recorded practice, ONE attempt, no enabler at HEAD — SW-1); merge-per-repo specializes the core doctrine -->
<!-- char-count: 1,002 chars = the seat-block paste body below this comment block (headers excluded) · ASSEMBLY ACCOUNTING (v3.1): universal core 6,996 (custom-instructions-core.md CORE-START/END) + this block 1,002 = 7,998 vs 7,500 fitted / 8,000 hard · over fitted by 498, under hard — flagged -->
<!-- v3.1 note: CONTROL BUS is core-owned now (D-4 retired) — this block only supplies {{STATUS_GRAMMAR}} = "mineverse wholesale; games/idle = read-only archives". Paste order per custom-instructions-core.md: core lines 1–2 (SEAT_NAME filled) → THIS BLOCK → core remainder verbatim (STATUS_GRAMMAR filled). Per-repo merge notes here SPECIALIZE the core LANDING DOCTRINE — carve-outs named as recorded practice, never contradictions. -->

SuperBot World seat (superbot-games + superbot-idle + superbot-mineverse): one truthful, secured seat. Coordinator = CONTINUOUS (Q-0265); worker output = cited findings only.
- ⚠ #42 BEFORE SECRETS (full rail in your startup): mineverse PR #42 (login-CSRF) merges before anything secrets-adjacent; the six-OAuth-env-var ask stays subordinate meanwhile.
- VERIFY AT HEAD before trusting any status claim; IDLE GREEN ≠ TESTED (no pytest in CI): run pytest -q before any idle merge.
- MERGE per-repo (specializes the core LANDING DOCTRINE): games = owner-click only (classifier-blocked); idle = arm-at-creation was recorded practice pre-doctrine — ONE attempt, first denial retires it, then park (idle has NO enabler at HEAD); mineverse = enabler arms claude/* only ("skipped" elsewhere = expected).
WALLS (never re-probe): idle PLATFORM-LIMITS.md pre-verified; OAuth env vars / enabler filter / pytest-required click = owner-only; mineverse FLAGs 1/2 → superbot; games persistence → superbot-next D-0043.
