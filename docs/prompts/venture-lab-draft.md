# venture-lab — proposed mission (NOT YET DEPLOYED)

> **Status:** `plan`
>
> **NOT YET DEPLOYED** — drafted 2026-07-09. Proposed as the **gen-2 born-right
> pilot**: the first lane launched from the gen-2 seed standard
> ([`../gen2-blueprint.md`](../gen2-blueprint.md)) instead of the gen-1 texts.
> Repo + Project creation are owner clicks (see `docs/owner-queue.md`).

## Proposed mission

**Find and validate the cheapest credible path to first revenue.** Agents
build, the owner clicks. Systematically generate, score, and validate
candidate ventures; ship the smallest real artifact that can earn a first
dollar; keep an honest ledger of what each candidate actually costs and
returns.

## Hard rails

- **No spend, no account creation, no publishing, no payment flows without an
  owner action** — every such step is queued in WHAT/WHERE/HOW format
  (click-level, per playbook R17), never performed by the agent.
- **Token-cost accounting per candidate** — every candidate carries its own
  running cost line (agent tokens spent researching/building it), so
  return-on-agent-labor is measurable, not vibes.
- **Distribution-first scoring** — every candidate must name its
  first-ten-customers path; a candidate that cannot name one scores down
  automatically. (The retro of the monetization-video analysis showed the
  headline patterns are audience-dependent; see the shortlist's skeptic filter.)

## Opening corpus

The venture shortlist:
[`../findings/venture-shortlist-2026-07-09.md`](../findings/venture-shortlist-2026-07-09.md)
— 5 least-investment candidates (membership-site boilerplate kit,
agent-workflow template packs, sponsorship-report micro-SaaS, fixed-price site
builds, programmatic affiliate directories), each with an agents-alone /
owner-clicks split and a named first-revenue path.

## Why this is the gen-2 pilot

A brand-new lane with no legacy state is the cheapest place to prove the
born-right seed standard (kit adopted at birth, CI-aligned auto-merge,
conventions file, control files + capability manifest + PLATFORM-LIMITS day 0,
heartbeat-before-work, walking-skeleton-through-merge-path). Launching
venture-lab gen-1-style would just add a 10th lane carrying all 13 known
cross-patterns; launching it gen-2 tests the fix set.
