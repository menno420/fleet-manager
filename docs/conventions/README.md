# `docs/conventions/` — Working conventions a live session is expected to follow

> **Status:** `reference`
>
> **Tier: TASK** — live; read when your task touches it.
> Where you are in the estate: [the map](../MAP.md).

The doc-routing hook surfaces the Gemini convention at the moment of a Gemini call; the review convention defines the [survived]/[conceded]/[partial] vocabulary the Stop hook expects. `outbox-rollover.md` governs a retired surface and reads as history.

| file | what it is |
|---|---|
| `adversarial-review.md` | Long `reference` doc (self-declared unratified proposal) for provenance-based adversarial review: the two question layers, the narrow gat…. |
| `outbox-rollover.md` | `binding` convention for rolling append-only control/outbox.md files past 200,000 bytes into dated archives with mandatory pointer stubs …. |
| `vertex-first-for-gemini.md` | `binding` owner directive (2026-08-05, corrected 08-06): two Gemini identities and three billing outcomes, the Deep Research exception, t…. |
| *…3 more* | see the files themselves — each gist is in [the audit's raw record](../audits/2026-08-10-full-read/raw/gists.tsv). |
