# The v1.21.0 phase-3 + gba-bypass review — four self-flagged items, settled

> **Status:** `reference` · 2026-08-14 · the review half of the follow-on
> session (fm #858). The phase-3 wave record
> ([`.sessions/2026-08-13-kit-v1210-rollout-phase3.md`](../../.sessions/2026-08-13-kit-v1210-rollout-phase3.md))
> and the gba-bypass card
> ([`.sessions/2026-08-14-gba-bypass-merge.md`](../../.sessions/2026-08-14-gba-bypass-merge.md))
> flagged four claims for adversarial re-check; each is settled below. Method
> mirrors [`2026-08-13-v1210-phase2-review.md`](2026-08-13-v1210-phase2-review.md);
> certainty tags per
> [`2026-08-05-foundation-continuation.md`](2026-08-05-foundation-continuation.md).
> Everything here is `MEASURED` this session over the direct-PAT path unless
> tagged otherwise. The one records correction was applied **in place, dated**,
> at the bypass-card site it touches — this file carries the evidence.

## 1 · superbot-mineverse: the round-2 revert held — zero regen residue at `main`

The wave record conceded that its round-1 "gate regen revert" was a measured
no-op (`git checkout HEAD --` against the already-committed regen) and was
redone against the merge-base only after Codex's round-2 catch. Spot-checked
`main` for residue anyway, three ways:

- **The merged diff contains no live-workflow path at all.** `GET
  /pulls/144/files` lists 18 paths: `bootstrap.py`, `substrate.config.json`,
  `.substrate/**` (including the kit's *staged* `ci/substrate-gate.yml` copy —
  the correct, inert home for the template), `docs/**`, and the session card.
  **No `.github/workflows/` entry** — the regen was fully unwound before merge,
  not merely counter-edited.
- **Both live workflows at `main` are blob-identical to the pre-#144 parent.**
  `.github/workflows/substrate-gate.yml`: blob `a354a27af6` at `main` == at
  parent `f158df612`. `.github/workflows/auto-merge-enabler.yml`: blob
  `9feb8bcb13` == `9feb8bcb13`. Read directly as well, not only by blob SHA:
  the live gate (188 lines) contains **zero** references to `pytest`,
  `requirements` or `tests/` — the template step that would have held the
  required gate red past the flip (kit worklist row 16) is not in the tree.
- **`main` is the #144 squash itself** — HEAD `9f8047f9e`, `compare` ahead_by
  0 — so nothing later reintroduced the regen.

**Verdict: no residue; the in-thread hash proof (`51f3ea33…` → `bf644599…`)
described a fix that genuinely landed.** The self-flag was earned by the false
round-1 claim, and the fix survived its own session.

## 2 · The gba turn's confident claims: every sibling of the wrong one re-measured true

The bypass turn had already been caught once overclaiming — *"nothing in CI
executes the NDS titles"* was wrong, corrected on gba #216 — so its other
confident claims were re-measured rather than inherited:

| claim (fm #856/#857 records) | measured 2026-08-14 |
|---|---|
| ruleset restored `['NDS ROM build', 'ROM builds']` | `GET /rulesets` → one ruleset, `main-branch-protection`, **active**, required contexts exactly `['NDS ROM build', 'ROM builds']` |
| #215 merged under the 3-second bypass window | merged `2026-08-14T05:58:15Z` @ `7a4977bb0` — inside the recorded lift 05:58:13Z → restore 05:58:16Z |
| #216 closed-unmerged, branch retained | state closed, `merged: false`, closed 06:41:13Z; `claude/nds-toolchain-1-22-3` alive at `fb73514` |
| mirror release live, 1 asset, hash-pinned | `ci-toolchain-mirror-1` → exactly one asset, `blocksds-toolchain-1.22.3-1-x86_64.pkg.tar.xz` (3,057,772 B); the #216 verdict comment records all 8 packages fetch-and-hash-verify against it |
| gba tree-verified at `main` | raw `bootstrap.py` sha256 `8807a00e…`, `KIT_VERSION = "1.21.0"`, config pin `1.21.0` |
| the **correction** itself: `rom-builds.yml` boot-runs the NDS titles | verified in the tree: two `install headless DeSmuME (py-desmume)` steps (`pip install py-desmume==0.0.9`), 82 `.nds` references, "twenty-eight headless py-desmume proofs" + "twelve headless py-desmume proofs" in the workflow's own comments — the harness that caught the half-rate regression |

**Verdict: the one wrong claim was already corrected in the record, and no
sibling claim from that turn failed re-measurement.**

## 3 · The Underroot half-rate: the scope discipline holds; hardware stays open

The measurement (per-vblank counter `t[2]`: 74@150 · 124@250 · 174@350 ·
249@500 — slope exactly 0.5 from boot) exists in two venues, CI and one local
run, **both py-desmume**. What the review checked is whether any committed
record rounds it up ("the game is broken") or down ("goldens drifted"):

- The #216 verdict comment states it exactly: *"**Not a golden to re-pin — the
  game genuinely runs at half speed under this SDK in the emulator.**"* — with
  the cause honestly *"undiagnosed"* (same single-`swiWaitForVBlank()` loop as
  full-rate Gloamline, so a game-workload × libnds-1.22 interaction).
- The §7 rows and both cards say "the emulator harness caught" / "measured in
  CI and reproduced locally" — venue-scoped, no generalisation to hardware.

**Verdict: scope discipline holds everywhere the number appears. Hardware
behaviour is LEFT OPEN with the reason:** no hardware venue exists in this
estate's CI, and the migration plan's own step list ends in an owner play
test — that is where the hardware question gets its answer, not in a record
written from an emulator.

## 4 · idea-engine: gate MEASURED green at `main`; the retired-roster question stays flagged

`ideas/shiftlife/` (created by #899 to satisfy `check_sections` against the
roster frozen at retirement) exists at `main` with its `README.md`. The gate:

- Push-runs on `main` stop at 2026-07-21 — **not** a red gate but the known
  GITHUB_TOKEN auto-merge suppression (another sighting of the mechanism the
  phase-2 review confirmed on superbot-next; the enabler merges as
  `github-actions[bot]`, so no push run ever fires).
- Measured by the squash-tree method instead: #899's head `2f01fb183` carries
  tree `c3862e0a67`, **hash-identical** to the merge commit `320acabd9` —
  which *is* `main` (ahead_by 0). The head's check runs: `substrate-gate`
  **success**, `enable-auto-merge` success. The gate is green on the exact
  tree `main` holds.

**The flagged-not-decided question, recorded rather than resolved:**
idea-engine's required gate runs `check_sections` against a roster **frozen at
the 2026-08-07 retirement** with shiftlife still listed active — so a
retired registry now decides what a live required check demands, and the
`ideas/shiftlife/` section exists to satisfy it. Whether a required check
should consult a retired roster at all (re-point it, prune the roster, or keep
the section) is an idea-engine design call for the owner — flagged on the
session card, deliberately not decided here.

## Also re-derived, not inherited

- **The kit registry at HEAD** (`docs/adopters.md`, stamp
  `2026-08-14T05:59:49Z`): **9 current · 3 honestly stale** — superbot-games
  v1.20.1 (+3-file self-report DRIFT) · trading-strategy v1.17.0
  (owner-skipped until its archive decision) · pokemon-mod-lab v1.15.0.
  Spot-checked against the adopters' own trees, not just the generated rows:
  superbot-games `KIT_VERSION = "1.20.1"` / pin `1.20.1`, and its three
  heartbeat lines read exactly as the DRIFT report says (`status.md`:
  *"kit: substrate-kit v1.15.0"* · `status-mining.md`: *"v1.7.1"* ·
  `status-exploration.md`: *"v1.7.1"*); pokemon-mod-lab `KIT_VERSION =
  "1.15.0"` / pin `1.15.0`, its private tree readable over the direct-PAT
  API path this session.
- **The 24-repo open-PR sweep**: exactly **2 open `claude/*` PRs** across the
  account — sim-lab #344 and trading-strategy #160, both pre-existing July
  parks — and 0 open PRs in fleet-manager at `main` `108b452`. One delta
  against the handoff's ~06:50Z snapshot: superbot holds a **7th** open PR,
  #2444 `bot/dashboard-refresh` (*chore(dashboard): refresh generated data*),
  created **2026-08-14T09:19:01Z** — after the snapshot, by the repo's own
  standing automation. Not session work, out of scope, noted so the next
  sweep is not surprised. spider-swing's 1 open PR is a Dependabot bump (the
  owner's own surface).

## The records correction this review applied

The gba-bypass card recorded gba #216 as *"holds open, carded and
`do-not-automerge`, as the migration vehicle"* — true at the card's commit,
reversed ~25 minutes later when the owner ruled that nothing waits in an open
PR and #216 was closed-unmerged (06:41:13Z, branch retained). The card is
annotated in place, dated; the ruling itself is now a stamped entry in
[the decisions ledger](../decisions.md) rather than a chat-and-comment-only
fact — the program's §7 review row carries the id, one citation home per the
stamp rule.
