# Findings — committed worker-research record

> **Status:** `living-ledger`
>
> Consolidated, dated findings from the manager's research/observation workers.
> Each doc is `reference` — a distillation of a worker's final report, committed
> so nothing lives only in an archivable chat. Verify claims against repos
> before acting on them (playbook R2).

## Index (2026-07-09, evening — succession set)

| File | What it is |
|---|---|
| [`retro-synthesis-2026-07-09.md`](retro-synthesis-2026-07-09.md) | The full 10-lane retro synthesis: coverage, per-lane digests, 13 cross-patterns, disagreements, stall census, owner-action merge, model census. **Primary input to the gen-2 blueprint.** |
| [`gpt-5-6-report-2026-07-09.md`](gpt-5-6-report-2026-07-09.md) | GPT-5.6 launch research: facts, pricing, relevance to the program; Codex-arm recommendation; METR eval-gaming caution |
| [`venture-shortlist-2026-07-09.md`](venture-shortlist-2026-07-09.md) | Monetization-video analysis: patterns, skeptic filter, 5-idea least-investment shortlist (venture-lab opening corpus) |
| [`ui-visibility-2026-07-09.md`](ui-visibility-2026-07-09.md) | Two screen-recording analyses of claude.ai fleet visibility: session-level signals good, zero project-level rollup |
| [`ping-test-2026-07-09.md`](ping-test-2026-07-09.md) | Fleet-wide PING dispatch table + write-latency findings (30s–14min by gate weight); ack sweep pending at handoff |
| [`ci-tier-sim-2026-07-09.md`](ci-tier-sim-2026-07-09.md) | CI-tier simulation: 451 PRs / ~1250 runs calibration + 144-cell × 40-seed discrete-event sim of 4 CI regimes × 3 repo classes. **Basis of the blueprint's CI-TIER STANDARD** (Tier 1 labs adjusted smoke-only → fast-full) |
| [`gba-toolchain-proof-2026-07-09.md`](gba-toolchain-proof-2026-07-09.md) | GBA toolchain scout ground truth (night): both game-lab tracks GO — pokeemerald byte-identical 1m20s/2.0s; Butano 17.5s via leseratte10 devkitARM r68 mirror; mGBA headless loop ~290 fps — caveats + private/public rails |
| [`ultracode-verification-2026-07-10.md`](ultracode-verification-2026-07-10.md) | Adversarial verification (R2) of tonight's three ultracode-class outputs against the repos at HEAD: superbot #1911 grand review 18/20 verified (starboard/paragon parity-row error), fm #15 experiment package 13 verified / 2 minor, superbot #1913 audit 17 verified / 1 refuted — dispositions + ⚑ owner flags |
| [`fable5-review-2026-07-09.md`](fable5-review-2026-07-09.md) | Fable-5 ultracode fleet review synthesis: 30/30 adversarially confirmed findings across instructions / goals / feedback-synthesis / new-ideas / consistency; blueprint amendment **PROPOSALS P1–P11** (next blueprint edit owned by the CI-tier sim session); drift-fix list D1–D6; companion: 10 PROPOSED instruction packages in `docs/proposals/instructions/` + 3 captured ideas |
| [`night-review-2026-07-10.md`](night-review-2026-07-10.md) | **Night review (audit)** of the first fully-autonomous night (116 PRs / 10 repos / zero stuck): 10-sentence executive summary, full 26-question Q&A (outcomes, code quality, process, safety, kit, platform, worries, next), send-ready Anthropic-email findings, ranked recommendations before scaling, "Where we were lucky." Key catches: venture-lab D1 Stripe bug behind ⚑B, superbot-games CI collecting 73/121 tests, pokemon-mod-lab PUBLIC against its "no exceptions" PRIVATE rail (all 13 account repos public) |
| [`fleet-economics-2026-07.md`](fleet-economics-2026-07.md) | **Fleet economics ledger (ORDER 004, P0)** — pre-close snapshot before the free EAP window ends 2026-07-14: per-lane table across all 13 repos (merged PRs 566 in-window / commits / session cards / Actions-run CI signal / routine state), honest nulls for CI-minutes+token+dollar (not agent-visible), top-3 heaviest lanes, cadence-vs-activity outliers |
| [`instruction-and-env-audit-2026-07-11.md`](instruction-and-env-audit-2026-07-11.md) | **Fleet instruction + environment audit (owner-directed, 2026-07-11).** Cross-project comparison of every lane's `projects/<repo>/` instructions and `environments/` scripts. Headline: the owner-landed `UNIVERSAL.md` merge clause tells every seat to do the classifier-**walled** thing (arm auto-merge / REST-merge yourself) — 12/13 lanes walled, only substrate-kit correct; the kit's own CAPABILITIES has the working recipe (open READY, the enabler workflow lands it). Includes the PROPOSED corrected UNIVERSAL clause (owner must land), the per-lane doctrine table, the instruction-drift fixes, and env consolidation R1–R6 (5 archetypes ≈ one base + 3 knobs; 5 live lanes unregistered). Drives ORDER 016. |
| [`permission-rules-port-2026-07-11.md`](permission-rules-port-2026-07-11.md) | **Agent-permission port (owner-directed, 2026-07-11).** Provenance for `.claude/settings.json` — the 12 explicit `mcp__github__*` landing allows mirrored from superbot `.claude/settings.json` @ `fae8bd0` (fixes the "Merge Without Review" classifier denial (2026-07-11, then-observed) on CI-green landings), plus the decide-and-flag list of superbot grants deliberately NOT ported (bypassPermissions, bare `mcp__github` server-wide allow, Bash lists, hooks) |
| [`model-matrix-2026-07.md`](model-matrix-2026-07.md) | **Fleet model matrix (ORDER 010)** — per-repo Project-setting (honest unknowns) vs card-self-reported model families (family-level per Q-0262), routine-vs-manual where determinable, the websites 16:01Z cross-surface disagreement (Routines screen fable-5 vs card sonnet-5); conclusion: per-session self-report is the least-bad attribution basis |
| [`branch-recreation-census-2026-07-14.md`](branch-recreation-census-2026-07-14.md) | **Branch-recreation census (coordinator-run, 2026-07-14)** — four-repo read-only sweep: 460/491 surviving claude/* branches (93.7%) sit at exactly their merged PR's head SHA; two-tier mechanism (auto-delete appears not to fire for bot-auto-merged PRs; post-merge session pushes re-create deleted refs — 6 diverged survivors); fm-seat Q-0120 spot-check verdicts; hub-sweep-safe corollary. Feeds email ask 4 + owner-checklist row 11 |
| [`2026-07-22-pat-and-automode-capabilities.md`](2026-07-22-pat-and-automode-capabilities.md) | **Verified GitHub capability matrix + access-path mechanics (2026-07-22, live owner session).** Every "verified" row tested with a real HTTP response (reversible probes); the access model (Claude GitHub App vs direct-PAT path), per-capability matrix, and the working recipes — capability-focused per owner instruction. *(Badge + index row added 2026-07-23 by the hub session — the file landed via #450 badge-less/orphaned and pre-reddened every full-gate PR.)* |
| [`2026-08-05-gemini-url-accuracy-benchmark.md`](2026-08-05-gemini-url-accuracy-benchmark.md) | **Gemini URL-reading accuracy benchmark (owner-directed, 2026-08-05).** Ten URLs already fetched by hand, re-asked of `gemini-3.1-pro-preview` via `url_context`. Retrieval split is total: **0/8 `support.google.com`, 2/2 `developer.android.com`** — and on failure the model answers from training data *phrased as if it read the page*. Cost: it stated **"at least 20 testers"** citing a page that says **12** (20 is the superseded figure), while the same model with `google_search` grounding answered 12 correctly an hour earlier. **The tool, not the model, decided correctness.** The instrument that catches it is `urlContextMetadata.urlMetadata[].urlRetrievalStatus` (and `groundingMetadata.groundingChunks` for search grounding) — prose gives no signal. Same shape as the HUD-telemetry finding: trust the instrument, not the narration |
| [`2026-08-05-google-play-submission-requirements.md`](2026-08-05-google-play-submission-requirements.md) | **Google Play submission requirements for spider-swing (2026-08-05).** Every requirement acted on carries the official URL the session fetched itself; everything unconfirmable is marked `NULL — unverified` rather than smoothed. AAB mandatory for new apps since 2021-08; target API **36** from 2026-08-31; Play App Signing upload-vs-app-signing key split (a lost upload key **is** recoverable); listing asset and character limits; privacy policy + Data safety + IARC + target-audience **all required even for an offline no-data game**. Headline is schedule, not build: a personal account created after 2023-11-13 needs **12 testers opted in 14 continuous days** before it may apply for production, then ~7 days review — a three-week floor. Method note: grounded citations treated as leads, which caught a **miscitation** (`answer/9859751` cited for content rating is the publishing-status page) and **two zero-grounding-chunk answers** answered from model memory. Buildable half → spider-swing #162 / ADR 0005; owner half → six `OQ-PLAY-*` entries |
| [`2026-08-05-playtest-discord-and-superbot-value.md`](2026-08-05-playtest-discord-and-superbot-value.md) | **A playtest Discord for Slingy Spider, and which superbot parts serve it (owner question, 2026-08-05).** Discord's own game-dev docs + Game Developer Playbook fetched in session for the channel/role structure and the *"one developer reply per day"* line; the support-centre articles are **Cloudflare-403 to automated fetching** and are cited from nowhere. Reframes the goal: Play's 14-day rule requires testers to stay **opted in, not active**, so retention is "give them no reason to leave", not engagement. Deployment state **measured, not read**: `superbot` is live (Railway `reliable-grace`/`worker` SUCCESS 2026-08-05T10:53), `superbot-next` has never deployed — and the GitHub API says **both repos are `archived=false`**, contradicting superbot-next's own closeout claim of permanent read-only. Headline gap: **cog routing is fully built and never enforced** — storage, mutation, audit, named profiles, wizard UI and Access Map all exist, but `resolve_command_access` never consults `command_routing`, so a "disabled" cog still runs; superbot-next independently reaches the same state and says so. Working lever today is `command_access` mode `selected_channels`. Also measured: no invite attribution anywhere in `disbot/`; Discord's guidance puts bug intake in **Forum channels**, not the bot's ticket system |
| [`2026-08-05-superbot-next-live-audit.md`](2026-08-05-superbot-next-live-audit.md) | **superbot-next booted, driven live, and measured (owner-directed, 2026-08-05).** Written for the owner's rebuild plan — several sessions and several models reviewing both bot repos independently, then one plan executed cog by cog, first milestone a **server bot with no game features**. Headline: `CAPTURE-WORLD LITERAL`, a **named convention** in that codebase where a value the old bot *computed* ships as a constant transcribed from the capture corpus — *"both goldens pin the one value, so the line ships as the pinned literal."* Worst case is the Cog Manager: **58 hardcoded old-bot module names**, status glyphs baked into the f-string (`f"✅ 🟢 …"`, no check), Unload observed `blocked` live, and a legend advising `!cog unload` — one of the 3 commands never ported. It scores **perfect golden parity because it IS the captured bytes**: a refusal, a photograph, and an absence are all invisible to a byte-comparison harness, so `533/533 · 49/49` is true and compatible with panels that do nothing. Scope measured honestly — **4 labelled files** (not the 34 mentioning "capture world"; most are legitimately-static game data), with the limit stated: unlabelled instances are invisible to a grep. Also measured by running it: boots `verified: true`, 1,327 dispatch targets, **640 buttons/161 panels** (an earlier "zero components" claim was a wrong key), 49/49 manifests load with **no filter parameter**, zero runtime load/unload, 17 of 368 command names absent (**3** real: `cog`/`loadall`/`unloadall`), 70 "not armed" terminals. **Corrected same day (§ 4b):** an earlier "menu-vs-text ratio is inherited, not introduced" claim was wrong — it counted how many *commands* build a view, in a bot whose commands nobody types. In superbot the **navigation graph IS the product** (owner: *"the only command anyone ever needs is `!help` … always 2 taps away from any command"*, confirmed from a screen recording — Blackjack 8 launch buttons, Server Stats live counts over an 11-button grid). Measured on the rebuild: **60 of 66 `help` panels have ZERO buttons (91%)**, 153 of 314 overall — the front door renders the captured *text* of the old help output instead of a live route table, via the same `command_catalog` literal. Retargets milestone one: **the navigation tree is milestone zero**, and the acceptance test the golden harness could never express is "from `!help`, every feature reachable by clicking in two taps". Confirms the owner's bootstrap diagnosis: a missing intent silently killed **1,300 of 1,327 targets** with one log line, where superbot's first-loaded `bootstrap_access_cog` guarantees an operator bypass. Carries a **§ 7 of this session's own five wrong claims** — including calling the environment's test token "production" — all traceable to skipping both repos' required boot files, plus the note that the owner's first review ran against a bot booted without intents on a 40-minute-old empty DB |
| [`merge-on-green-rollout-verification-2026-07-15.md`](merge-on-green-rollout-verification-2026-07-15.md) | **Merge-on-green rollout verification (audit, 2026-07-15).** 19-repo read-only sweep (14:00–14:10Z, four workers): **13 PROVEN / 5 INSTALLER-PR-OPEN / 1 MISSING**. Today's 13:41–13:57Z rollout wave landed nothing on any main — all five installer PRs (opus4.8 #24 · fable5 #17 · product-forge #25 · pml #89 · plugin-hello #3) self-parked on the workflow-file owner-merge-only rail; sonnet5 skipped. Cross-cutting gaps: superbot PAT attribution, branch-prefix allowlist holes, plugin-hello inert-without-CI, fm #227 conflict-dirty despite green checks. Feeds owner-queue OQ-ROLLOUT-INSTALLER-CLICKS + the A#63 amendment |
