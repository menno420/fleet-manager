# CONTRACTS — night fleet audit, 2026-09-01/02 (fleet-preflight sheet)

> **Status:** `reference` · the fleet-preflight sheet for the night session continuing fm #1009: Fleet A (rerun of `04-eap-mail-evidence-pass.js`, args only) and Fleet B (`05-eap-false-done-ledger.js`, authored tonight). Venue: cloud container, this session, concurrency unmeasured until the demand test below. Filled before either fleet's first agent spawns, per `fleet-preflight`.

## Fleet A (rerun, unchanged script, new args)

AGGREGATE   : dies_if (a.refuted || b.refuted) || (a.already_covered_by && b.already_covered_by) — bare truthiness, unchanged, this is what tonight's run actually executed · unread fields 0 (REPORT_ONLY: what_i_opened, discrepancies, corrected_claim) · fixture kill 2/3, survive 1/3 (asserted in-script before any agent spawns, unchanged from the pilot) · refute authority 2/2 (both lenses told to refute; per-lens verdicts kept). NOTE (Codex review round 11): Fleet B's script (line 19 below) was patched post-run with isNegativeCoverage() normalization; this script (04) was not, per this repo's own convention of not editing scripts committed verbatim-as-run. The two fleets therefore now run DIFFERENT survival rules if both are rerun as committed — if both verifiers on some future Fleet A candidate emit negative prose ("none —"/"n/a —") instead of an empty string, Fleet A's bare-truthiness check would still wrongly treat that as agreement-on-coverage and kill the row, where Fleet B's normalized check would not. Not observed in tonight's actual Fleet A run (not checked); flagged here as a known, disclosed divergence for whoever reruns Fleet A next.
INSTRUMENT  : UNCONTRACTED — no regex or extractor; the readers are the instrument. Cover: the pilot's transcripts already read whole (04-CONTRACTS.md); this is a full rerun of the same script, not a new instrument.
PILOT       : already run 2026-09-01 (04-CONTRACTS.md: 18 agents, 1.22M tokens, 19.0 min, all transcripts read whole, 4 changes made). Not re-piloted tonight — same script, same corpus, only judgeModel/paths changed via args; the changes already landed in the committed script.
CORPUS      : fleet-manager@fa9a391 — 20 synthesis/record documents ≈ 590 KB (unchanged from pilot; verified fa9a391 vs pilot's cb3fc9a base touches only 2026-09-01-owner-direction.md, 2026-09-01-fleet-manager-measured.md and the workflow/planning artifacts the pilot itself produced — none are in the DOCS/OWNER_DOCS reader lists, so this is a genuine gap the completeness critic is expected to name, not silently patched in, since Fleet A runs "unchanged except args" per the night brief) · 284-pattern catalogue (1.47 MB) resharded fresh tonight into 12 files at /tmp/eap-night/patterns (161684+136817+130276+123659+119956+129328+120359+125216+127275+123470+117583+93707 bytes; high-severity&repo_count>=3: 118; no covering mechanism named: 51; panel-killed: 7) + 20 repo censuses (395 KB) · prior mails: design was 7 readers (6 superbot + this repo's current draft), CORRECTED per Codex review round 8 — the executed run's retained result carries 6 (5 superbot: anthropic-email-2/3/4, gen1-wrapup-email-final-candidate, 2026-07-18-followup-email-draft; + the current draft); permission-classifier-findings-consolidated-2026-07-16.md is missing from the executed prior-mail EXTRACTION readers despite being cited as covered_by for non-survivor A3-7 — CORRECTED rounds 9-10: not "never opened" for A3-7 — its verifiers list the file in what_i_opened and cite it directly; the gap is narrower, absence from the systematic extraction stage only. B14 is NOT the same gap (checked directly, round 10): its already_covered_by names anthropic-email-4-classifier-regression-sent-2026-07-16.md, a file that WAS in the executed 6-file corpus (superbot HEAD unchanged since pilot — 5e3a667 both times).
RETAIN      : unchanged from 04-CONTRACTS.md (per finding: claim · numbers+method/date · FILE:LINE · certainty · scope · novelty; per pattern: verbatim instance cites, repo_count, severity; per verdict: what_i_opened, discrepancies, corrected_claim).
BASE        : fleet-manager@fa9a391 at 2026-09-01T21:42:46Z · open PRs 0 · superbot@5e3a667 docs/eap fetched 2026-09-01T21:40Z (unchanged SHA from pilot) · re-read fa9a391..origin/main before writing the record.
SIZE        : see SIZE (shared) below.
EXTERNAL    : one Codex round requested at flip-readiness on the PR carrying the combined report (comment `@codex review`).
UNCONTRACTED: INSTRUMENT (above, with its cover). Same citation-drift caveat as the pilot: a cite into another repository resolves at that repo's current HEAD, not the harvest SHA — reported as "not re-verifiable", never as false.

## Fleet B (authored tonight — 05-eap-false-done-ledger.js)

AGGREGATE   : dies_if (a.refuted || b.refuted) || (!isNegativeCoverage(a.already_covered_by) && !isNegativeCoverage(b.already_covered_by)) — UPDATED after Codex review round 1 (fm #1010): the executed run used Fleet A's identical bare-truthiness expression; isNegativeCoverage (treats "", "none — ...", "n/a — ...", "not covered by ...", "no ..." as non-coverage) was added afterward and is NOT what tonight's run enforced — recorded here as the current script state, not as what produced the committed JSON · unread fields 0 (REPORT_ONLY: what_i_opened, discrepancies, corrected_claim) · fixture kill 2/4, survive 2/4 (current script; the run itself asserted the original 3-fixture 2/3-vs-1/3 form) · refute authority 2/2 (holds-lens and fit-lens both told to refute; per-lens verdicts kept)
INSTRUMENT  : UNCONTRACTED — no regex or extractor; the 48 reader units are the instrument (one reader per file, split at 400 lines by line range, per the night brief). Cover: pilot below, read whole.
PILOT       : run wf_d0348386-8b6, 2026-09-01T22:25Z. 3 readers (docs/eap-story.md[1-300], docs/PROJECT-CLOSEOUT.md[1-210], superbot anthropic-email-2-draft-2026-07-11.md[1-310]) + 2 merge groups + 1 dedupe + 15×2 verify + 1 critic = 37 agents, 2.66M tokens, 38.4 min wall (concurrent with Fleet A — real cost to Fleet A's wall-clock, see DECISION below). Output read whole (all 15 verdict pairs + the critic). RESULT: 59 claims/35 corrections from just 3 reader units → 18 merged rows → 16 ranked → 15 verified → 4 survivors (L02 venture-lab Stripe-path false-green, L03 substrate-gate 73/121 false-coverage claim, L04 pokemon-mod-lab false-PRIVATE claim, L08 self-arm-routines partial false-done); 11 refuted, 0 rows killed by both-lenses coverage agreement (individual fit-lens verdicts DO populate already_covered_by for several rows including L01, L02, L05, L06, L15 — none reached agreement with the paired holds-lens, so none were killed by it; see the report's own § 3 pilot section). CHANGED: (a) confirmed the rank/verify caps (30 rows, 60 verify agents) hold regardless of reader-corpus density — merge-group count is the only stage that scales with corpus size, so CHUNK raised 25→40 for the full run to bound it further; (b) the critic surfaced that verifiers, while checking a citation, sometimes found a BETTER primary source outside the fixed 11-file reader list but still under fleet-manager (e.g. docs/findings/night-review-2026-07-10.md, which the critic names as the primary source behind 3 of 4 survivors) — verifiers are not restricted to the reader corpus (only readers are), so this self-corrects inconsistently; recorded as a data-quality caveat for the report, not a corpus change (Fleet B's corpus is DECIDED, not relitigated mid-run); (c) non-survivors need a reason logged beyond the bare word "refuted" for auditability — the merge/dedupe prompt already asks the dedupe stage to log a drop reason, but individual verifier refutations do not carry one in the row itself; the report's "dropped, and why" table will pull the reason from the verifier's own discrepancies field, which does carry it, rather than changing the schema mid-run; (d) L07/L08/L12 in the pilot show the same underlying claim (self-arming routines) surviving under one id and refuted under two others — a genuine dedupe-thoroughness gap the full run's larger merge pool will not fix by itself; flagged for the report as "same-mechanism split verdict", not treated as a blocking defect.
CORPUS      : superbot@5e3a667 docs/eap — 30 files, 6,192 lines, 507,874 bytes (fetched 2026-09-01T21:40Z via direct GitHub API egress, base64-decoded) · fleet-manager@fa9a391 — 11 named EAP docs, 343,739 bytes (docs/eap-story.md 580 lines, docs/eap-retrospective.md 427, docs/eap-audit-collection.md 102, docs/eap-final-recon-2026-07-14.md 116, docs/launch-readiness-2026-07-10.md 710, docs/fleet-inconsistencies-2026-07-13.md 312, docs/pr-landing-audit-2026-07-16.md 131, docs/dispatch-log.md 671, docs/audits/eap-project-audit-2026-07-14.md 215, docs/fleet-account-2026-07-26.md 314, docs/PROJECT-CLOSEOUT.md 414) · combined 41 files / 851,613 bytes, expanded to 48 reader units (7 files over 400 lines split in two: superbot gen1-wrapup-email-final-candidate.md 425 lines and anthropic-email-2-draft-2026-07-11.md 608 lines; fleet-manager eap-story.md, eap-retrospective.md, launch-readiness-2026-07-10.md, dispatch-log.md, PROJECT-CLOSEOUT.md).
RETAIN      : per reader — claims[] and corrections[] separately, each with statement, citation, date_stated, certainty, topic (corrections also carry refers_to); per ledger row — claim, claimed_where, claimed_when, actually, found_when, found_by, citation_claim, citation_found, certainty; per verdict — what_i_opened, discrepancies, corrected_claim, already_covered_by. Follow-ups: "which corrections had no matching claim" (orphaned_corrections, with reason) yes · "is this false-done already named in docs/traps.md" (already_covered_by) yes, though never checked against traps.md as a corpus item (see UNCONTRACTED) · "which of the 505 claims were never matched to a row" **[CORRECTED, Codex review round 7]** NOT directly answerable from the retained output — `readers[].claims` holds all 505 but nothing in the return links a specific claim to whether it was matched, and the capped `ranked`/`verified` arrays (30-45 of 150 merged rows) don't preserve full claim-to-row traceability either; answerable only by re-deriving the match from `readers[].claims` against `verified[].row.claimed_where` citations by hand, not a stored field.
BASE        : fleet-manager@fa9a391 at 2026-09-01T21:42:46Z · open PRs 0 · superbot@5e3a667 docs/eap fetched 2026-09-01T21:40Z · re-read fa9a391..origin/main before writing the record.
SIZE        : see SIZE (shared) below. If Fleet A alone would run past 04:00 Europe/Amsterdam (02:00Z), Fleet B's satellite-heavy (superbot, 32 of 48 reader units) lane is dropped before its fleet-manager lane (16 units) — recorded in the report as what ran and what was cut.
EXTERNAL    : same PR — THIRTEEN Codex rounds so far (each on the prior round's fix commit, since a review binds the SHA it ran on, TRAP-006/007), 78 findings total (10+10+8+5+1+3+6+5+5+3+8+6+8). Full per-round breakdown for rounds 1-11: git log on this branch and the PR's own review comments (round 11's commit message records its two substantive core-finding reversals in full). Round 12 (6 findings, all sync/labeling, no new factual reversals): the 04-vs-05 survival-rule divergence was disclosed in the CONTRACTS sheet but two more places (05's own meta.phases comment, workflows README) still called it "the same rule" — fixed at the source phrase in both; the CONTRACTS sheet's pilot summary still said "0 already-covered" after the report's own § 3 was corrected — synced; an owner quote had its typo ("whih") silently normalized to "which" with an added period and no OWNER label — restored verbatim with the label and citation; survivor A2-4+A2-5's two-part-test scoring was called this report's "real, distinct measurement" when the source labels it REASONED, not MEASURED — corrected; the distilled "rules for the mail" section lacked a DERIVED marker even though its own source material flags parts as non-verbatim readings — added. Round 13 (8 findings, all sync/labeling/quote-fidelity, no new factual reversals — flagged to Codex beforehand as the planned final round; addressed rather than closing the loop early): the session card's L02/L08 summary still framed both as a symmetric "did not survive" pair after the report's § 3 correction distinguished them — synced; three more owner quotes (2026-09-01 mandate, 2026-07-21 thesis/throughline) lacked OWNER labels — added, with citations where an in-repo file exists; 05's own executable-adjacent comment (directly above `dies()`) still said "same rule as Fleet A" after the meta.phases comment was already fixed — corrected to same shape, not same rule; the "+127 sections" regex-fix figure dropped its source's own "+1%" scale qualifier — restored; A1-6 conflated a full-161-card zero-empty-review-sections result with a disjoint 27-card ~1-in-9-empty-idea-slot sample as if one measurement at two sample sizes — separated per both lenses' corrected_claim; FD-01/L03's "substrate-gate now runs the suite" and "mining gen-1 complete green" were presented as verbatim PR #16 / heartbeat quotes when both verifiers say no file here reproduces either — relabeled as fleet-manager's own rendering, both places it appeared; FD-02's residue narrative said the R22 standing guard (`a76ada7`, 12:56Z) was appended *after* the API-verified flip, reversing the source document's own two timestamps (the API check is timestamped ~15:12Z, later than the guard) — corrected to state the ordering the source itself leaves unreconciled.
UNCONTRACTED: INSTRUMENT (above). The merge stage's claim/correction matching is a judgment call (topic + substance, not exact keyword match) — the dedupe pass and the completeness critic are the only checks on it; a false pairing is a design risk this sheet does not fixture-test beyond the 3-case aggregation kill/survive. The dedupe stage's own output cap (150 merged rows -> at most 45 returned) silently drops rows beyond it with no marker at all — distinct from and worse than the rows it explicitly marks "dropped"; logged now, not fixed (Codex review, fm #1010). ASYMMETRIC COVERAGE (Codex review round 3, fm #1010, not fixed): the survival rule requires BOTH lenses to populate already_covered_by before a row is killed as already-covered, but only the fit-lens prompt is instructed to hunt broadly for coverage — the holds-lens prompt only checks two specific named sources (traps.md, the fresh-start-redirect doc). A row like FD-02 can therefore have its fit-lens call it "already-known plumbing" (naming docs/playbook.md R22) while its holds-lens leaves the field empty (out of its narrower scope), and the row survives as a fresh finding despite one lens's own judgment that it is not. A real fix gives both lenses the same overlap-search scope, or aggregates a single authoritative coverage verdict instead of requiring agreement; not done tonight. DATE-SCOPE CREEP (Codex review round 6, fm #1010): the reader prompt never bounded CLAIMS to the EAP fortnight (through 2026-07-21) — verified directly against this run's own claim pool, 30 of 505 claims (5.9%) are dated 2026-07-22 or later (phone-controller, shiftlife and other post-close estate history), consuming capacity in the capped merge (45-row)/verify (30-row) stages. Reader prompt fixed for future runs (claims bounded to on/before 07-21; corrections stay unbounded, since a late-found correction is the pattern being measured). Checked directly, not assumed: all 3 of this run's actual survivors (FD-01, FD-02, FD-17) have claimed_when dates within the fortnight (07-09 through 07-14) — the pool dilution did not contaminate the survivor set, though it may have displaced an in-window candidate that never reached the ranking stage; unknowable without a re-run. CLAIM WINDOW NOW BOUNDS BOTH ENDS (Codex review round 7, fm #1010): the round-6 fix only added an upper bound (07-21); the committed run's own claim pool already had 3 pre-EAP June claims, so the same capacity-consumption risk existed at the lower end too — reader prompt now bounds 07-07 through 07-21. CRITIC LACKS THE FULL CLAIM POOL (Codex review round 7, not fixed): the critic is asked to find orphaned corrections the merge missed a matching claim for, but it only sees the claims attached to verified rows (up to 30), not the full 505-claim pool — it cannot actually check an orphan against a claim that was matched then discarded by the ranking cap, or against an unmatched claim never selected into any row. A real fix passes the full claimPool (or a matched/unmatched claim-id split) into the critic prompt; not done tonight, disclosed instead. ONE ROW PER CORRECTION, NOT PER MATCHED CLAIM (Codex review round 8, fm #1010, not fixed): the ROW schema holds exactly one claim/citation_claim pair, and the merge stage partitions by correction (150 rows + 182 orphans = 332 corrections exactly) — verified directly. If one later correction refutes several distinct earlier done-claims, only one gets a row; the others are invisible to this ledger even though they are exactly its subject. A real fix emits one row per claim-correction match (a correction could then produce several rows) or extends ROW to hold multiple matched claims explicitly; not attempted tonight — this run's 3 survivors are not known to be affected, but it was not checked.

## SIZE (shared demand test, both fleets run in this same container)

SIZE        : limit **2** MEASURED by demand test at 2026-09-01T21:38-21:44Z — 12 probes (`date -u; sleep 45; date -u`) dispatched at one instant via `parallel()`. Observed: only 2 ran concurrently at any moment, across 6 waves of 2; each new pair started 7.3-8.0s after the PRIOR pair's slots freed (wave gaps: 7.3s, 8.0s, 8.1s, 7.6s, 7.5s — consistently tracking slot-frees, not a fixed cadence), which is the slot-limited signature, not provisioning-limited. This is this container's own number (fleet-preflight § 7: "the number does not transfer") — it matches the 2026-08-29 retrospective's "a later container measured an effective limit of 2", not its "4" or the laptop pilot's "6".
FLOOR (Fleet A): 84 agents (39 read + 6 merge + 32 verify + 3 spine + 3 judge + 1 critic) × ~180s (pilot's own measured mean) ÷ 2 ≈ **2.1 h floor** — understated: phases are sequential barriers, not one flat pool, and the verify/spine/judge/critic agents run at `effort: 'high'`, which the pilot's 180s mean does not isolate.
FLOOR (Fleet B): 48 readers + ~7 merge (dedupe groups sized to the actual correction count, TBD post-pilot) + up to 60 verify (30 rows × 2 lenses) + 1 critic ≈ 90-115 agents at similar per-agent duration ÷ 2 ≈ **2.3-2.9 h floor**, before its own pilot's real mean is known.
DECISION: at limit 2, Fleet A alone is very likely to run past 04:00 Europe/Amsterdam (02:00Z) on its own — dispatched 21:38Z, ~4h22m remain to 02:00Z, Fleet A's floor alone is ~2.1h with material upside risk. Per the night brief's own SIZE rule, Fleet B is scoped DOWN BEFORE Fleet A is cut: Fleet B's pilot runs first (cheap, 3 readers), then Fleet B launches with its satellite-heavy lane (32 of 48 superbot units) sized to whatever wall-clock remains after Fleet A lands and is pushed — dropped first if time is short, fleet-manager lane (16 units) kept. Fleet C (the 19-repo closeout audit) is not attempted: at this floor it cannot fit before 05:00 Amsterdam even if Fleet A and B both land early.
