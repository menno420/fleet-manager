# CONTRACTS — EAP-mail evidence pass, pilot sheet (2026-09-01)

> **Status:** `reference` · the fleet-preflight sheet as filled for the 2026-09-01 pilot; a night run fills its own sheet beside this one.

# CONTRACTS sheet as filled for the 2026-09-01 pilot (fleet-preflight). A night run fills its own sheet beside this one; this one is the worked example. Scratchpad paths below were this session's; the script now takes them as args.

# CONTRACTS — EAP-mail evidence pass, 2026-09-01 (fleet-preflight sheet)

AGGREGATE   : dies_if (a.refuted || b.refuted) || (a.already_covered_by && b.already_covered_by) · unread fields 0 (REPORT_ONLY: what_i_opened, discrepancies, corrected_claim) · fixture kill 2/3, survive 1/3 (asserted in-script before any agent spawns) · refute authority 2/2 (both lenses told to refute; per-lens verdicts kept)
INSTRUMENT  : UNCONTRACTED — no regex or extractor; the readers are the instrument. Cover: the pilot's transcripts read whole (below), and every citation is FILE:LINE the verifier re-opens.
PILOT       : 3 readers (estate-agent-error-audit · kit-tree-truth-pass · the sent 07-16 mail) + 1 merge + 3×2 verifiers = 18 agents, 1.22 M tokens, 19.0 min, run wf_869e2a79-72c · all 3 reader outputs, the 3 candidates and all 6 verdicts read whole · changed: (a) reader output capped at 12 findings with 30/60-word fields — the pilot's two audit readers returned 18 and 30 findings at 34 KB each, which would crowd a 15-reader merge; (b) merge A split into three groups of five; (c) a WORDING DISCIPLINE block added to the merge rules, because all three candidates survived both lenses only after the verifiers rewrote them for the same four faults — population scope dropped, measured count fused with reasoned mechanism, a product-mechanism claim the estate never measured, and superlatives the source does not use; (d) nothing changed in the verifier prompts — both lenses opened the cited files (4 files each on average), produced corrected claims, and one caught that a source never states the "~1,000 agents" figure the owner uses (the data README does: 986 agents, 80 readers). Rule behaviour: 0 of 3 killed by the real verdicts; the rule's kill path is proven by fixtures only — recorded as a limit.
CORPUS      : fleet-manager@cb3fc9a — 20 synthesis/record documents ≈ 590 KB (docs/findings 2026-08-08 → 09-01, docs/traps.md, the E1 plan/draft/sweep/evidence pack, owner-direction and sitting records, intent.md, owner-reflection, eap-retrospective) · the raw catalogue: 284 candidate patterns (docs/findings/data/2026-08-29-agent-error-patterns.jsonl, 1.47 MB) sharded into 12 files sorted by repo spread, plus 20 repository censuses (395 KB) · prior mails from superbot@5e3a667 docs/eap: 7 files ≈ 160 KB (email-2 sent 07-12 · email-3 send-ready 07-13, sending unverified · email-4 sent 07-16 · gen1 wrap-up candidate 07-10 · 07-18 follow-up NOT sent · permission-classifier consolidated 07-16) + fleet-manager docs/anthropic-email-pack.md (sending unresolved). Not in the corpus: the 07-08 and 07-12 mail bodies as sent (gone from Gmail — MEASURED-PRIOR via the correspondence record), the 47 MB uncommitted harvest corpus (regenerable only).
RETAIN      : per finding — claim · exact numbers with method/date · FILE:LINE citation · certainty tag as the source states it · scope (eap/general) · novelty; per pattern selection — verbatim instance cites (repo · path:line) · repo_count · severity · already_covered_positive; per verdict — what_i_opened · discrepancies · corrected_claim. Follow-ups answerable from the output: "which finding goes where in the mail" (mail_role + spine.which_candidates_used) yes · "was this already argued, and where" (already_covered_by names the file) yes · "which candidates were dropped and why" (verified[].reason) yes.
BASE        : fleet-manager@cb3fc9a at 2026-09-01T20:43:59Z (.launch-sha.fleet-manager in the clone) · open PRs 0 · superbot@5e3a667 docs/eap fetched 2026-09-01T20:45Z · re-read cb3fc9a..origin/main before writing the record.
SIZE        : limit 6 MEASURED by demand test at 2026-09-01T20:46Z — 8 probes dispatched at one instant, each alive 45 s: six started within 4 s, the seventh and eighth started ~50 s later within 5 s of the first slots freeing (slot-limited, not provisioning-limited); matches min(16, 8 CPUs − 2) · full run ≈ 78 agents × ~180 s ÷ 6 ≈ 0.65 h floor.
EXTERNAL    : the owner reads the map and the spine proposals; no Codex round for this record (no PR). If the mail draft becomes a fleet-manager PR, one Codex round is budgeted there — the draft's own history is 5 of 5 rounds changing it.
UNCONTRACTED: INSTRUMENT (above, with its cover). Verification of pattern instances cited in other repositories opens those repositories at their current HEAD, not at the SHA the harvest read — a cite that no longer resolves is reported as "not re-verifiable", never as false.
