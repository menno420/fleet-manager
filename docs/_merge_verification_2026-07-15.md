# Merge-on-green verification probe (2026-07-15)

> **Status:** `historical`

This file is an inert, one-time probe created during a merge-on-green audit session to confirm that an ordinary (non-workflow-file) code/doc PR in this repo lands on green CI with zero human click, via the repo's existing `merge-on-green.yml` REST sweep; it can be deleted once verified.
