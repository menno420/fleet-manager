# The consolidation program — the working plan

> **Status:** `living-ledger` — **THE plan.** The owner works this program
> together with any future session, one step at a time. Update the step ledger
> and the NOW pointer as steps complete; nothing else about this file should
> churn. Supersedes [`2026-07-26-consolidation-plan-v2.md`](2026-07-26-consolidation-plan-v2.md)
> (kept as analysis); background: [`../fleet-account-2026-07-26.md`](../fleet-account-2026-07-26.md)
> (the full documentation-derived read-back, owner-reviewed 2026-07-26).

## 0 · The goal, in the owner's sentence

> "Create a plan from which I can work together with any future session to
> consolidate the repos in a structured way, one step at a time."

And the quality bar, also his: **make all the repos feel right** — a fresh
session orients correctly and fast (this program exists partly because one
session needed three tries), the documentation tells the truth, the websites
reflect reality, and CI is a few checks that mean something.

## 1 · Owner directives (standing — do not re-ask these)

Recorded from the owner live in the hub chat; each is a durable decision.

| # | Date | Directive |
|---|---|---|
| OD-1 | 07-26 | ~~`superbot-next` is the bot's future; live testing gates the cutover.~~ **SUPERSEDED 2026-08-21 by OD-16.** Old `superbot` remains frozen as the behavioral oracle; the former `superbot-next` production target/cutover ladder is historical, not executable. |
| OD-2 | 07-26 | `venture-lab` stays a live repo. |
| OD-3 | 07-26 | **Archive, never delete.** Nothing is deleted under this program. **AMENDED 2026-08-08 (owner, intent interview):** cleanup is now allowed and wanted. *"Stale docs, and eventually repos, could be deleted if they do not have any value. The goal is not a perfect archive, but rather an efficient workflow. Any doc or repo that has served its purpose and could not be of value anymore will only be noise and it's probably better to clean them up every once in a while."* The bar moves from **never** to **has it served its purpose and can it still be of value** — a judgement, so it is decided-and-flagged per repo/doc, not swept. Archive remains the default for anything still arguably useful; deletion needs a stated reason. |
| OD-4 | 07-26 | **`idea-engine` + `sim-lab` remain active** — standing assets for future projects (566 idea files; the 4-gate verification method). |
| OD-5 | 07-26 | **The claude.ai Projects are terminated (since 07-21).** May return ~August for general use — not a fact. Everything runs in **regular sessions**, possibly indefinitely. No return to EAP-scale parallelism — that scale existed to produce vendor feedback. |
| OD-6 | 07-26 | ~~**Pace: slow.**~~ **RESTATED 2026-08-08 (owner, intent interview) — "slow" was the wrong word for it.** *"That does not mean we should ever rush things, though it does also not mean we can't make progress. What I meant by it is that we should just focus on one thing at a time and do it properly from start to finish."* So the directive is **one thing at a time, finished properly** — not deliberate slowness, and not a reason to stop short of a finished job. Nothing needs profit now; incremental testing and reviews stand. |
| OD-7 | 07-26 | **Priorities: documentation first, websites second.** |
| OD-8 | 07-26 | **Websites:** execute the cutover — the new `websites`-repo services replace the old `superbot`-repo sites **under the old names**; then rework the sites that don't serve their purpose, one at a time. (The cutover plan exists and is prerequisite-cleared: websites `docs/plans/site-consolidation-cutover.md`.) |
| OD-9 | 07-26 | **CI: consolidate to a few checks — ideally ONE required check per repo** wherever possible. |
| OD-10 | 07-26 | **Ideas Lab is ON-DEMAND.** No standing loop. If work runs dry, use `idea-engine` to source the next feature; when building a new feature or improving an existing one, **run it through a dedicated `sim-lab` simulation** (the 4-gate method) before/while building. |
| OD-11 | 07-26 | **Venture: let it sit.** The live $29 listing stays as-is (owner: "I don't think anyone would ever buy it" — no kill-clock action, no delist, no publish wave). The owner will work the sellable-products angle himself, at his own pace, later. |
| OD-12 | 07-26 | **proxybench: no action required.** Origin recorded: built with a session mostly as a joke, in response to a cold sales email from a proxy vendor about venture-lab — it is the honest benchmarking harness for exactly that vendor's claims (success rate / geo / stickiness). Stays parked as-is; disposition is open and unimportant. |
| OD-13 | **08-08** | **Method and enforcement work comes before high-value product work.** Owner, intent interview: *"Before we can reliably focus on product work, we should make sure to further improve the methods and the rule enforcement. Product work is not explicitly blocked, but my advice is to first improve the workflow and further define the right mix of AI agents across different providers before we actually continue high value product work."* Two prerequisites, both named by him: **the methods/enforcement layer** (the roadmap's Phases 2–3) and **the multi-provider agent mix** ([`../intent.md`](../intent.md) § 7). Product work is not forbidden — it is deprioritised against these until they are further along. This is the standing answer to *"what should a session pick up"*. |
| OD-14 | **08-08** | **fleet-manager's intent is recorded, and it is the thing plans are checked against.** [`../intent.md`](../intent.md) — purpose, success criteria, non-goals, decision heuristics, the agent roster, and the growth rule (**records may grow, instructions may not**). Owner-stated content is labelled `OWNER` and is not revisable by a session; anything labelled `DERIVED` is. |
| OD-15 | **08-10** | **shiftlife is not active; spider-swing and the superbot repos are the important ones.** Owner, live, during the full-read audit session. This supersedes the D2 order’s `shiftlife` target (recorded at the NOW pointer below) and is the statement the audit found missing from the tree — every document agreed on shiftlife precisely because nothing recorded this. Concrete re-target: `OQ-FM-D2-TARGET`. |
| OD-16 | **08-21** | **The next Discord bot is server-first:** basic game-testing + general game-community operation, retaining the best of both bots and giving AI more freedom from the start. The [authoritative pre-repository plan](2026-08-21-game-community-bot/README.md) uses live `superbot` as behavior/UX oracle and `superbot-next` as architecture donor; its clean-repository recommendation remains owner-confirmed at GCB-1 before creation. This supersedes OD-1's `superbot-next` production target and the old R4/R7 execution route. |
| OD-17 | **08-22** | **Cleanup is the priority, and the bar is agent legibility — not tidiness.** Owner, live: *"we should really focus on cleaning up excessive repos and documentation where possible while still making it easy for an agent to understand work."* Both halves bind together: a cut that shrinks the estate but makes a session slower to orient has failed the directive. The tier vocabulary in [`../MAP.md`](../MAP.md) (CORE / TASK / RECORD) is the instrument — cut RECORD-tier bulk out of read paths, never CORE-tier detail. Paired with OD-18 below; this one says *why*, that one says *what the output is*. Both quoted verbatim with the rest of his 2026-08-22 direction at [`../findings/2026-08-22-owner-direction.md`](../findings/2026-08-22-owner-direction.md), which also carries the three asks from that session that are NOT directives and have no home yet. |
| OD-18 | **08-22** | **Every remaining repo gets a disposition, and the disposition has a THIRD axis the program never had.** Owner, live: *"review the remaining repos to finalize the plan, which set of repos to keep eventually and should they start fresh or just be reworked etc."* The existing R-track only ever asked keep-vs-archive. This adds, for each repo that is kept: **rework it, or start it fresh.** That is a real question here — the estate has already answered it once, in favour of fresh, for the Discord bot (OD-16), and the reasoning there (live coupling, accidental product scope, parity ≠ ported) is the precedent to test each repo against rather than a rule to apply. **Deliverable: a per-repo disposition table with a stated reason per row**, not a sorted list. **DELIVERED 2026-08-22:** [`2026-08-22-repo-dispositions.md`](2026-08-22-repo-dispositions.md) — keep 14 · archive 12 · delete 0; **13 of the 14 keeps are reworks** and the fourteenth (`superbot`) is neither — it stays frozen while its successor starts fresh — and the two genuine start-fresh cases (the bot's code, phone-controller's home) were both already decided elsewhere. It is a **recommendation**; R5 executes it after the owner's yes. His wording and the derivation are at [`../findings/2026-08-22-owner-direction.md`](../findings/2026-08-22-owner-direction.md) § 1. |

## 2 · Target picture — 7 sections

| Section | Repos | End state |
|---|---|---|
| ShiftLife | `shiftlife` | The consumer app. Healthy — untouched by this program. |
| SuperBot | live `superbot` frozen as behavior oracle · `superbot-next` parked as architecture donor · clean game-community bot repository planned after GCB-1 | One focused server/playtest bot; any live responsibility transfer is separately evidenced and owner-approved. |
| Phone Controller | `phone-controller` (graduated from product-forge) | Own repo, own APK releases. |
| Game Lab | `gba-homebrew` + `pokemon-mod-lab` | Two repos forever (copyright rail), one section. |
| Venture | `venture-lab` | Live; owner-paced publishing. |
| Ideas Lab | `idea-engine` + `sim-lab` | **Active, on-demand** (OD-4/OD-10); conformance + truth pass only, no fold. |
| Workshop | `substrate-kit` + `fleet-manager` + `websites` | The kit, the records, the owner's visible surfaces. |
| *(archive)* | codetool ×3 (opus4.8 stays unarchived — live mdverify URLs) · product-forge remainder · trading-strategy · old superbot (post-cutover) · `curious-research` stays as-is (owner's parked gift) · `proxybench` parked as-is (OD-12) | Read-only, zero attention cost. |

## 3 · The step ledger

**Rules of the ledger:** steps are small (one session each), sequential within
a track, and only ONE step is NOW at a time — the owner (or the session, if he
hasn't said) picks the next NOW from the top of any track. **Verify before
fold** — no repo is merged or archived before its conformance/truth pass. Every
completed step appends a row to §7 with its PR.

### ➡ NOW: **D2 — next repository awaits the owner's target (shiftlife SUPERSEDED)**

> **Target correction — OWNER, live, 2026-08-10 (recorded by the full-read audit
> session, fm #839/#840):** *shiftlife is **not** active; spider-swing and the
> superbot repos are the important ones.* The order below encodes 2026-07-26
> activity and predates spider-swing (created 2026-07-28), which appears in
> none of §2's target rows — so advancing this pointer could never reach the
> repo he actually works on every evening (see the E1 deferral note above).
> **Do not pick up shiftlife on this pointer.** Until he re-targets D2
> (`OQ-FM-D2-TARGET` in [`../owner-queue.md`](../owner-queue.md)), the standing
> answer to "what should a session pick up" is **OD-13**: the methods/enforcement
> layer ([roadmap](2026-08-08-agent-operating-environment-roadmap.md) Phases 2–3)
> and the multi-provider mix — not high-value product work.

The fleet-manager pass completed the first repository in D2's order. Apply the
same fresh-session test to whichever repository the owner names next: purpose,
live state and next step from no more than three files.

> **The candidate set is now much smaller — 2026-08-22, OD-18.** D2's order was
> written when the estate had 26 undifferentiated repositories. The
> [disposition table](2026-08-22-repo-dispositions.md) puts **7 active keeps**
> at the front of the queue (`fleet-manager` ✅ · `spider-swing` ·
> `couch-legend` · `websites` · `superbot` · `substrate-kit` ·
> `estate-backups`) — those are where a future session will actually work, which
> is what a truth pass buys.
>
> **Archive-bound does not mean skip.** Sort the remaining twelve by whether the
> repo stays a **reference**, not by its disposition: several are archived
> precisely *because* they keep being read — `superbot-next` as the architecture
> donor, `superbot-mineverse` as the SuperBot-World MASTER closeout, the three
> labs as install documentation — and a stale front door frozen read-only is
> worse than one that can still be fixed. Those want a front-door pass **before**
> R5, which is the same window the disposition table's § 4 already describes. The
> ones with no future reader (`proxybench`, `Substrate-kit-app`) need nothing.
>
> **This does not answer `OQ-FM-D2-TARGET`** — which repo is his call, and OD-13
> still orders methods ahead of product work regardless.

Evidence for the fleet-manager pass:
[`../findings/2026-08-10-fleet-manager-cold-read.md`](../findings/2026-08-10-fleet-manager-cold-read.md).

> **Before acting on this pointer, read
> [`../findings/2026-08-05-foundation-continuation.md`](../findings/2026-08-05-foundation-continuation.md)** —
> it revises the order of work (foundation before rebuild) and carries the
> certainty legend that governs every dated claim in this repo, including the
> ones on this page. The boot file has said so since 2026-08-06; this page did
> not, so a session arriving here by grep, route or handoff never learned it.

> **E1 remains OWNER-RESERVED and deliberately deferred — status 2026-08-01,
> from the
> owner live.** It is not stalled and it is not available to pick up. He has the
> prework and a plan for how he wants to write it, and has declined a drafted
> version twice: *"this is something that deserves an evening of my full
> attention and I won't rush it. I will probably finish it within the next few
> days."* A session must NOT draft, send, or restart this step. If you are
> looking for work, take the repository named by **D2's NOW pointer** instead.
>
> Why he has deferred it: every evening since 07-26 has gone to **spider-swing**
> — a repo created 2026-07-28 that is not in the fleet account, and the only
> asset in the estate with a live external signal (returning players, unprompted
> difficulty feedback). Deferring E1 for it is triage, not neglect.

### Track E — one-off, owner-paced

| Step | What | Done when |
|---|---|---|
| **E1** | Help the owner write and send the **final EAP review email**: fresh thread, short, Part 1 owner voice / Part 2 Claude, the numbered wish list + the good parts. Method + sources: [`2026-07-26-final-eap-email-plan.md`](2026-07-26-final-eap-email-plan.md). | The owner has sent it from a fresh compose; §7 row added; the unsent 07-18 draft + email-pack get a superseded note. |

### Track D — Documentation *(priority 1, OD-7)*

| Step | What | Done when |
|---|---|---|
| **D1** | Refresh `.claude/CLAUDE.md` (hub boot file) to the post-program era: sessions-not-seats, this program as the entry point, live-vs-historical map. | A fresh session orients from the boot file alone, without the 3-try failure. **← this PR** |
| **D2** | **The truth pass, one repo per session** — for each active repo: boot docs say what it is NOW; seat-era docs get an `historical` era-banner (never rewritten); its closeout + current-state are linked from the front door. Order: `fleet-manager` ✅ → **`shiftlife` NOW** → `superbot-next` → `websites` → `substrate-kit` → `idea-engine`/`sim-lab` → `venture-lab` → game repos → the archive-bound. | Per repo: the fresh-session test passes (a cold session states the repo's purpose, live state, and next step correctly from ≤3 files). |
| **D3** | Fill the empty journal guidebooks with the real recurring traps (fm's `.session-journal.md` is placeholder headings today). Seed list: exit-code-read-through-a-pipe; proxied-vs-direct GitHub paths; born-red flow; MCP PR-read staleness; badge vocabulary. | Guidebook sections non-empty in fm + each active repo, each trap citing its incident. |
| **D4** | ~~Retire the dead apparatus *records*~~ **— roster half DONE 2026-08-07.** The owner ruled *"retire the roster, I don't need it"*; both `roster-regen.yml` crons and `roster-freshness.yml`'s `pull_request` trigger are gone (`workflow_dispatch` kept, OD-3), `docs/roster.md` is era-bannered `historical`, PR #808 closed. **Still open:** the trigger-registry and prompt-registry docs, and the three generated `projects/curious-research/` prompt copies that its 2026-08-07 handover flagged. | Roster no longer regenerates against a seatless fleet ✅; the remaining seat-era docs say so at the top. |

### Track W — Websites *(priority 2, OD-8)*

| Step | What | Done when |
|---|---|---|
| **W1** | Execute the cutover, **review-site first** (lowest risk), per websites `docs/plans/site-consolidation-cutover.md`: repoint references → reclaim old names → retire the three `reliable-grace` surfaces (`review-…-f027`, `superbot-app`, `superbot-dashboard`). **HARD RAIL (operation-scoped, per the cutover plan's Step 3): never stop, scale, disconnect, or delete the `worker` service (the LIVE Discord bot) or the two Postgres DBs in that project.** A config-only change to `worker` under an explicit owner directive (e.g. the 2026-08-14 watch filter, fm #863) is outside the prohibited class. The old "keep reliable-grace URLs reachable for the vendor reference" constraint has lapsed (correspondence concluded 07-21; verified 07-26). | One estate; old names serve the new sites; bot + DBs untouched. |
| **W2** | Per-site purpose review, one site at a time (control-plane → botsite → dashboard → review): does each page show what it is, what it does, its most important features (the owner's clarity bar)? Rework what fails it. | Owner signs off per site. |
| **W3** | Make this program visible: a control-plane page that renders §7 progress + the NOW pointer, so the owner reviews by looking, not asking. | The page exists and is current. |

### Track R — Repos *(verification-led, slow)*

| Step | What | Done when |
|---|---|---|
| **R1** | Generalize shiftlife's `plan-conformance.md` into the conformance template (claims vs code; module + test per row; wrong row > tidy table). | Template in this repo; applied first to `superbot-games` (known drift: claims plugin-shipping, has no packaging). |
| **R2** | Graduate `phone-controller` to its own repo (subtree split, history + release workflow + signing secret carried; pointer left behind). | Clean clone builds a signed APK; CI green; product-forge remainder ready for archive queue. |
| **R3** ✅ **DONE 2026-08-22** | ~~Releases-before-archive: tag + Release `cfgdiff` v0.1.1 and `envdrift` v0.1.0/v0.2.0 (archiving freezes tag-push forever — this is the one time-ordered step).~~ **Executed — and the urgency in that strikethrough was FALSE, corrected 2026-08-22.** Archiving is **reversible**: GitHub's own documentation says *"You can also unarchive repositories that have been archived"*, with no stated time limit, condition, or cap on how often. So R3 was never a hard time-ordered gate — an archived repo can be unarchived, released, and re-archived. It remains worth doing first (it is a standing step, and the unarchive round-trip is friction on a decision the owner has not yet made), but **"the one time-ordered step" overstated it**, and this session inherited that phrasing from this row and amplified it to the owner before checking. **Executed.** `cfgdiff` v0.1.1 @ `0b1eb60` tagged, its own `release.yml` built and published the Release (2 assets); its `publish-pypi` job failed as its workflow comment predicts (no trusted publisher), which reds the run without touching the Release. `envdrift` had no release workflow at all, so one was added (fable5 #20, squash `29863b0`) carrying `workflow_dispatch` with a tag input — the two tags already existed, and deleting/re-pushing published refs to provoke CI is not a habit to teach a repo — then dispatched twice: v0.1.0 @ `73ef38d` and v0.2.0 @ `13a84e5`, both runs green, 2 assets each. **Route fact, MEASURED:** this session type gets a hard `403 Creating, editing, or deleting releases is not permitted for this session type` on the releases API by every direct path tried, while a repo's own Actions job with `contents: write` creates them fine — so the plan's *"create the Releases via the API"* is not the available route; a workflow is. Tag push itself works via the direct-PAT path with the proxy bypassed (the session git proxy refuses the tag ref). | ✅ All three labs released — mdverify v0.1.0/v0.2.0 (pre-existing), cfgdiff v0.1.1, envdrift v0.1.0/v0.2.0. **All three are now safe to archive.** |
| **R4** | ~~Fold bot games/sites/plugins into `superbot-next`.~~ **SUPERSEDED by OD-16 (2026-08-21).** Do not execute this fold as the successor route. The clean game-community plan's Phase 0 creates the canonical home; source harvesting is per pinned extraction ledger, and unrelated game/economy scope stays out. | The new repo exists only after GCB-1, owns the transplanted plan, and neither source repo was modified. |
| **R5** ✅ **DONE 2026-08-23** | Archive the emptied/parked repos per OD-3, one at a time, each verified migrated first. **EXECUTED for the nine ungated rows** (fm #912): `superbot-games` · `superbot-idle` · `superbot-mineverse` · `trading-strategy` · `codetool-lab-sonnet5` · `codetool-lab-fable5` · `codetool-lab-opus4.8` · `Substrate-kit-app` · `proxybench`. `MEASURED` by fresh live re-read after the run: **26 repositories, 9 archived, 0 deleted**; the three gated rows (`superbot-next` + `superbot-plugin-hello` on GCB-1, `product-forge` on R2) were not touched and are **still open R5 work** when their gates lift. The § 4 pre-archive writes all landed first and were verified live before any archive call. Three things the estate had never measured are now measured: the archive `PATCH` works agent-side (200, first ever in this estate); the documented `git+https://…` install still works against an archived repo (`cfgdiff 0.1.1`, real exit 0); and archiving does **not** remove a repo from `search/code` (292 hits before and after) — though that last probe exposed that **only 3 of 26 repos are in that index at all**, which invalidates the method behind an earlier recorded sweep (see [`../ESTATE.md`](../ESTATE.md)). Still open: whether archiving stops **scheduled Actions** — baseline and one-call check recorded in the disposition table § 4. | Active repo list ≈ the §2 table. |
| **R6** | Ideas-Lab truth pass (active, no fold): make the two-era reality legible from the front door (fleet-ideation corpus vs the math-verification loop); surface the 566-file idea corpus so the owner can actually browse it. | Owner can find and read the idea corpus without archaeology. |
| **R7** | ~~Cut over production to `superbot-next`.~~ **SUPERSEDED by OD-16 (2026-08-21).** Use the [game-community bot plan](2026-08-21-game-community-bot/README.md) Phases 0–8: isolated app/guild/DB, journey/effect evidence, canary, then a separate owner-approved responsibility/data cutover only if replacement is still desired. | No production target is inferred from parity; live `superbot` remains untouched until a later exact cutover decision. |

### Track C — CI *(OD-9; mostly falls out of R)*

| Step | What | Done when |
|---|---|---|
| **C1** | The model: **one required check per repo** — the `websites` pattern (everything runs inside `bootstrap.py`/one workflow behind a single required context). Apply to each active repo as its D2/R step touches it; never retrofit archive-bound repos. | Each active repo: 1 required check (2 max where an artifact build is genuinely separate). |
| **C2** | Delete the agent merge-plumbing class where sessions are attended (keep `merge-on-green`/enabler only where it still lands PRs — note: private repos can't use GitHub's native auto-merge on this plan, so shiftlife keeps its enabler). | No workflow exists whose only job was serving the terminated seats. |
| **C3** | Confirm the pre-close standing failsafe crons are actually disabled (page the trigger registry to exhaustion; wipe survivors — the 07-21 snapshot showed 10 standing crons enabled; unverified since). | Registry shows zero seat-era crons enabled. |

## 4 · How any session works this program

1. **Boot:** hard-sync main → read `.claude/CLAUDE.md` → this file → the NOW step.
2. **One step per session** unless the owner directs otherwise. Small PRs, kit
   discipline (born-red card, `check --strict` green, real exit codes — never
   `$?` after a pipe).
3. **Verify-first:** the live surface beats any doc; each fold is preceded by
   its conformance pass.
4. **Initiative + the flag rule (owner-stated):** take initiative in organizing
   and planning; **flag to the owner only the genuinely ambiguous forks** you
   cannot resolve from the record — add them to §6, don't block on them.
5. **Close:** update §7 + move the NOW pointer + session card. Leave the truth
   accurate.

## 5 · What this program is NOT

No deadlines. No revenue pressure. **No *undirected* deletions** — OD-3 as
amended 2026-08-08 allows cleanup of docs and repos that have served their
purpose, per item and with a stated reason; what stays forbidden is sweeping
deletion and deleting anything still arguably useful. No seat revival, no
mass parallelism (OD-5). No merging of the two GBA repos (copyright rail). No production-bot changes outside a later exact, owner-approved cutover under the 2026-08-21 game-community bot plan.

## 6 · Open forks for the owner (non-blocking — answer whenever)

*All three founding forks were answered 2026-07-26 and promoted to OD-10 /
OD-11 / OD-12 above. Sessions: add new genuinely-ambiguous forks here — one
line each, never blocking.*

*(none open)*

## 7 · Progress ledger (append-only)

| Date | Step | What landed | PR |
|---|---|---|---|
| 2026-07-26 | — | Program created; fleet account (read-back) reviewed by owner; OD-1..OD-9 recorded | fm #540/#541/#543/#545 + this |
| 2026-07-26 | D1 | Hub boot file refreshed to the post-program era | fm #547 |
| 2026-07-26 | — | §6 forks answered by owner → OD-10/11/12 recorded; Gmail layer read on owner direction (EAP thread · vendor thread), proxybench origin recorded | fm #548 |
| 2026-07-26 | — | **E1 planned + set as NOW** (owner: final EAP email is tomorrow's priority): plan, source map, seeded candidate list · verified the 07-18 follow-up draft was never sent (its findings are unused material) | fm #545 |
| 2026-08-05 | — | *(ledger gap noted, not reconstructed)* Eleven substantive PRs landed 08-01→08-05 outside the program tracks — the Vertex-first directive, two Gemini benchmarks, the Play submission requirements, the superbot-next live audit and the navigation-graph correction. They are spider-swing / bot-rebuild work, not program steps, so they get this one summary row rather than back-filled step rows. **NOW is unchanged: E1 stays owner-reserved; D2 is the available step.** | fm #742–#760 |
| 2026-08-05 | — | Three-repo state audit (fleet-manager · superbot · superbot-next) as the foundation for the server-first rebuild: superbot's reachability guard found and **run green at HEAD** (0 GAP), the "help renders transcribed text" reading overturned, four record claims corrected, `tools/gemini_delegate.py` given a Vertex path so delegated reads spend credit not card | fm #761/#763 |
| 2026-08-05 | **D2 (partial — fleet-manager)** | Hub read path repaired. It omitted `current-state.md` and `owner-reflection-2026-07-21.md` — the doc `current-state.md` introduces as *"read this if you read nothing else"* — so a session following the boot file exactly never met it. The path now opens with those two, adds `PROJECT-CLOSEOUT.md` §3, and declares itself **a floor, not a ceiling**. Root cause traced past the boot file to the `continuation-prompt` skill, which templated `READ FIRST` as *"the minimum to act"* with no exception for a comprehension mandate; skill gains §4b. **This was the D2 fresh-session bar failing on the hub's own front door.** The rest of D2 — every other repo — is untouched and still open. | fm #779 |
| 2026-08-06 | **D2 (partial — substrate-kit) + foundation** | Kit checker classification and a second boot-path repair. **Measured:** `check --strict` output was 87% (kit) / 90% (fm) non-exit-affecting advisory, both trees exiting 0, and every tag that fired was an ager or a false positive (13 stale-wall rows titled `'any'`; nine skill-grounds rows naming `READ FIRST` and a numpy expression as "commands"). All 29 advisory sites are now classified deterministic/heuristic in `guards.ADVISORY_CENSUS`, pinned by a parity meta-test, with the heuristic tail routed off the agent's channel (`--advisories` to read it, `--gate-preview` to size the promotion). Output 47→7 and 89→10 lines, exit codes unchanged. **substrate-kit had no working boot read path at all** — its router's step 1 named a `.claude/CLAUDE.md` the kit by design never installs, a dead pointer already fixed in the template on 07-12 and never re-rendered into the kit's own copy (recorded in the kit's own decisions ledger). fleet-manager's path omitted the foundation-continuation doc; added. **Deliberately not done:** promoting the deterministic checkers to exit-affecting — two trees is not evidence for ~22 adopters; `--gate-preview` makes that a sweep. | fm #789 · kit #577 |
| 2026-08-07 | **D4 (partial — roster) + curious-research handover** | **The owner retired the roster** (*"Yes retire the roster, I don't need it"*), and it needed retiring for a second reason nobody had noticed: the regen had **deadlocked** since 2026-08-06T08:02Z. It opened its PR with `github.token`, GitHub suppresses workflow runs for that actor, `substrate-gate` never reported, and `main` requires it — **18 consecutive failed runs**, PR #808 permanently unmergeable, a red `freshness` on every `claude/*` PR. The workflow's own header knew the token behaviour and compensated by parking the PR for *"the next manager wake"*; that wake was the fleet, which closed 07-21. Crons removed, `workflow_dispatch` kept (OD-3), `docs/roster.md` bannered `historical` with its purpose preserved, PR #808 closed, `OQ-FM-APPARATUS-SIZING` resolved, `OQ-FM-ROSTER-READ-PAT` mooted. **Same root cause, second repo:** `curious-research`'s site silently stopped publishing for exactly this reason — auto-merge landing PRs as `GITHUB_TOKEN` so `pages` never fired. Fixed there **without a new credential** (owner declined one): merges attributed to a real user are not suppressed, so the enabler now refuses to arm PRs touching `site/`/`guides/`. Also: boot-source finding (booting on a satellite repo loads 1 skill instead of 27, silently), three corrected ledger claims, `OQ-CR-SLICER-ANSWER` closed after 23 days, 11 research dossiers landed + made discoverable, 3 guides translated to Dutch, the `lasersnijden` tab filled, and an external-review prompt built on the measured "unframed reviewers endorse anything" finding. | fm #810/#811 + this · cr #67–#75 |
| 2026-08-08 | — *(index restructure, not a lettered step)* | **Layer 2 exists, one folder deep.** `docs/repos/spider-swing/` built as the reference shape — README (standalone entry + four thread blocks) · `capabilities.md` · `records.md` · `working-here.md`, with `current-state.md` and `goals.md` **deferred and the reasons recorded** rather than silently omitted. Deliberately stopped at one folder: the first instance of a repeated shape is the only cheap place to be wrong, and the owner has not yet seen it. Both owner-stated acceptance tests written down and run — test 1 PASS 14/14 from the folder alone with the repo unattached; test 2 PARTIAL, correct on the one folder that exists and honest about the 23 that do not. Retrieval closed the gap that lets Layer 1 stay light: `route_docs.py` now also runs on `UserPromptSubmit`, payload key **verified from the shipped binary** (top-level `prompt`, not `tool_input`) rather than assumed, opt-in per route so the 21 existing probe routes keep their blast radius; `install_root_hooks.py` registers both events, so the case-three rescue path no longer installs half the mechanism. Two Layer-1 gaps closed: a **27-skill roster** (`rationalize` and `scope-backlog-item` were in *no* index; `chase-references`/`prep-owner-steps` were indexed as living in superbot and are installed here), and `@codex` added to the boot file — 0 occurrences before, despite being a measured, load-bearing review path. This is D2-adjacent (per-repo truth, fresh-session test) but is not filed as D2: D2 is a truth pass over a repo's *own* docs, and nothing in spider-swing was touched. **NOW is unchanged: E1 stays owner-reserved.** | fm #818 |
| 2026-08-08 | — *(instruments, cont.)* | **The owner's root-cause ask, answered and mechanised.** After #818's shape work, the day's follow-ups (#819–#822 + this) revived the owner-review hook (it had never fired — google-auth absent, every skip now logged, free-key-first routing), built `read_before_write` (unread-file descriptions + closed vocabularies), planted `scripts/preflight.py` (ORDER-018 parity — CI's added-card lane now reds locally), and added `git_state_guard` (squash-stack, force-push tree check, dirty reset). The finding that organises it: [2026-08-08-why-rules-dont-bind.md](../findings/2026-08-08-why-rules-dont-bind.md) — 17 incidents, catchers owner 5 / hook 4 / gate 1 / CI 2 / self 2 / after-the-fact 2 / **docs 0**, so rules move from recall to injection. NOW unchanged: E1 owner-reserved; Layer 2 replication awaits owner sign-off on the spider-swing shape. | fm #819–#823 |
| 2026-08-08 | — *(correction to the row above)* | **"17 incidents" is wrong; the number is 16** — and the row's own catcher list proves it (5+4+1+2+2+2 = 16). The finding corrected itself the same day and this ledger did not follow, so the stale headline outlived it on a read-path page. Kept as a correction row rather than an edit, because §7 is append-only and a silently-fixed number teaches nothing: the defect class is a **gloss composed over a correct table instead of computed from it**, which is the same failure the finding catalogues as its own incident #24. | fm #826 |
| 2026-08-08 | — *(D2, partial — the orientation layer)* | **Retrieval and orientation repaired so the corpus can be retrieved from.** Boot file corrected at net-zero words (stale route count and roster clause; one-command verify truth; the Stop-hook answer protocol; a canonical-for line for the decision records; era flags on the floor list). `session-close` gained the Layer 2 handoff step that [`2026-08-08-fleet-manager-as-index.md`](2026-08-08-fleet-manager-as-index.md) § Maintenance decided and the skill never carried, plus the §7/NOW step and a live-venue rewrite. One prompt route added under a stated admission bar, replayed against 12 real owner utterances (0 false fires). Seat-era routers (`NEXT-TASKS`, `RESUME`, `reading-path`, `PROJECT-CLOSEOUT` §5, `AGENT_ORIENTATION`, `MISSION`) now say which era they describe. This is **D2's method applied to fleet-manager's own front door** — era-banner the seat-era, point the live surfaces at what is true now — so it is filed as partial D2. **NOW unchanged: E1 owner-reserved.** | fm #826 |
| 2026-08-08 | — *(intent layer, Phase 2 opened)* | **The owner's intent asked, recorded, and two standing directives changed by his answers.** A 21-question intent batch, all answered, landed as [`../intent.md`](../intent.md) — the first instance of the roadmap's § 4.6 invariant (one discoverable canonical intent source per active repo). Purpose, success criteria, non-goals, decision heuristics, and the **agent roster** (Claude everything + credentials · ChatGPT *Work* doing real implementation in spider-swing · Gemini/Grok review and planning · Codex PR review) — none of which any record here carried. **The measured result: 20 of the 21 questions were unanswered by the corpus** (the purpose question was partially answered — two records already called this repo *"hub + records custodian"*; the reader, the boundary and any success criterion were absent, and the filtering pass missed it — one miss out of 21 on one occasion — **a count, not a rate**; a single sample cannot support one, and the "1-in-21 false-negative rate" phrasing was retired 2026-08-09 as the fake precision roadmap § 4.4 rejects — corrected by Codex review on #827) — twelve OD rows, three `[D-NNNN]` entries and a PL register all record *what was decided* and nothing recorded what the repo is *for*. **Two answers amended directives rather than adding to them** — OD-3 (cleanup of spent docs and repos now wanted, per item, with a reason) and OD-6 (*"slow"* → **one thing at a time, finished properly**) — so the interview's output is not additive-only, and Phase 2's procedure now names a reconciliation step (roadmap § 4.8). New **OD-13** (methods + provider mix precede high-value product work — the standing answer to *"what should a session pick up"*) and **OD-14** (intent is what a plan is checked against). Also corrected: *"his attention is the scarcest resource"* implied minimising asks; he wants **ask immediately, keep working, stop only when genuinely blocked**. **NOW is unchanged: E1 stays owner-reserved.** | fm #827 |
| 2026-08-08 | — *(follow-up to the row above)* | **The Layer-2 ratification his answers implied, plus the close-sequence trap that delayed it.** Three surfaces still read *"awaiting owner sign-off on the shape"* after he signed off (coverage table · `working-here.md`'s PROPOSAL header · the earned-files row); all three now record the ratification, and coverage becomes **four Tier-1 folders cleared to build, the rest on demand** rather than 24 pre-stubs. The delay is the part worth carrying: `merge-on-green` merged fm #827 **22 seconds after** a Codex review was requested on it — not an early merge by a session, but a **flip-before-review sequence error**, because the born-red hold is the only thing keeping the automatic lander off a PR and the flip is therefore the irreversible act. `session-close` gains step **6c** (everything owed to the PR happens before the flip) and step 7 now ends *"after the flip, treat the PR as gone."* Also re-earned: the PR API read was stale in exactly the window that would have hidden this, and `git ls-remote` was what surfaced it. | fm #828 |
| 2026-08-09 | — *(OD-13, the intent layer — Phase 2's first slice)* | **The intent map exists, runs inside `intake`, and has been walked through ten owner asks as the record preserved them.** Built on `intake` rather than as a new skill, because it already owned this moment and is already in the boot file's task→skill table — so the mechanism inherits a working trigger instead of needing a new prompt route, the class the design explicitly rejects. Its *"FULLER PICTURE (short prose)"* is **gone**: that one paragraph fused what the owner said, what the repo decided and what the agent inferred into three claims that read alike, which is the failure roadmap § 4.1 names. Replaced by EXPLICIT / ESTABLISHED / DERIVED / OPEN kept separate plus GOAL / NON-GOALS / SUCCESS, with ESTABLISHED as a **retrieval** step (eye-filtering measured at one miss in 21), the LOW/MEDIUM/HIGH classes, a categorical `INTENT STATUS` with no numeric score and **no question budget**, and the ephemeral-vs-durable split so maps do not silt up `intent.md`. Walkthrough over ten owner asks **as the record preserved them — none raw**, nine being agent-quoted and agent-segmented fragments and the tenth a correction: [`../findings/2026-08-09-intent-map-replay.md`](../findings/2026-08-09-intent-map-replay.md) — **0 clean catches · 1 partial · 1 correction-handled · 1 HIGH surfaced · 7 correct silences · 0 false alarms**, nothing made worse. **It is the author's walkthrough, not § 4.8's test** — that prescribes a *fresh* agent scorer and **remains outstanding** (Codex, #830: disclosing the bias does not satisfy the protocol). **The partial is the finding:** in the one ask that went wrong, intent was already understood correctly and still lost to an imperative contradicting the goal above it — *an intent map does not protect intent downstream of itself*, which sharpens § 5.5 into checking the produced artifact rather than the planner. Honest nulls kept in front: retrospective scoring by the procedure's own author, n=10, the HIGH branch exercised by exactly **one** case, and **the most informative case unrunnable** — the owner message that produced `Pace: slow.` was never preserved, so the compression destroyed the evidence needed to audit the compression. **Also this PR — the audit he asked for before continuing:** the layer-2 error ledger rendered six of eleven rows as literal pipe text and its class claim was wrong three ways (now six of eleven, `REASONED`, residuals named); `(this PR)` resolved to real numbers in five places including two rows here; a *"1 in 21 rate"* from one sample restated as a count; § 4.9 written to give the *"Layer-2 card marker"* open item the referent two handoffs assumed it had. **One mechanism landed:** prompt-route admission bar rule 4 is now enforced by `check_doc_routes.py` after a Bash command was measured consuming the `repo-spider-swing` route mid-audit; the route is split so the dedup counters are independent. **Owner ask answered:** why the 27 skills are invisible to claude.ai — two registries, `MEASURED` on the container, three routes recorded with the blocker (15 of 27 descriptions exceed the 200-char upload cap). **Left for him:** the kit-upgrade sequencing (vendored `KIT_VERSION` is **1.20.1** and carries **no** `ADVISORY_CENSUS` in any vendored Python — which is an absence measured **here** and settles only that this copy lacks it. **Whether any released kit carries `ADVISORY_CENSUS` in executable code was NOT verified** — substrate-kit was never attached, and the belief that it exists rests on [`../findings/2026-08-06-checker-classification.md`](../findings/2026-08-06-checker-classification.md) describing *that* repo's tree, not on reading it. So the upgrade's payload is `UNVERIFIED`, not established); whether § 4.9's presence check is adopted (**recommendation revised to hold** — 4 of 4 adoption, zero observed failures). **Two decided live the same day:** kit sequencing → **A, upgrade first** (so the P1 below bites immediately), and skills → **the Claude account, route 1, not the plugin** — *"the goal should be to get the skills in the claude account"*, not urgent. **P1 from Codex round 2:** `intake` is kit-named, and the documented copy loop reverts kit-named skills from `.substrate/skills/`, whose staged copy still holds the superseded body — **the upgrade silently reverts Phase 2 unless `intake` is re-applied afterwards**, now recorded in `docs/SKILLS-local.md`'s ⚠ re-apply table beside `session-close`. **NOW unchanged: E1 stays owner-reserved.** | fm #830 |
| 2026-08-09 | — *(OD-13, follow-on — the owner's ask)* | **The error ledger turned into mechanisms, and the estate's first `PostToolUse` hook.** He read fm #830's thirteen-error ledger, drew the conclusion the data supports — *"nearly all of them have been caught without my attention, so this is already a good example of the system working"* — and asked which errors a hook or skill could have caught **at the moment**, with the ones that already did as the template. All thirteen scored in [`../findings/2026-08-09-error-to-mechanism.md`](../findings/2026-08-09-error-to-mechanism.md): **4 already watched · 2 built · 3 skill steps · 2 buildable and declined · 1 no moment · 1 open design question** — 8 of 13 have a machine-decidable moment. **The templates share one property: none depends on the author noticing** — an exact-match that cannot accept a paraphrase, a hook that reads the *reply*, a reviewer that reads the *tree*. Everything that failed required remembering something, including a ⚠ warning sixty lines from the edit it governed. **Built:** `.claude/hooks/change_guard.py` — kit-named skill amendments (which the upgrade reverts, across **14** skills), broken markdown tables, verbatim un-propagated edits — on `PreToolUse` **and `PostToolUse`, an event this estate had never used**, because propagation is only knowable after the edit lands. Plus `capability-probe` step **3b**, the positive control before recording an absence, promoted from a trap that did not bind as prose. **The honest half:** check C **fails the case it was built for** (both real propagation failures were paraphrases; replayed from git, both silent), and **Codex found 14 code defects across two rounds** in checks their author had called two-directionally tested — that testing covered each defect and its absence and none of the traffic the hook sits in. The durable answer is `tools/test_change_guard.py`, 13 cases built from the boring ones. **Left for the owner, unchanged:** the kit upgrade is next by his decision A, and check A now warns whoever runs it that `intake` must be re-applied. **NOW unchanged: E1 stays owner-reserved.** | fm #831 |
| 2026-08-09 | — *(OD-13, the kit upgrade — and the E1 evidence base)* | **The upgrade's premise was false, and checking it took one clone.** The plan was *"find the released kit carrying `ADVISORY_CENSUS` and upgrade to it"*; fm #830 labelled that half `UNVERIFIED` and #831 carried it forward. **No released version carries it** — it landed on substrate-kit `main` 2026-08-06, sixteen days after the last release, under `## [Unreleased]`. Settled three ways, each with a positive control: the published v1.20.1/v1.20.2 `bootstrap.py` assets contain **0** occurrences (32 `KIT_VERSION` hits as control); `git tag --contains b505d13` is **empty** over a tag set complete by enumeration (26 local = 26 from the tags API — a completeness check added only after Codex pointed out that `--contains` reads local tags and this clone began `--depth 1`); and the changelog entry sits above `## [1.20.2]`. **The asset leg is the deciding one and a changelog read would have skipped it** — adopters vendor the published file, so what it contains *is* the payload. Owner ruled **"both, in order"**: **v1.20.2 landed here** (sha256 agreed four ways; rollback banked), v1.21.0 cut in a dedicated session that owns a *release* — outward-facing, twelve adopters, and a batch promoting six checkers to exit-affecting, so it should re-run `--gate-preview` across the adopter trees rather than trust this repo's measured zero. **The upgrade nearly took the gate's teeth with it:** regenerating the kit-owned `substrate-gate.yml` dropped the host-added `repo checkers` step — 0 checker references against 3 in the banked pre-regen — which would have left the gate running and green while running neither checker, one layer up from the *"self-enforced meant unenforced"* failure that wired it. The kit's carve-out scanner **caught it unprompted**, named the step and banked the file; re-applied in place (not in a separate workflow as the kit advises) because `substrate-gate` is the REQUIRED check and a separate one would not block until the ruleset said so — a policy change not asked for. **Verified running in CI**, step 8 success. Cost stated: the next upgrade drops it again. **`intake` survived the upgrade** — byte-identical — but the destroying step is **named, not absent**, and it is not a kit command at all: **no kit command ever writes `.claude/skills/`** (`cmd_skills`' docstring, `bootstrap.py:24405-24412`: the kit *"never writes a live `.claude/` tree"*; recorded as a measured capability at `docs/CAPABILITIES.md:635-648`). The installer is a **hand-run `cp` loop** at `docs/SKILLS-local.md:95-97`, which both docs instruct sessions to re-run after an upgrade. This upgrade staged **14** skills including `intake`, refreshing the superseded 3,745-byte body against the live 8,929-byte one — so the hazard is **live and its trigger is a documented instruction**, not mis-located; check A and the ⚠ re-apply table stand, and only the attribution moves. `SKILLS-local.md:114-115`'s `⚠⚠` that this *"bites on the very next session"* was right in substance and wrong only in which command fires it. **This row took three drafts:** *"staged six, copied none"* (read off the tail of the upgrade output and stated as a count over the whole set — error #17, generalising over a set never enumerated), then `skills --build` named as installer (contradicted by its own docstring), then the measured answer. Both bad drafts answered a mechanism question from the nearest artifact rather than the authoritative one, while `CAPABILITIES.md` held the dated, evidenced answer throughout. **E1's evidence base is now committed** ([`../findings/2026-08-09-eap-correspondence-record.md`](../findings/2026-08-09-eap-correspondence-record.md), linked from [the E1 plan](2026-07-26-final-eap-email-plan.md) § 2): four direct questions **never answered** against a **205-character** reply, **two verbatim promises** of a final review now 19 days old, and **no vendor agenda ever arrived** — so the mail's content is entirely his. Of the relayed facts, three were wrong and **a fourth is uncorroborated, not disproved** (no owner-sent mail exists on 07-22/07-23, positive control passing — an absence in one lane only). His outcome axis (**FORGETTING**, **FALSE-DONE**) is net-new against § 4's thirteen platform asks. **Confidentiality ruled** (ledger entry + `owner-reflection` amended): the strict clause is that file's editorial choice, not an estate-wide bar. The lesson under it outlives it — **the born-red gate holds the merge and does nothing about disclosure**, so on a public repo that question is settled before the first push. **NOW unchanged: E1 stays owner-reserved.** | fm #833 |
| 2026-08-10 | **D2 — fleet-manager complete** | **The repository's own front door now tells the truth on a surface with no boot file.** Before the repair, README named retired fleet state as canonical, `current-state.md` opened on the seat-era apparatus, and the actual next action required a fourth-file hunt plus reconciliation against E1. After the repair, exactly [`../../README.md`](../../README.md), [`../current-state.md`](../current-state.md), and this program state the repo's purpose, live operating era and next actionable repository without inference. Fully dated snapshots carry `historical` banners; mixed-era documents name the live and historical portions instead of rewriting their record. The no-`AGENTS.md` evidence is preserved and the owner choice is queued. E1 remains owner-reserved; **D2 advances to shiftlife**. | fm #837 |
| 2026-08-12 | — *(OD-13, Phase 2 — § 4.8's producer half)* | **The producer half of the fresh-agent test every Phase 2 citation called outstanding has run; the fresh-scorer half stays open.** Five fresh agents produced intent maps over the committed ten-ask corpus against `git archive` snapshots pinned to the ask-time trees (`7fbc065`, `f53d7ea`); the running session scored them on a rubric committed before any output was read — and the recorded § 4.8 bar is a fresh agent that produces **and scores** (replay finding § 4), so the mechanism claim is earned producer-side only. Measured: **221/222 checked citations substance-correct (ESTABLISHED subset 187/188) · 0 invented OPEN entries · 0 silently resolved HIGHs · 0 false alarms · verdict PARTIAL** (one citation-overreach, one ESTABLISHED miscount, eleven exact-range attribution imprecisions; pre-registered bands). The headline divergence: the walkthrough's one HIGH (*"genuinely better built"*) **dissolved under fresh retrieval** — the ask-time tree already carried operational content for it (`2026-08-05-superbot-next-live-audit.md` § 4/§ 1/§ 4b), so the author under-retrieved and the HIGH-ask branch is now demonstrated by **zero** committed cases (a wider null, recorded). Both case-B agents independently converged on the OD-6 repair the estate actually shipped, sight unseen. Codex round 1: 6 findings, 5 conceded 1 partial — four of them errors in the *scorer's* first push, none in the agents' maps, which is the scorer-bias residue § 1.5 predicts. Residual defect class — imprecise cites — is mechanically checkable (the `gemini_delegate` pattern); flagged, deliberately not built. Full record + raw maps: [`../findings/2026-08-12-intent-map-fresh-agent-test.md`](../findings/2026-08-12-intent-map-fresh-agent-test.md). **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | fm #851 |
| 2026-08-13 | — *(OD-13 — substrate-kit v1.21.0 cut, published, adopted: the owner's "both, in order" completes)* | **The seven-defect worklist is closed upstream and fleet-manager runs the release that closes it.** Kit #581 (born-red, merged `0021adc`): defect 7 first — subordinators are clause boundaries, so `does not reproduce because <wall>` can no longer clear a genuine wall on the required gate — then 6 (quoted-wall mention region + quote-anchored predicate), 2 (occurrence-level attachment), 3 (`(?:re)?deploy`), 4 (fence/blockquote bridge stops), 1 (fence-scoped render exemption), 5 (SKILLS-index teaches staging-only + the copy loop). **The three fm #833 gate carve-outs ship in the generator** (claims-guard `env:` block · planted `repo checkers` extension running host-owned `scripts/repo_checks.sh` · absent-card sentinel on direct `bootstrap.py check` verify commands) — the re-apply tax is over, and SKILLS-local's generated-file table is emptied against the DIFFED regenerated workflow, not on faith. The capability-seed's three route-quirk walls are retracted upstream (+ the same wall in the `enforcement-required-unverified` NOTE and branch-sweep template). **Verification is layered and none of it is a changelog read**: corpus A/B over 93,940 md lines (0 newly-flagged live lines; old→clear rows are the repro's own documentation), a FRESH 12-adopter `--gate-preview` sweep with the new dist (six promoted sites: 0 findings on 12/12 trees; every would-red row on a deliberately unpromoted site), publish run `31699815412` with four-way sha256 `8807a00e…` (downloaded = release.json = .sha256 asset = committed dist @ bump SHA), and `tools/ab_kit_scan.py` against the PUBLISHED asset — all seven rows in the wanted direction (the harness itself gained a context-aware defect-5 claim counter after its raw substring count stopped discriminating, plus a `[^.]`-gap fix its own positive control caught). Codex: two rounds on kit #581 — R1 6 findings (4 conceded+fixed, 2 partial with evidence), R2 3 findings (all conceded+fixed; final commit dispositioned under the two-round cap, stated not inferred). **The intake graduation (roadmap § 7) is DEFERRED from this release** — a defect-fix cut reaching twelve adopters should not also carry a feature graduation, and fm #852 left the checker-side needle rule unpinned; its own session, later. The wider ~11-adopter wave is owner-called, per-repo. **Codex then read the vendored v1.21.0 in the adoption diff and returned five P2s** (three pre-existing on kit main, two the new fixes' own price) — none patched locally per the fm #833 doctrine, all recorded with provenance and fix order as the next kit worklist: [`../findings/2026-08-13-substrate-kit-v1210-followups.md`](../findings/2026-08-13-substrate-kit-v1210-followups.md). **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | kit #581 · fm #853 |
| 2026-08-13 | — *(OD-13, Phase 2 — § 4.8's fresh-scorer half; the bar closes)* | **Two blind scorers re-scored the five committed maps and the § 4.8 bar (produce AND score, both fresh) is met.** Isolation was probed, not assumed: the default subagent launch path was **measured leaking the prior verdict** (subagents receive a session-start snapshot of the boot file — a working-tree edit demonstrably cannot redact it), so the scorers ran as sandbox-rooted headless sessions probed clean of all fleet-manager context; inputs were the five raw maps, the producer asks, the §§ 1–2 rubric with three outcome-bearing redactions, the rebuilt pins, and the committed checker — never the adjudications or prior TSVs. Result: **PARTIAL / PARTIAL — the committed band confirmed 3/3 scorings**, with the § 4.8 failure surface scorer-independent (0 fabrications · 0 invented OPEN · 0 silent HIGHs · 0 false alarms) and the prior outcome-aware scoring bent in four named places (fragment-7 anchor under-scored 3/3; residual-flag claim overstated; one D1(b) zero too clean; the "worst defect" row over-graded — the pinned range carries the entry's principal clause) plus one missed count error; citation-imprecision counts are scorer-relative (4–11 across three scorings of the same maps). Both blind scorers independently re-derived the committed headline (the registered A-7 premise is contradicted by the pin), so the HIGH-branch null **stands, now triple-endorsed**. Bundled: intake's decided-items line (deferred in fm #851 § 4), the spider-swing OD-6→OD-3 mis-cite fix (agent B2's find, verified live), and the owner-directed permissions allowlist (his 2026-08-13 live instruction; automode off). Full comparison: [`../findings/2026-08-13-intent-map-fresh-scorer.md`](../findings/2026-08-13-intent-map-fresh-scorer.md). **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | fm #852 |
| 2026-08-13 | — *(OD-13 — v1.21.0 rollout **phase 2**: the owner's named first batch)* | **The owner called the wave's first batch live: BOTH superbot repos, superbot-next then superbot.** Recorded here because it existed only in chat. One `upgrade-distribution` run each, deliberately never batched — a trivial hop must not be coupled to a delicate one. **superbot-next** (sbn #606 @ `d5f66dc`): v1.20.2 → v1.21.0, sha256 agreeing **five** ways (asset · `release.json` · sidecar · kit's committed dist at `0021adc` · fleet-manager's copy), rollback banked and verified byte-identical to the pre-upgrade tree. Its enabler regen dropped four host customizations; the briefing described the allowlist as *widening* to `{claim/*, claude/*}` — measured, it **narrowed** six prefixes to one, and that advisory is **superbot's, not superbot-next's**. The dropped in-progress-card guard mattered: **`substrate-gate` is not a required check there**, so the born-red card holds nothing closed and that step was the only thing stopping an incomplete session self-merging. A replacement host workflow was built and **Codex refuted it 7/7** (it would have stranded every born-red PR unarmed), so the regen was **reverted** — zero arming-behaviour change — and the durable fix (make `substrate-gate` required) is flagged, blocked on a pre-existing `ast.Num`/Python-3.12 break that reds that gate. **superbot** (sb #2436 @ `6067b2d2`): adopt-shaped and neither documented path fits — `upgrade` refuses (no state), `adopt` over-corrects into 21 paths and a workflow regen; the working third path is **vendor the dist + bump the pin**, which runs `check --strict` in full. All **14 false-wall findings dispositioned docs-only** — 3 corrected in place, 11 under 10 reasoned allowlist entries (records of the wall's own *removal*, verbatim classifier quotes, and the estate's own anti-wall instruction). Codex: 4 findings, **conceded 4/4**, the load-bearing one being that two of my "corrections" imported *fleet-manager's* capability matrix into a repo whose canonical records disagree — **a correction unverifiable in this tree must narrow the overclaim, not swap in another unverified claim**, which binds the remaining ~9 adopters. Gate honestly **red at 2** there (`orientation-budget`, `enforcement-unwired`), flagged not suppressed. Aftermath: registry regenerated. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | sbn #606 · sb #2436 · this |
| 2026-08-13 | — *(OD-13 — v1.21.0 rollout **phase 3**: the phase-2 review settled MEASURED, then the owner's named five)* | **The review half first, and all three of phase 2's self-flagged claims settled by measurement** ([`../findings/2026-08-13-v1210-phase2-review.md`](../findings/2026-08-13-v1210-phase2-review.md)): the superbot-next `ast.Num` break is **MEASURED in the venue** — the gate failed 3/3 real runs on the PR head whose squash tree is hash-identical to the merged `main` (CPython 3.14.6, `test_parse_message_shapes`, 1 failed/3672 passed), and the missing merge run is **CONFIRMED** as GITHUB_TOKEN auto-merge suppression (push actor `github-actions[bot]`, 0 runs on the merge SHA; July's main runs were the owner's own web-UI merges) — required-check enforcement is unaffected, so the flagged owner decision is now costed; superbot's *"neither documented path fits"* **narrowed in place** — `init` (which both refusals recommended and nobody ran) succeeds minimally and unlocks `upgrade`, which then reproduces `adopt`'s over-correction (20 paths, `docs/decisions.md` beside `docs/decisions/`, enabler regen) and still vendors no dist, so vendor+pin stands on measured ground; the kit #583 currency citation (`:398-402`) verified right against the tree, and the KNOWN-FALSE registry cell **healed itself** (the stale unauthenticated raw expired) while the structural defect stays filed. **Then phase 3, owner-paced live: the five trivial v1.20.1 hops, one `upgrade-distribution` run each; trading-strategy skipped until its archive decision.** Four landed and TREE-verified at their mains — websites #499 @ `882378abd` (+ its own exact-pin test moved per its documented ritual) · venture-lab #289 @ `a7220b1d3` · idea-engine #899 @ `320acabd9` (host wake-preflight step re-applied into the regenerated gate, fm #833 precedent; enabler regen reverted; `ideas/shiftlife/` section created against the roster frozen at retirement) · superbot-mineverse #144 @ `9f8047f9e` (gate regen reverted for real on round 2 after the round-1 "revert" was measured a no-op — conceded in-thread with hash proof) — and **gba-homebrew #215 is complete but PARKED on an owner fork**: its required `NDS ROM build` breaks on every PR because the BlocksDS rolling repo rotated out the pinned `1.21.1-1` toolchain (bump vs re-host vs park — flagged in the PR body; enabler regen reverted there too, card guard preserved). **Eight Codex rounds across the five PRs returned 41 findings; six were adopter-side and fixed, one declined `[survived]` with grounds, one deferred with reason (mineverse's capability-seed fence), and the kit worklist grew from 5 to 22 rows** — among the new: four P1 rows — strength-advisories promotion `:18946`, qualified-reassertion false NEGATIVE `:5873`, claims-guard mixed-control hole `:135`, and the requirements-install template defect that bit live on mineverse. Registry regenerated (kit #584, stamp `19:23:46Z`): 8 current · 4 honestly stale. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | fm #855 · websites #499 · gba #215 (parked) · venture-lab #289 · idea-engine #899 · mineverse #144 · kit #584 |
| 2026-08-14 | — *(product work, owner-live directive — R2 prepped, not started)* | **phone-controller Slice 18 shipped (product-forge #49, v0.18.0) and the owner's repo question answered from his own record.** He asked live: new repo for the controller app, or continue in product-forge — and directed a customization slice. The destination was already his decision three ways (§2's Phone Controller row · **R2** · product-forge's owner-confirmed incubator mechanic), so the session answered **both, in order**: the slice landed in product-forge on the proven signed-release rails (per-widget behavior config · long-press alternates · fine position sliders · **backup/restore-everything** — built partly as R2 insurance), and the graduation stays R2's own future step. **R2's sharp edge measured:** the stable keystore lives ONLY in product-forge's repo secrets (set sealed-box 2026-07-24; values unreadable agent-side) — three carry options + a recommendation recorded in the new on-demand Layer-2 folder [`../repos/product-forge/README.md`](../repos/product-forge/README.md). **Codex reviewed pre-merge: 5 findings, 5 conceded + fixed** (first-ever review in that repo; same-basis latency 416 s request→review vs fm's 335 s — the session's interim "not installed" inference was wrong and is kept as a correction in the #49 card, with the label-park having held the PR unmerged through the wrong-belief window). OD-13 tension named, not silently resolved: his live directive picked product work for this session; the standing default is unchanged. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | pf #49 · this |
| 2026-08-14 | — *(OD-13 — phase 3 completes: gba unblocked, the owner's five are 5/5)* | **The gba park resolved by measurement, owner fork, and a three-second authorized bypass.** The toolchain re-pin PR (gba #216) ran the migration test the phase-3 card called for: the self-hosted mirror release (`ci-toolchain-mirror-1`, hash-pinned, round-trip verified) and all four game compiles pass under BlocksDS 1.22.3 — but the emulator harness caught **Underroot at exactly half rate** (counter slope 0.5, measured in CI and reproduced locally; Gloamline/Brineward shift by one boot frame), and the exact 1.21.1 bytes went from "unresolved" to **unrecoverable** (Wayback: no capture — closing the last source). No behaviour-preserving path could green the required `NDS ROM build`, so the fork went to the owner with three options; he chose the one-time bypass. Executed narrow and recorded on gba #215: `ROM builds` honored green on the flip head first, `NDS ROM build` lifted from the ruleset at 05:58:13Z, squash-merged `7a4977bb0`, original ruleset restored 05:58:16Z and verified from the effective-rules endpoint. gba tree-verified at `main` (dist `8807a00e…`, pin 1.21.0); registry regenerated (kit #585) — **9 current · 3 honestly stale** (superbot-games +DRIFT · trading-strategy, owner-skipped · pokemon-mod-lab). The 1.22.3 migration stays open on gba #216, carded and labeled, with the Underroot half-rate as its first question. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | gba #215 `7a4977bb0` · gba #216 (open) · kit #585 |
| 2026-08-16 | — *(W-track wrap-up: the owner's three answers executed)* | **The consolidation's last loose ends closed on the owner's live answers (A / Accept / "try minting a PAT").** `postgres-botsite`: dumped from a GitHub Actions runner over a temporary TCP proxy (this container cannot open non-443 TCP — new ledger wall with the Actions-venue workaround), **restore-verified in-run** (full `pg_restore` into a scratch postgres:16 + row-count diff), archived on the new PRIVATE `menno420/estate-backups` release `postgres-botsite-final-2026-08-16`, then proxy + service + one-shot secret deleted — `reliable-grace` = `Postgres` + `worker` exactly (re-read). **The DB was empty at dump time, all schemas — measured twice** (live count loop + the archived dump re-read: 0 CREATE/COPY statements); a never-written history is `REASONED`, not measured. Backup-artifact visibility: **owner ruled Accept**, recorded. PAT mint: **probed at the owner's direction — no API exists** (three endpoints 404 vs 200/201 positive controls, same token; ledger entry); `OQ-WEBSITES-PAT` **stays open** with the one-recipe-two-tiers UI path — owner mints, a session wires on paste. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | fm #867 |
| 2026-08-14 | **W1 — EXECUTED** *(owner go, live: "execute the recommended plan, retire the restarts etc and remove the duplicate websites in superbot")* | **The cutover ran end to end and the churn is stopped; every step verified live** ([execution record](../findings/2026-08-14-railway-websites-audit.md) § 7): the three `reliable-grace` duplicates **deleted** (`review-f027` → f027 404 / fc91 keep 200 · old botsite · old dashboard), the freed names **reclaimed per OD-8** — `superbot-app.up.railway.app` and `superbot-dashboard.up.railway.app` now serve the canonical `superbot-websites` services (titles verified) — and `reliable-grace` holds exactly `worker` + its two Postgres. **Churn**: worker got its watch filter (all build inputs; first live test the same hour — a workflows-only merge produced a `SKIPPED` worker deployment, no bot restart) and superbot #2446 retired the 2-hourly refresh schedule + moved backups daily → weekly (verified first: 171–180 MB gz/dump artifacts). **Egress root cause measured**: Railway `httpLogs` show Meta-range crawlers (57.141.2.x, spoofed UAs) hammering control-plane's ~620 KB seat-era `/orders` page with `robots.txt` 404ing everywhere — websites #501 adds Disallow-all robots.txt to the three ops surfaces (botsite stays crawlable). App Sleep enabled on review-fc91/dashboard/botsite. **Deliberately not executed**: `postgres-botsite` (hard-rail protected — `OQ-RG-POSTGRES-BOTSITE`, one-letter owner call) and `OQ-WEBSITES-PAT` (console-only mint). W2/W3 remain open; W1 is done. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | fm #863 · sb #2446 · websites #501 |
| 2026-08-14 | — *(owner-directed audit — W-track evidence)* | **The Railway estate audited end to end; the €30 bill attributed per service from the receipts + the usage API.** 4 projects / 14 services, all running 24/7, none using App Sleep; all nine web surfaces probed 200 live. The Aug 13 receipt is **$30.73, all usage** (memory $15.43 · network $11.74 ≈ 234 GB egress · vCPU $3.29) against $7.75 the cycle before — the first cycle with (nearly) the whole EAP-era estate at run-rate (11 of 14 services the full month, three joined mid-cycle, so the full-estate run-rate is slightly higher), not a one-off spike. The duplicates confirmed live and title-verified: review ×2 (identical page, same source repo), old botsite (`superbot-app`) and old dashboard (`superbot-dashboard`) vs their `websites`-repo replacements — exactly the cutover plan's retire set (W1/OD-8, prerequisites cleared; the f027 reachability constraint lapsed 07-21, verified 07-26). **New defect measured:** pushes to the frozen `superbot` repo rebuilt **and restarted the production bot 344 times in the billed cycle (~11×/day, ongoing — ~293 of them the 2-hourly `dashboard-data-refresh` commits)** — `worker` has `watchPatterns: []` while the old botsite's `botsite/**` filter correctly skips the same pushes; the fix is one workflow disable + one watch filter covering every build input (not source-only), both reversible. `postgres-botsite` verified (DB-host wiring) as serving only the old botsite — **one of the two DBs W1's hard rail protects**, functionally orphaned post-W1; disposition needs an explicit owner amendment, flagged not decided. Full packet with per-service attribution and the post-cutover estimate: [`../findings/2026-08-14-railway-websites-audit.md`](../findings/2026-08-14-railway-websites-audit.md); `OQ-RAILWAY-PROJECT-SPLIT` updated (canonical home already decided by websites #407). **No Railway mutation was made** — audit only; W1 execution stays owner-gated per the cutover plan. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | fm #861 |
| 2026-08-14 | — *(OD-13 — the phase-3/gba review settled MEASURED; `[D-0017]` captured; #216 CLOSED on the owner's principle)* | **The review half of the wave, run adversarially, and all four self-flagged items settled by measurement** ([`../findings/2026-08-14-v1210-phase3-review.md`](../findings/2026-08-14-v1210-phase3-review.md)): **mineverse regen residue — NONE** (the merged #144 diff touches no `.github/workflows/` path; both live workflows blob-identical to the pre-#144 parent, and the live gate read directly carries zero pytest/requirements references; `main` == the #144 squash, 0 after — the round-2 revert held); **the gba turn's other confident claims all re-measured true** (ruleset `['NDS ROM build','ROM builds']` active · #215 merged 05:58:15Z inside the 3-second window · mirror release 1 asset · branch retained · dist `8807a00e…`/pin 1.21.0 · and `rom-builds.yml` verified boot-running the NDS titles under py-desmume — the corrected claim, confirmed in the tree); **the Underroot half-rate keeps its scope** ("under this SDK in the emulator") in every committed record — hardware left open with the reason (no hardware venue; owner play test is the listed step); **idea-engine's gate MEASURED green at `main`** by the squash-tree method (#899 head tree `c3862e0a67` == `main` `320acabd9`; push-runs GITHUB_TOKEN-suppressed — another sighting), `ideas/shiftlife/` present, and the required-check-vs-retired-roster question stays **flagged, not decided**. Registry re-derived at HEAD (stamp `05:59:49Z`): 9 current · 3 stale, the superbot-games 3-file DRIFT re-measured at its source heartbeats and pokemon-mod-lab's private tree read over direct PAT. 24-repo sweep: 2 open `claude/*` fleet-wide (sim-lab #344 · trading-strategy #160, both July parks, put to the owner as land-or-close); one post-snapshot delta (superbot #2444, its own dashboard automation, 09:19:01Z). **The row above is corrected by events: gba #216 is CLOSED-unmerged (06:41:13Z), branch retained** — the owner's ruling that *nothing waits in an open PR* is now **`[D-0017]`** in [`../decisions.md`](../decisions.md), and the bypass card is annotated in place. Remaining adopters stay owner-paced (superbot-games · pokemon-mod-lab — posed, not assumed). **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | fm #858 |
| 2026-08-14 | — *(the owner's answers, landed same hour: rollout stays paced; both July parks merged; 0 open `claude/*` PRs account-wide)* | **He answered fm #858's two forks live: no adopter yet (the v1.21.0 rollout stays owner-paced — superbot-games and pokemon-mod-lab wait for his word), and land BOTH July parks.** sim-lab #344 merged @ `f54ec219` (required gate already green; `main` is the squash). trading-strategy #160: the three resident capability overclaims holding its required gate red since July narrowed **in place** — `current-state.md:389`'s review rhythm now names the owner's review pass *or a session he directs* (merging is normal agent work; the repo's no-self-armed-auto-merge policy survives untouched) · `CONSTITUTION.md:166`'s scheduler rule restated positively after "must not arm" re-fired the same v1.20.2 agents-negation pattern "do not arm" did · `review-queue.md:8`'s #37 row dates the July denial as the momentary venue state it was — then merged @ `6cf2e93` on its own green gate. Landing it created a fresh heartbeat DRIFT (self-report still v1.17.0): reconciled at the source per the registry protocol (ts #163 @ `b5eba03`) and the registry regenerated (kit #586) — trading-strategy reads **v1.20.2 three ways**, honestly stale vs v1.21.0 and still owner-skipped until its archive decision; superbot-games' 3-file DRIFT unchanged, its own upgrade session's item. **The account-wide open `claude/*` PR count is 0** — the state the no-open-PRs ruling names as normal, for the first time since the parks were created. *(Corrected same hour: the flat "0" was composed from the 09:2xZ sweep and was wrong at write time — fm #859, a parallel owner-live session's in-flight records PR, opened 10:18:59Z. The true statement: **0 parked**, 1 in flight by a live session — the open state the ruling licenses.)* Venue facts, not walls: the currency tool's proxied API probes 403 (direct egress cleared them) and its unauthenticated raw read served a stale pre-restamp heartbeat — the cached-vs-current defect already filed upstream (kit #583). `OQ-JULY-PARKED-PRS` resolved in place. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | sim-lab #344 `f54ec219` · ts #160 `6cf2e93` / #163 `b5eba03` · kit #586 · this |
| 2026-08-15 | — *(product work, owner-live directive — Slice 19)* | **phone-controller Slice 19 shipped: the PS2 (DualShock) preset the owner asked for** (*"Improve the controller so it has a ps2 preset"*). A starter template in the New-layout picker — glyph-colored **true-circle** △ ○ ✕ □ diamond (positional mapping per the enum's BTN_* comments), D-pad, both sticks center-low, all four shoulders digital (L2/R2 were already descriptor bits 8/9), chip-safe Select/Start — **no HID descriptor change, so v0.19.0 installs in place; L3/R3 stick-clicks deferred as scope** *(premise corrected in-PR, Codex round on fm #864: the live descriptor already declares 16 button bits with the enum at 11 — L3/R3 would be enum-only, no re-pair; the merged pf-side records still carry the old premise, flagged in Layer-2 for the next pf touch)*. Codex two rounds, **4 findings, 4 conceded + fixed pre-merge** — focus-chip collision · CIRCLE-renders-oval (fixed app-wide: ButtonStyler now inscribes a centered true circle; the one flagged behavior change for existing layouts, hit-areas untouched) · a D-pad/stick overlap (geometry re-cut, all 13 rects proven pairwise disjoint by a one-off session script — a proof of the committed numbers, not a committed test) · the overlay call site the "app-wide" claim had missed, which the estate's owner-review hook surfaced independently minutes before Codex did. pf #50 squash `3c462a6`, tag `phone-controller-v0.19.0`, release verification in the fm card. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | pf #50 `3c462a6` · this |
| 2026-08-16 | — *(product work, owner-live directive — Slice 20)* | **The PS2 pad reached the layout spinner — the owner's screenshot overruled Slice 19's template-only scope.** His feedback (*"It's not visible here. Can you make it more easily accessible"*) named the defect exactly: a preset living only in the New-layout picker does not exist where pads are chosen. Fix: **"PS2 pad" as a directly selectable spinner row** rendered straight from `CustomLayout.template()` (new position-free key class `t:<kind>` beside `b:`/`c:`; spliced after NDS; per-host layout memory traced through store → `selectionExists` guard → restore, all three sites read). Only PS2 earns a row — the other template kinds mirror built-ins or are Blank, and list-bloat was the complaint. Codex one round, **1 finding, conceded + fixed pre-merge**: the Settings-close refresh enumeration missed the new `t:` class — the #49 staleness class recurring at the same seam, which is why the Layer-2 thread now records "replace the enumeration with an unconditional refresh" as the next slice's first item rather than a chat intention. Pre-tag sweep check run both sides of the merge (`git log 3c462a6..main` = exactly the #51 squash). README state counter unstuck (said "Slices 1–18" since Slice 18). pf #51 squash `d5a412f`, tag `phone-controller-v0.20.0`; release verification in the fm card. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | pf #51 `d5a412f` · this |
| 2026-08-20 | — *(product work, owner directive 2026-08-16 — Slices 21+22)* | **The owner's keyboard + foldable ask, designed first and shipped as two finished slices (v0.21.0 + v0.22.0).** His verbatim ask (*"recognize keyboards etc … use the keyboard as input/controls or just as a way to write. There should also be some extra support for foldable phones etc. The next session should think about this and try to implement it as properly as possible"*) treated "think about this" as a design mandate: options · choice · reasons recorded on the pf Slice-21 card § D1–D6 BEFORE code. **Slice 21 (pf #52 squash `e69afe5`, tag `phone-controller-v0.21.0`)**: `dispatchKeyEvent` capture under an explicit policy (chosen over the volume-key `onKeyDown` seam — play-mode pad buttons are focusable, so nav keys focus-wander; dialogs self-exempt by owning their windows) · TYPE mode = live keycode→HID type-through (new pure-JVM hid-core `KeyEventMap`, both-side modifiers, media→consumer taps, everything within Report 2's 0x01..0x65) · PAD mode = key→action bindings through the one `resolveRaw` vocabulary (VoiceStore pattern; defaults behind a confirm) · ⌨ toggle exists exactly while a keyboard is attached (the Slice-20 invisibility lesson) · the `keyboard` configChange (hot-plug used to RECREATE and drop the live connection) · **the queued unconditional Settings-close refresh CONSUMED**. Codex two rounds, **6 findings: [conceded]×4 · [partial]×1 · [survived]×1** — the load-bearing concession became `HeldKeyLedger` (hid-core): (device,key)-keyed reference-counted holds, per-device release, stateless taps uncounted, 8 tests pinning both round-1 scenarios; the [survived] (cross-producer holds) is the app's standing last-writer-wins model, recorded as a candidate not smuggled in. **Slice 22 (pf #53 squash `7a53b26`, tag `phone-controller-v0.22.0`)**: `smallestScreenSize\|density` join `configChanges` — a fold mid-game RECREATED the activity and dropped the HID connection (the manifest's rotation rationale, third instance) — plus per-screen layout memory (sw600 bucket, rotation-invariant; cover-screen pad vs inner pad remembered automatically); AndroidX WindowManager postures REJECTED as the explicit non-bend of the Slice-3 choice. Codex R1 [conceded]×2 (onCreate bucket restore — fold state is not process state; pending flip through the editor), R2 clean. **Both releases verified in full**: APK+sha256 assets two-way (`feaf1d73…`, `0916a484…`), "signing: stable repo keystore" in both run logs, signer cert **byte-identical v0.20.0 ↔ v0.21.0 ↔ v0.22.0** (sha256 `7bda3340…`, v2 signing-block parse). One CI red this session, owned in the card: a future-dated heartbeat stamp (353 s ahead) red on pf #52's `check` — heartbeat-guard doing its job. Device-venue honesty: no Android hardware here, so capture semantics + both recreation fixes are REASONED with owner device steps on both pf cards. OD-13 tension named, not silently resolved: the prompt relays his live product directive; the standing default is unchanged. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | pf #52 `e69afe5` · pf #53 `7a53b26` · this |
| 2026-08-20 | — *(product work, owner-live directive — the Grok game adopted)* | **`couch-legend` created and the Grok one-prompt prototype graduated into an owned, tested, deployed repo.** His ask (orient · open `idle-stoner.grok.me` · improve it · new repo on his PAT · state the perceived goal back), sharpened live mid-session: *"Eventually I want this to be a fully working android game. What I need is a solid base for the mechanics. The main idea should be clearly mapped and decided."* Executed: repo created via the PAT path; the game **reconstructed from its deployed production bundles** (Grok App Builder exposes no source — bundles beautified and read in full, every table and formula transcribed; art/fonts carried over; tuning kept faithful, zero balance changes); mechanics decided in the repo's `docs/DESIGN.md` (resources · loops · exact formula shapes · content invariants · **Android path = Capacitor shell over the same build**, phone-controller release rails as the pattern, native rewrite and TWA rejected with reasons); additive improvements only (portable save codes, per-mood revelations, itemized offline report, prestige preview, tab affordability dots, keyboard + reduced-motion, PWA manifest/icons); a 32-test engine suite pins the math (`pnpm check` = the one CI check, not yet required — flagged). **Verified live**: ci + pages both green on the deploy SHA, page and every referenced asset probed 200 at <https://menno420.github.io/couch-legend/>, and the game played end-to-end in Chromium (hits, purchase, save, reload-resume, zero console errors; Chromium cannot reach external hosts from this container, so the deployed-artifact play test ran on the byte-identical local build — venue fact, not a wall). Layer-2 folder created on demand: [`../repos/couch-legend/README.md`](../repos/couch-legend/README.md). OD-13 tension named, not silently resolved: the owner's live product directive picked this work; the standing default is unchanged. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | fm #870 · couch-legend `142cd14` |
| 2026-08-20/21 | — *(product work, owner directive 2026-08-20 — couch-legend phase 2, the brief's planning/testing session)* | **The life-story stage system is decided and the balance simulator exists, is validated, and has already earned its keep: the prototype tuning is measured running away.** Executed per the committed brief (couch-legend `docs/planning/2026-08-20-life-story-direction.md`, simulator-before-content): DESIGN.md § 9 now carries the decided system — the `lifeHigh` story axis (Wake & Bake resets an afternoon, never the story; "One afternoon, forever" reconciled as *"No afternoon ever fails"*), 18 stages in three arcs with thresholds **fitted to the measured economy** (weed inside the first attended half hour; authored story closes ~12–15 days; sim-lab verdict **approve**), six numeric fairness rails, the endless-tail answer (the prestige loop itself, with the trade stated: post-knee cycles pay in story cadence, not compounding speed), and the per-stage visual plan (18 scenes, produced later via the image-prompt/asset-pipeline method, then the owner's ChatGPT-Work looks pass). The simulator consumes the game's own engine through a newly extracted pure action layer (no second implementation anywhere), is seeded/deterministic (dt error ≤ 0.8 % measured), and was **validated against two real hand-played Chromium sessions before any conclusion was drawn** — 180 snapshot comparisons, 0 replay misses, worst field error 0.24 %, a live Wake & Bake replayed exactly; the validation also caught the recorder's own index-drift defect, annotated in the committed fixtures with evidence. **The headline:** left alone 14 simulated days, the prototype loop reaches Clarity ~9×10⁷ and lifeHigh ~10¹⁷ — six orders past the deepest authored content, with a ~200× prestige-discipline cliff — mechanism structural (milestone-doubling feedback + linear clarity vs sqrt gain brake); a knee+cap tuning candidate tames it ~6 orders with the playtested first two hours measured invariant, proposed **but not adopted** (live game byte-untouched; adoption is the implementation session's one-line flip). Also found live: the Lore tab's "revelations survive Wake & Bake" is false today (keys on `peakHigh`) — recorded in DESIGN § 9.2 with the fix path. couch-legend PR #1 (design + sim + results + data, `pnpm check` 62/62 after the review rounds). OD-13 tension named, not silently resolved: the owner's phase-2 directive picked this product work; the standing default is unchanged. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | couch-legend #1 · this |
| 2026-08-21 | — *(correction row — the row above's session, after both merges)* | **Both PRs' "round 3 clean" readings were false; five findings had landed pre-merge and were read only from the queued notifications after close.** Mechanism: the review-freshness queries filtered `since=` timestamps set *after* the actual round-3 submit times (`01:05` vs `00:53:19Z`, `00:35` vs `00:32:51Z`), structurally excluding what they checked for. All five verified [conceded] and fixed same-session: couch-legend #2 — the minimum-start boundary corrected ("one hit is enough" was also false: 1–3 hits freeze a save forever, 4–9 open only the jobs half, 10 open the full game; engine-pinned) · a taken prestige now counts as an offered move in the dead/check-in classifiers · unrecovered rebuilds are closed and counted instead of silently overwritten (the survivorship bias sat in exactly the F6 statistic); fm #873 — the card's shipped-head SHA corrected to the merged `f8869c3` · the Layer-2 "permanent lore lines" line qualified against the card's own defect record · the full correction block on the card, with the trap sized for a mechanism (derive `since` from the review's own `submitted_at`, never the check's wall clock). Codex-cloud's fm #872 task comment verified sandbox-only (`d73e867` exists nowhere; the merge head was `de85e23`). **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | couch-legend #2 · fm #873 |
| 2026-08-21 | — *(second correction row — the follow-up round refuted two claims in the row above)* | **The correction itself needed correcting, twice, and the record says so.** (1) The row above's trap prescription — "derive `since` from the review's own `submitted_at`" — reproduces the very false-clean it names: inline comments are created while a review is pending, so their timestamps can precede submission, and the API's `since` filters on `updated_at` regardless; the robust form correlates by identity, `GET /pulls/{n}/reviews/{review_id}/comments`. (2) "`d73e867` exists nowhere" was object-absence reasoning, and its first replacement — locally-authored commits plus a SHA-identical merge head — still proved only *which tree* merged, not where its changes originated (the round after said so); the argument that stands compares content: the cloud comment's blob links resolve to this session's **own** born-red commit `e117177` (the branch tip its viewer could see — not cloud content, and still born-red, so the links don't show the "completed card" the comment describes), the cloud commit's subject and its described PR appear nowhere, and the merged records cite facts created ~23 minutes *after* the 00:05:28Z comment (`5edac9a`, 00:27:59Z), which its state could not contain — establishing that the merged tree is the session's own and the cloud snapshot did not merge *unchanged*, and no more than that: the cloud patch itself is unavailable to compare, so whether any individual sentence was copied in is not checkable from the record (the final round's narrowing, conceded). Also from that round: couch-legend #2's first fix **fabricated** "~91 % of eager cycles never recover" via its own ordering bug (cycles marked unrecovered *before* checking the pre-reset peak; 959/959 flagged cycles in the checked seed had in fact recovered) — ordering fixed, both datasets regenerated, F6 restated in the three-discipline form (patient lanes 0.90–0.93 inside the ≤ 0.95 rail, one unrecovered · eager 0.97–1.00 with zero unrecovered, structural · hoard-everything late 1.00 **outside** the rail, stated, owner-vetoable). And the process fact the row above could not yet see: fm #873 auto-merged on green a minute after creation — a correction PR edits an already-`complete` card, so no born-red hold exists — thirteen minutes before its requested review answered; the successor PR parks itself `do-not-automerge` (the sweep's designed hold) until its round answers. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | couch-legend #2 · this |
| 2026-08-20 | — *(W-track — the keep-bot-only worklist EXECUTED, slices 1–5)* | **The owner's "keep only bot things" direction ran end to end in one session; every mutation verified live** ([worklist](2026-08-20-railway-keep-bot-only-worklist.md) § 6 order; evidence: [audit § 8 addendum](../findings/2026-08-14-railway-websites-audit.md)). **Slice 1 — the crawler DoS is over**: state re-verified first (healthz 3×30 s timeouts reproduced; the crawl WORSE than the handoff — 2,001-request sample 100 % ending HTTP 499, app runtime-silent ~4 h), then websites #508 gated `/orders` 608 KB + `/orders.json` 775 KB + `/prompts` 513 KB IN PLACE behind the [D-0012] overlay — route-scoped, never an IP-range 403; Codex at the exact head, 0 findings; minutes post-deploy: **healthz 5×200 ≤ 0.62 s, anonymous `/orders` 42 B, owner Basic 200 at the byte-exact 620,461 B page**. **Slice 2 — mineverse OFF Railway** (volumes verified NONE → `serviceDelete` + `projectDelete` both `true`; re-read 3 projects / 9 services; URL 404; the repo verified untouched). **Slice 3 — the old Pages wall fell on probe**: `POST /pages` (build_type workflow) → **201 over the direct PAT** (2026-08-07's finding was workflow-token-scoped — ledger entry); websites #509 (merged `b596b70`) lands the exporter (35 routes offline, exit-1-on-partial) + deploy workflow + `review-bake` schedule retirement (websites' decisions ledger names the losses, incl. the `/ask/api` AI path); **#510 (merged `f0e5bd3`, 2026-08-21) completes the cutover** — every consumer repointed, retirement semantics through envdrift/envhub, Codex R1 8/8 + R2 5/5 [conceded] under the cap (R2's P1: the exporter double-prefixed the hardcoded self-link in all 30 pages — made idempotent, pinned) — **then `serviceDelete(review)` → `true` with the § 6.3 order held**: Pages re-verified serving the fixed export BEFORE the delete; after it, project re-read shows review absent (**estate: 3 projects / 8 services**), the old URL 404s in 0.5 s, and the gated `/owner/environments` renders the `+1 retired` chip with no lifecycle-drift warning. **Slice 4 — superbot #2450** retires both poller crons (~170 no-op runs/day; sb #2446 pattern; the enabler's instant auto-merge arm DISABLED so green CI can't race the exact-head review). **Slice 5 — the ~2 GB dump mystery is SOLVED, read-only**: a one-shot sealed-secret Actions job (fm #867 venue; secret PUT 201 → run → DELETE 204) measured the DB at **949 MB, 97.5 % of it three `btd6_*` ingestion tables** — **newest row stamped 27 min before the probe** (last observed activity), averaging one row per ~26 s over the ~86-day span (a count/endpoints average; steady-vs-burst unmeasured — Codex #871 R2 caught the original "around the clock" phrasing overstating it) — XP/server-logging (the § 5.2 suspects) are 64–80 kB each; prune + loop-cadence now one-letter asks (`OQ-BOT-DB-BTD6-PRUNE`); worker + Postgres hard rail untouched. **Open residue**: `OQ-RAILWAY-SHIFTLIFE-SCOPE` (shiftlife untouched), `OQ-WEBSITES-PAT` (mint), new `OQ-RG-ORPHAN-VOLUMES` (4 volumes / 1 Postgres — orphans survive serviceDelete). **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | websites #508 `74410ff` · #509 `b596b70` · #510 `f0e5bd3` · sb #2450 `5e3a667b` · estate-backups `c1439ab` · this |
| 2026-08-21 | — *(product infrastructure, owner directive — couch-legend seeded with the kit)* | **couch-legend is a substrate-kit adopter at v1.21.0, and the seed smuggled in zero game changes.** The owner's ask (*"properly seed the new couch life repo with the substrate kit, but verify first"*) ran verify-first both ways: no repo named couch-life exists (couch-legend, created 2026-08-20, is the referent — restated in-session for correction), and the tree at `4759e90` measured kit-less before anything was written. The dist's sha256 agreed **four ways before commit** (release asset = sidecar = release.json = fm's committed v1.21.0 dist, `8807a00e…`), then the kit's own `adopt --wire-enforcement` + 16 slots answered from measured facts + `render --live` (0 unfilled). Landed as couch-legend #5 (squash `1e8c685`, merged by the session — no enabler there, deliberately: enabler + branch-sweep stay STAGED, land-it-yourself stands) with `substrate-gate` wired as the SECOND required check beside `ci` (ruleset 21117825, verified from the effective-rules endpoint — the spider-swing precedent; the OD-9 one-check trade flagged on both cards). `pnpm check` green and untouched throughout; the live page re-deployed identical (200). **Codex: three rounds at exact heads, 18 findings (6 P1) — 18 [conceded] and fixed, 0 [survived]**; the two-re-review cap reached at round 3 per the skill, fixes landed with the trail as the record. The seed fed the kit worklist **rows 24–34** (+ row 14's FIRST LIVE BITE — the pytest step on a vitest-TS `tests/`, converted in-repo into a real dist-pin test — and rows 20/21/26/29 sightings/widenings): the adopt seed failing its own inbox grammar, the free-prose verify slot rendered as bash spans across two trees, reset-before-inspect boot ordering, the tracked session-anchor dirtying every boot, dual boot lists under `--include-claude`, the enabler promise in session-close, capability declarations narrower than steps, root-collision downloads, and the upgrade flow never installing the sidecar it verifies. Also this morning, verified from the tree: **the owner's ChatGPT-Work looks pass LANDED** (#3 the Lucid Chronicle contract — the looks CONTRACT'S name, not a product rename — + #4 the first three Arc-1 scene packages, dormant by design), so `OQ-CL-LOOKS-PASS` is resolved and **the Claude implementation session is UNBLOCKED** (balance doc § 7 + #4's handoff notes = the checklist); new `OQ-CL-CHATGPT-REPASTE` (one paste refresh). fm surfaces trued: Layer-2 threads re-cut, ESTATE row, routes + `lucid-chronicle` alias, ChatGPT-instructions fence, adopter-invisibility set widened (spider-swing + couch-legend measured roster-invisible). OD-13 tension named, not silently resolved: the owner's live seeding directive picked this work; the standing default is unchanged. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | couch-legend #5 `1e8c685` · fm #879 |
| 2026-08-22 | **R3** ✅ | **Releases-before-archive executed; all three code-tool labs are now archivable.** Owner directive, live: *"final verification and reviews of the repos, making sure the important ones are kept and improved/merged, and the others are either archived or deleted."* R3 is the **one time-ordered step** in this program — archiving freezes tag push and release creation permanently — so it ran first, before any disposition call. **`cfgdiff`** (codetool-lab-sonnet5): tag `v0.1.1` @ `0b1eb60` pushed; its own `release.yml` built and published the Release with 2 assets. Its `publish-pypi` job failed exactly as that workflow's own comment predicts (no trusted publisher registered) — `build-and-release: success` / `publish-pypi: failure`, so **the run reads red while the Release is intact**; recorded because a future session reading only the run conclusion would mis-file this as a failure. **`envdrift`** (codetool-lab-fable5) had **no release workflow at all**, so one was added (fable5 #20, 5/5 CI green, squash `29863b0`) with `workflow_dispatch` + a tag input alongside `push.tags` — both tags already existed, and deleting/re-pushing published refs to provoke CI is not a habit to teach a repository — then dispatched for `v0.1.0` @ `73ef38d` and `v0.2.0` @ `13a84e5`, **both runs green, 2 assets each**. **Two route facts MEASURED against the plan's own text.** (1) The R3 row said *"create the Releases via the API"*; every direct API path this session tried returns a hard `403 — Creating, editing, or deleting releases is not permitted for this session type`, while a repository's own Actions job with `contents: write` creates them without trouble. **The available route is a workflow, not the API** — which is why envdrift got one rather than a one-off call. (2) Tag push works via the **direct-PAT path with the proxy bypassed**; the session's own git proxy refuses the tag ref with `send-pack: unexpected disconnect`, four attempts, consistently — a path quirk with a working alternative, not a wall. **NOT recorded in `current-state.md`:** its boot-read set sits at exactly 7000/7000 words, so any "Recently shipped" row reds the gate until the budget question is settled (raised in fm #892). **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | sonnet5 tag + release · fable5 #20 `29863b0` · this |
| 2026-08-22 | — *(owner directive — the verification half of the repo pass)* | **A dead quality gate found and fixed on `websites`; eight of nine kept repos green.** The owner's *"final verification and reviews of the repos, making sure the important ones are kept and improved"* splits in two, and the archive half is owner-gated (`OQ-`-class letters), so the verification half ran: CI health on main across the nine repos he keeps. `websites` was red. **`quality-main-sweep` exists to backfill the `quality` check on main after bot merges suppress the push event** (recursion guard), and `gh workflow run quality.yml --ref main` was written **without `--repo`** in a job with no checkout, so it died on `failed to run git: fatal: not a git repository`. Fixed at websites #511, squash `d2bba01`, Codex 2 rounds. **The measurement, and the correction that produced it:** the first claim — *"7 of the last 7 runs failed, it has never dispatched"* — was read off 30 repository-wide runs and **Codex round 1 refuted it**; re-measured against the workflow's own list, **361 runs exist, and of the 100 most recent 71 succeeded / 29 failed**. Reading logs by branch is what made it useful: **1 of 1 sampled successes took `quality present … nothing to do`, and 4 of 4 sampled failures took the dispatch path and died there** — so in every sampled case it was green when idle and dead when it had work, 5 logs read of 100 runs examined. **Not verified:** the post-merge dispatch took the `quality pending` branch, so the repaired line is still unexercised; it proves itself on the next bot merge. **Separately, `smoke-crawl` is red and is NOT broken** — it correctly reports three dead links on the live control-plane (`/journal/product-forge` 404 · `/projects/_inventory` 404 · a `/fleet` link into the private `pokemon-mod-lab`, which this session independently re-fetched as **403**, not the 404 the crawler logged). Those are content calls, raised with the owner. **NOT recorded in `current-state.md`** — boot-read set still at 7000/7000, second session running. **NOW unchanged: E1 owner-reserved; D2's target awaits the owner (`OQ-FM-D2-TARGET`).** | websites #511 `d2bba01` · this |
| 2026-08-22 | **OD-18** ✅ *(the table; R5 still gated)* | **Every repository in the estate now has a stated disposition** — [`2026-08-22-repo-dispositions.md`](2026-08-22-repo-dispositions.md): **keep 14 · archive 12 · delete 0**, re-derived per repo from each one's own `current-state.md`/`PROJECT-CLOSEOUT.md` rather than inherited from the five-repo list on record. Live sweep first: **26 repositories, zero archived**, all 26 reconciling against `ESTATE.md`. **The start-fresh axis has one taker and it was already taken** — OD-16's grounds (live coupling, accidental product scope, parity that was never porting) belong to the bot, so 13 of the 14 keeps are reworks and the fourteenth (`superbot`) stays frozen with a fresh successor; the only other start-over is a fresh *home*, not fresh code (phone-controller via R2). **Two ESTATE.md claims tested and both moved:** the `Substrate-kit-app` dependency check that was withdrawn as too narrow has now run account-wide (5 hits, all in fleet-manager, none in the other 25), and `superbot-plugin-hello`'s *"never archive"* does not follow from its lockfile — the pin is a `manifest_hash`, read directly, and a hash does not fetch. **R3's lesson generalized:** writes must precede the archive, sourced to GitHub's own recommendation, which turns the mineverse trigger-deletion baton into a pre-archive item rather than a trap sealed into the family MASTER. Deletion recommended for nothing: OD-3's amended bar is a conjunction and every candidate still has value. **Not executed** — the list is the owner's. | fm #906 |
| 2026-08-22 | — *(OD-18 follow-on — the first pre-archive write, executed)* | **The sharpest item on the disposition table's pre-archive list is done, in the repo it belonged to.** `superbot-mineverse`'s coordinator baton told the next session to *rebind-then-delete* a trigger — the one call this estate forbids, because it raises an approval prompt in automode and stalls the session until the owner is physically back — and `MEASURED` 2026-08-22, the trigger it named no longer exists on the account. Corrected in place rather than rewritten (era-banner convention), header re-badged HISTORICAL: **superbot-mineverse #145, merged `fc7c349`**, verified live. **Bounded:** both siblings searched for the same instruction, zero hits — the family's only instance. This mattered because that document is the SuperBot-World fleet MASTER and the repo is archive-bound, so archiving it as-is would have sealed a forbidden instruction read-only into the document games and idle both route to. **Also corrected here:** the table's dependabot conclusion stated at a confidence its evidence did not carry — the file lists are measured, the deploy filter they were judged against is Railway service configuration this session never read (`watchPatterns`: zero hits in the repo), so the merge recommendation is withdrawn and the settling read named. | fm #907 |
| 2026-08-23 | **R5** ✅ | **The estate's first archives — nine repositories, and the step OD-3 described on 2026-07-26 finally ran.** Live sweep first, as the prompt required: **26 repositories, 0 archived** — R5 had never partially run, so no row needed reconciling. The owner's go-ahead (`OQ-ESTATE-ARCHIVE-LIST`) covers the **nine ungated rows**; the three gated on GCB-1/R2 were not touched, and nothing was deleted. **Writes first, in full:** every one of the nine got a README notice *and* an updated GitHub description, verified live before any archive call — the three labs saying the tool is FINISHED and UNMAINTAINED, `proxybench` #1 closed, and `Substrate-kit-app` given the only line in its tree that correctly says what it is (its README is `substrate-kit`'s verbatim). Four landed by PR with session cards and green `substrate-gate` (games #186, idle #177, mineverse #146, trading-strategy #164), three by PR, two by direct commit. **Then the archives**, each confirmed by a fresh live re-read rather than by the API's 200: **26 repositories, 9 archived, 0 deleted.** **Three capability facts this estate had never had:** the archive `PATCH` succeeds agent-side over direct egress; the documented `git+https://…` install survives archiving (`pip install` → real exit 0 → `cfgdiff 0.1.1`), upgrading a `REASONED` claim to `MEASURED`; and a write to an archived repo returns **403 _"Repository was archived so is read-only."_** while reads keep working. **And one finding worth more than the step:** archiving does *not* hide a repo from `search/code` (292 hits before and after) — but measuring that exposed that **only 3 of 26 repositories are in that index at all**, before any archiving. That invalidates the *method* behind the recorded `Substrate-kit-app` dependency sweep (a zero from an unindexed repo is indistinguishable from an absence); it did not change that row's disposition, which rests on value, but it bars the method from supporting the irreversible deletion call the table defers. Still open, with a baseline and a one-call check recorded so nobody stalls on it: whether archiving stops **scheduled Actions**. | fm #912 |
