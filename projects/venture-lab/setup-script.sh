#!/usr/bin/env bash
# RETIRED (ORDER 018 R4, 2026-07-11): this per-package probe variant is superseded
# by the ONE setup-script lineage in fleet-manager environments/ — paste
# environments/archetype-python-lab.sh (thin config over environments/setup-base.sh;
# knob table: environments/archetypes.md) into this lane's env instead.
# Kept as an exit-0 tombstone so any env still holding this file provisions safely.
echo "[setup] RETIRED probe variant — use fleet-manager environments/archetype-python-lab.sh (see environments/archetypes.md)"
exit 0
