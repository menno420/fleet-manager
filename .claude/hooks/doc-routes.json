{
  "_comment": [
    "Route table for .claude/hooks/route_docs.py — see .claude/hooks/README.md.",
    "A route fires when one of its `when` regexes matches the tool input, and",
    "surfaces `says` plus the `docs` paths as additionalContext. Once per route",
    "per session. Validated by tools/check_doc_routes.py, which requires that at",
    "least one `when` pattern actually occurs in at least one of the route's docs",
    "— so a doc that stops covering its trigger fails the check instead of",
    "quietly routing a session to the wrong page.",
    "Adding a route is cheap and reversible. The bar: a session that skipped this",
    "doc would probe blind and could conclude 'unavailable' about something that",
    "works.",
    "",
    "PROMPT ROUTES (\"tools\": [\"UserPromptSubmit\"]) fire on the owner's message",
    "instead of a tool call, and they need a STRICTER bar because conversational",
    "vocabulary has unbounded paraphrase where a hostname does not:",
    "  1. Name the wrong ACTION a session would take without the doc — not the",
    "     background it would lack. 'Adds useful context' is noise.",
    "  2. Distinctive multi-word patterns only. A single common word ('plan',",
    "     'next', 'review') fires at prompt 1 of nearly every session, and the",
    "     once-per-session dedup then BURNS the route before the moment it was",
    "     written for — false-fire converts into false-silence exactly when it",
    "     matters.",
    "  3. Inject the METHOD, never an interpretation of what the owner means.",
    "     'Resolve existing intent before planning' is a method; 'the owner wants",
    "     X' is a guess the session will act on.",
    "  4. UserPromptSubmit ONLY — do not copy a repo route's tool list. Listing",
    "     Bash/Read/Grep lets any command or grep pattern containing the trigger",
    "     word consume the route before a prompt ever does.",
    "     ENFORCED since 2026-08-09 by tools/check_doc_routes.py, after a grep",
    "     containing 'repo-spider-swing' was measured consuming that very route.",
    "     To cover both events, ship TWO routes with distinct ids — `<id>` for",
    "     the tool list and `<id>-prompt` for UserPromptSubmit — so the",
    "     once-per-session counters are independent. See repo-spider-swing.",
    "Recall is deliberately traded away for precision here: a route that misses a",
    "phrasing costs one un-hinted session, while a route that cries wolf costs the",
    "silence every other route depends on."
  ],
  "routes": [
    {
      "id": "recording-a-wall",
      "tools": [
        "Edit",
        "Write",
        "Bash"
      ],
      "when": [
        "·\\s*wall\\s*·",
        "\\b(is|are|was|were)\\s+not\\s+(available|supported|reachable|possible|enabled)",
        "\\bunavailable\\b",
        "\\bcannot be (used|called|reached|enabled)",
        "\\bnot possible\\b",
        "preview-gated",
        "\\bwalled\\b"
      ],
      "docs": [
        "docs/CAPABILITIES.md"
      ],
      "says": "STOP — you are about to write a limitation down. Two checks first, in this order. (1) DID THE OWNER SAY OTHERWISE? His statements about this estate are source truth, not a hypothesis your probe gets to overturn — a failed call means you used the wrong path, not that he was wrong. This exact failure is on the record: 2026-08-05 he wrote that the Interactions API 'works fully turn based and stored session history', a probe of the Vertex path returned RESOURCE_PROJECT_INVALID, and 'unavailable' went into the ledger anyway. It works, on the other surface, exactly as he said. (2) Has the estate already measured it? grep docs/providers/ and docs/conventions/ before recording anything. See THE DISCOVERY RULE step 0.",
      "authored_only": true,
      "repeat": true
    },
    {
      "id": "discovery-probe",
      "when": [
        "\\$discovery",
        "discovery\\.googleapis\\.com",
        "\\$rest\\b",
        "\\?fields=.*methods",
        "openapi\\.json",
        "/\\$metadata"
      ],
      "docs": [
        "docs/CAPABILITIES.md"
      ],
      "says": "You are about to infer an API's shape from its own spec. A discovery document that omits an endpoint is NOT evidence the endpoint is absent — measured 2026-08-05: generativelanguage's discovery doc (v1beta and v1alpha, both 200) has zero hits for `interactions`, and the endpoint works. Check the estate's own provider doc before concluding anything is unavailable, and see THE DISCOVERY RULE."
    },
    {
      "id": "adversarial-review",
      "when": [
        "adversarial[- ]review",
        "provenance-review",
        "provenance-mechanism",
        "owner stand-in",
        "ownerreview",
        "what made you draw",
        "review mandate"
      ],
      "docs": [
        "docs/findings/2026-08-06-provenance-mechanism-measured.md",
        "docs/conventions/adversarial-review.md"
      ],
      "says": "The review mechanics are MEASURED, not argued (4 controlled runs, 2026-08-06/07): an UNFRAMED reviewer endorses whatever it is shown — including a known-wrong design, whose specific defect it praised — so 'explain it to Gemini' verifies nothing by itself; the owner-stand-in system prompt is the load-bearing component (committed verbatim in the findings doc § 7). Stop is the only viable hook event — UserPromptSubmit fires before any claim exists to ask about. The two questions, in order: 'What made you draw that conclusion?' (expand), then 'Can you simplify the mistake?' (compress — only after the first surfaces something). Read the findings doc before touching substrate-kit#580 or building any review hook."
    },
    {
      "id": "gemini",
      "when": [
        ":generateContent",
        "GEMINI_API_KEY",
        "GEMINI_API_KEY_PAID",
        "GEMINI_VERTEX_SA",
        "aiplatform\\.googleapis\\.com",
        "deep[-_ ]?research",
        "gemini-[0-9]",
        "gemini_delegate",
        "generativelanguage\\.googleapis\\.com",
        "interactions:create"
      ],
      "docs": [
        "docs/conventions/vertex-first-for-gemini.md",
        "docs/providers/gemini.md"
      ],
      "says": "TWO identities, and the paid one bills differently per route — THE ROUTE DECIDES WHO PAYS, NOT THE KEY. GEMINI_API_KEY is free tier on AI Studio: costs nothing, capped ~20 req/day flagship Flash and 500/day Flash Lite, and it serves the Interactions API (POST /v1beta/interactions + previous_interaction_id, server-side history). The paid GCP project has two routes on the SAME billing account: via Vertex it draws the prepaid credit (no daily cliff, but NO server-side history, so conversations resend their whole transcript each turn), while GEMINI_API_KEY_PAID against generativelanguage bills the OWNER'S CARD — the credit excludes the AI Studio SKU and does not exclude Vertex. So: Vertex for volume, image and video; the FREE key for any AI Studio call including long multi-turn; the paid key only when Vertex has actually failed OR for DEEP RESEARCH, which is not on Vertex at all and not on the free tier either — that is the one documented exception, so use the paid key for it without hunting for a Vertex route that does not exist. Say so in the session card either way. Pull GEMINI_VERTEX_SA_JSON from Railway; grounding is `googleSearch` (camelCase) on Vertex. tools/gemini_delegate.py implements both."
    },
    {
      "id": "github-api",
      "when": [
        "api\\.github\\.com",
        "\\bgh\\s+api\\b",
        "GITHUB_PAT",
        "uploads\\.github\\.com"
      ],
      "docs": [
        "docs/CAPABILITIES.md"
      ],
      "says": "The token is a full, account-scoped PAT — admin plus push on every repo. Use direct egress: `curl --noproxy '*'`, or requests with trust_env=False and verify=/root/.ccr/ca-bundle.crt. The PROXIED REST path 403s; that is a path quirk, not a permission boundary, and not a wall. Merging PRs, deleting branches, editing rulesets, creating releases and tags are all normal agent work."
    },
    {
      "id": "railway",
      "when": [
        "backboard\\.railway\\.com",
        "RAILWAY_API_KEY",
        "\\brailway\\s+(variables|run|up|link|logs)\\b"
      ],
      "docs": [
        "docs/conventions/vertex-first-for-gemini.md"
      ],
      "says": "The Railway token is account-scoped and unrestricted. § 1 of this doc has the working GraphQL call for reading a service's environment variables (project / service / environment triple), which is how secrets that are not in your container get retrieved."
    },
    {
      "id": "provisioned-secrets",
      "when": [
        "DISCORD_TOKEN",
        "discord\\.com/api",
        "printenv\\s*\\|\\s*grep",
        "\\bBOT_TOKEN\\b"
      ],
      "docs": [
        "docs/CAPABILITIES.md"
      ],
      "says": "Secrets are provisioned in the environment — sessions routinely forget they exist and declare a task blocked. Check with `printenv | grep -iE 'token|key|railway|discord'`. Confirm PRESENCE only (names, never values) and use them via the env var. See § CAN — Use provisioned secrets."
    },
    {
      "id": "video-audio",
      "when": [
        "\\bffmpeg\\b",
        "\\bffprobe\\b",
        "\\.(mp4|webm|mov|mkv|mp3|wav|m4a|ogg)\\b"
      ],
      "docs": [
        "docs/conventions/reading-screen-recordings.md",
        "docs/CAPABILITIES.md"
      ],
      "says": "Video is readable — but sampling rate, not legibility, decides how much you actually read. Gemini samples at 1 fps by DEFAULT: measured on a 6.6s phone scroll, 5 of 6 consecutive-frame pairs shared NO content, and the reply still reads as a clean transcript. Set videoMetadata.fps to 16 on Vertex (0 gaps of 105 pairs; 8 fps still had 5) and ask the model to report its own coverage. Do NOT verify coverage by measuring displacement — it aliases on repetitive UI and fabricates motion. ffmpeg is not installed here; `pip install imageio-ffmpeg` supplies it without root."
    },
    {
      "id": "youtube",
      "when": [
        "youtube_transcript_api",
        "youtube\\.com/watch",
        "youtu\\.be/",
        "\\byt-dlp\\b"
      ],
      "docs": [
        "docs/CAPABILITIES.md"
      ],
      "says": "youtube_transcript_api is IP-blocked from datacenter addresses, so sessions conclude transcripts are impossible. They are not: `yt-dlp --skip-download --write-auto-sub <url>` works via its android-vr endpoint, then parse the .vtt. See § CAN — YouTube transcripts."
    },
    {
      "id": "shared-ai-chat",
      "when": [
        "chatgpt\\.com/share",
        "claude\\.ai/share",
        "g\\.co/gemini/share",
        "gemini\\.google\\.com/share",
        "read_shared_chat"
      ],
      "docs": [
        "docs/conventions/reading-shared-ai-chats.md"
      ],
      "says": "Shared-chat links do not render usefully through a plain fetch. tools/read_shared_chat.py handles the per-provider extraction; this doc records which providers work and what each returns."
    },
    {
      "id": "openai",
      "when": [
        "api\\.openai\\.com",
        "OPENAI_API_KEY",
        "\\bcodex\\b"
      ],
      "docs": [
        "docs/providers/chatgpt.md"
      ],
      "says": "Model family, the post-consolidation modes, and the Work / Codex cloud environment with its variables, secrets and network defaults are recorded here — including which surfaces have network during which phase."
    },
    {
      "id": "anthropic",
      "when": [
        "api\\.anthropic\\.com",
        "ANTHROPIC_API_KEY",
        "platform\\.claude\\.com"
      ],
      "docs": [
        "docs/providers/claude.md"
      ],
      "says": "Model IDs re-derived from Anthropic's own changelog, the surfaces, what a Code session can actually do here, and the estate-specific quirks that have previously produced false walls."
    },
    {
      "id": "deepseek",
      "when": [
        "api\\.deepseek\\.com",
        "DEEPSEEK_API_KEY",
        "deepseek-(chat|reasoner|v[0-9])"
      ],
      "docs": [
        "docs/providers/deepseek.md"
      ],
      "says": "The V4 pair, the thinking modes, the MIT open weights, and the pricing (including the peak-hour band) that makes this the cost floor of the provider set."
    },
    {
      "id": "grok",
      "when": [
        "api\\.x\\.ai",
        "XAI_API_KEY",
        "\\bgrok-[0-9]"
      ],
      "docs": [
        "docs/providers/grok.md"
      ],
      "says": "xAI's text models and reasoning knobs, the image/video/voice stack, X Search — and the silent-redirect retirement scheme, which is why a model ID that still answers is not proof the model you asked for ran."
    },
    {
      "id": "mistral",
      "when": [
        "api\\.mistral\\.ai",
        "MISTRAL_API_KEY",
        "mistral-(large|small|medium)"
      ],
      "docs": [
        "docs/providers/mistral.md"
      ],
      "says": "The per-model licence mix (Apache / MIT / Premier / NC) governs whether you may self-host a given model, plus the specialist bench — OCR, Voxtral audio."
    },
    {
      "id": "cohere",
      "when": [
        "api\\.cohere",
        "COHERE_API_KEY",
        "command-a"
      ],
      "docs": [
        "docs/providers/cohere.md"
      ],
      "says": "The retrieval specialist: Command A+ under Apache 2.0, the context-vs-output trade inside the Command line, and first-party Embed/Rerank."
    },
    {
      "id": "copilot",
      "when": [
        "\\bcopilot\\b",
        "githubcopilot\\.com"
      ],
      "docs": [
        "docs/providers/github-copilot.md"
      ],
      "says": "The provider this estate touches without choosing it. Covers the cloud agent's Actions environment — firewall behaviour and which secrets actually reach the agent — plus AI-credits billing."
    },
    {
      "id": "llama",
      "when": [
        "ai\\.meta\\.com",
        "\\bllama-?[34]\\b",
        "llama\\.cpp"
      ],
      "docs": [
        "docs/providers/meta-llama.md"
      ],
      "says": "The open-weights generations and their Community License terms, and the 2026 pivot to Muse that froze the open line at Llama 4."
    },
    {
      "id": "owner-drive",
      "when": [
        "drive\\.google\\.com",
        "drive\\.usercontent\\.google\\.com",
        "GOOGLE_DRIVE"
      ],
      "docs": [
        "docs/conventions/owner-drive-folder.md"
      ],
      "says": "The owner's shared Drive folder has a fixed convention for where things land and how they are named. Read it before writing anything there, so a delivery does not go somewhere he will not look."
    },
    {
      "id": "outbox",
      "when": [
        "\\boutbox\\b"
      ],
      "docs": [
        "docs/conventions/outbox-rollover.md"
      ],
      "says": "The outbox has a rollover convention — how it is emptied and what happens to items that were never collected. Follow it rather than inventing a scheme."
    },
    {
      "id": "empty-repo-first-commit",
      "when": [
        "git push.*--set-upstream.*\\bmain\\b.*\\bempty\\b",
        "create_or_update_file"
      ],
      "docs": [
        "docs/CAPABILITIES.md"
      ],
      "says": "git push to a genuinely empty repo fails through the proxy tooling. Make the first commit via the Contents API (create_or_update_file / push_files) — that creates main, and normal git works from then on. Playbook R13."
    },
    {
      "id": "repo-spider-swing",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bspider[- ]swing\\b",
        "\\bslingy[- ]spider\\b(?!['’]s[- ]community[- ]bot|[- ](?:(?:discord[- ])?server|(?:community[- ])?bot))",
        "menno420/spider-swing"
      ],
      "docs": [
        "docs/repos/spider-swing/README.md"
      ],
      "says": "Layer 2 entry point — read it BEFORE attaching the repo. It answers what the repo is, where it stands and whether you need to attach at all (it is PUBLIC, so read-only work needs raw fetch, not add_repo). Two live threads: core feel & difficulty is the owner's north star (2026-08-02 — tune until excellent play reaches 25k+; the game is still 'too difficult and moves too quickly', and unlocks/Campaign/monetisation are DEFERRED by directive), and the Google Play release, which is owner-gated on OQ-PLAY-APP-ID / OQ-PLAY-UPLOAD-KEY / OQ-PLAY-LISTING and carries a hard three-week floor (12 testers x 14 continuous days, then ~7 days review). Depth in the same folder: working-here.md (main requires BOTH substrate-gate AND game-quality — different from fleet-manager), capabilities.md (access paths, the @codex relay), records.md (28 dated files here). The repo's own docs/current-state.md wins over all of it."
    },
    {
      "id": "repo-spider-swing-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bspider[- ]swing\\b",
        "\\bslingy[- ]spider\\b(?!['’]s[- ]community[- ]bot|[- ](?:(?:discord[- ])?server|(?:community[- ])?bot))",
        "menno420/spider-swing"
      ],
      "docs": [
        "docs/repos/spider-swing/README.md"
      ],
      "says": "You named spider-swing. Read docs/repos/spider-swing/README.md BEFORE attaching the repo — attaching is often unnecessary (it is PUBLIC, so read-only work needs a raw fetch, not add_repo), and the folder tells you what the repo is, where the last session left off, and which of the two live threads you are touching: core feel & difficulty (the owner's north star) or the Google Play release (owner-gated, hard three-week floor). Depth in the same folder: working-here.md, capabilities.md, records.md. The repo's own docs/current-state.md wins over all of it. Paired with the `repo-spider-swing` tool route so a Bash or Grep carrying the name cannot spend this route's once-per-session budget before your message does."
    },
    {
      "id": "repo-spider-bot",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bspider[- ]bot\\b",
        "\\b(?:the[- ])?community[- ]bot(?:[- ](?:in|of|for)[- ](?:the[- ])?slingy[- ]spider(?:[- ](?:discord[- ])?server)?)?\\b",
        "\\bgame[- ]community[- ]bot\\b",
        "\\b(?:the[- ])?slingy[- ]spider(?:['’]s[- ]community[- ]bot|[- ](?:(?:discord[- ])?server|(?:community[- ])?bot))\\b",
        "menno420/spider-bot"
      ],
      "docs": [
        "docs/repos/spider-bot/README.md"
      ],
      "says": "Layer 2 entry point — read it BEFORE touching the repo. spider-bot is the AI community bot of the Slingy Spider Discord server, LIVE on Railway since 2026-08-24, and push to main deploys STRAIGHT TO PRODUCTION (no PR gate; CI is informational) — the entry point carries the deploy trap (verify meta.commitHash = HEAD; never run a local instance next to the Railway worker), the venue rule (public repo: raw fetch for reads, fresh clone deleted after for writes), and the 12-invariant pointer (the repo's CLAUDE.md; never-Administrator and AI-never-side-effects are the two most tempted). What the bot does next is the OWNER's pick — do not infer a feature. The repo's own README.md and CLAUDE.md win over all of it."
    },
    {
      "id": "repo-spider-bot-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bspider[- ]bot\\b",
        "\\b(?:the[- ])?community[- ]bot(?:[- ](?:in|of|for)[- ](?:the[- ])?slingy[- ]spider(?:[- ](?:discord[- ])?server)?)?\\b",
        "\\bgame[- ]community[- ]bot\\b",
        "\\b(?:the[- ])?slingy[- ]spider(?:['’]s[- ]community[- ]bot|[- ](?:(?:discord[- ])?server|(?:community[- ])?bot))\\b",
        "menno420/spider-bot"
      ],
      "docs": [
        "docs/repos/spider-bot/README.md"
      ],
      "says": "You named spider-bot. Read docs/repos/spider-bot/README.md BEFORE touching the repo — it is PUBLIC (raw fetch for read-only work; writes take a fresh clone, deleted after) and LIVE: push to main deploys straight to the production bot real people talk to, with CI informational-only. The entry point carries the deploy trap (deploy SUCCESS can run stale code — verify meta.commitHash = HEAD), the two-live-instances hazard, the extraction-ledger discipline for any superbot/superbot-next reuse, and the standing rule that the next feature is the owner's pick. The repo's own README.md and CLAUDE.md (12 invariants) win over all of it. Paired with the `repo-spider-bot` tool route so a Bash or Grep carrying the name cannot spend this route's once-per-session budget before your message does."
    },
    {
      "id": "repo-couch-legend",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bcouch[- ]legend\\b",
        "\\bidle[- ]stoner\\b",
        "\\blucid[- ]chronicle\\b",
        "menno420/couch-legend"
      ],
      "docs": [
        "docs/repos/couch-legend/README.md"
      ],
      "says": "Layer 2 entry point — read it BEFORE attaching the repo. couch-legend is the Grok one-prompt idle game (Couch Legend; \"the Lucid Chronicle\" names its looks contract, #3, not a rename) adopted 2026-08-20: PUBLIC repo, live at menno420.github.io/couch-legend, so read-only work needs raw fetch, not add_repo — but pushes need add_repo. Since 2026-08-21 it is a substrate-kit adopter (v1.21.0, #5): born-red cards, check --strict, required checks ci + substrate-gate, land-it-yourself (no enabler). docs/DESIGN.md is the binding mechanics map (§ 9 = the decided life-story stage system); docs/sim/2026-08-20-life-story-balance.md carries the balance evidence. Threads: mechanics base (landed) · life-story design + simulator (LANDED) · looks pass (LANDED 2026-08-21, #3 Lucid Chronicle + #4 first Arc-1 scenes — no longer dormant: #7 landed the stage schema and the STAGE_PRESENTATION registry that activates them) · life-story implementation (LANDED 2026-08-21, #7) · Android shell milestone A, the sideloadable debug APK (LANDED 2026-08-21, #11), and its stable signing identity (LANDED 2026-08-22, #14 — a committed debug keystore; before it FOUR CI runs minted four different keys, making successive builds signature-INCOMPATIBLE, so replacing an installed build would need an uninstall first, which clears the save — four certificates were measured, nothing was installed, so treat the refusal and the lost save as the inference they are). NEXT: the REST of milestone B (DESIGN § 7's working-handoff list), OWNER-GATED on his phone results — no agent container has an SDK, emulator or device, so real Android System WebView behaviour cannot be closed from here. No tuning change without sim evidence. The repo's own tree wins over all of it."
    },
    {
      "id": "repo-couch-legend-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bcouch[- ]legend\\b",
        "\\bidle[- ]stoner\\b",
        "\\blucid[- ]chronicle\\b",
        "menno420/couch-legend"
      ],
      "docs": [
        "docs/repos/couch-legend/README.md"
      ],
      "says": "You named couch-legend (the Couch Legend idle game; \"the Lucid Chronicle\" names its looks contract, not a rename). Read docs/repos/couch-legend/README.md BEFORE attaching the repo — it is PUBLIC (raw fetch suffices for read-only work; pushes need add_repo) and the folder says where the last session left off and which thread you are touching: mechanics base (landed), life-story design + simulator (LANDED — stage system decided in DESIGN.md § 9, prototype runaway measured, tested fix proposed; the fix then SHIPPED as the engine default in #7, so the live game is no longer untouched), looks pass (LANDED 2026-08-21 — #3 the Lucid Chronicle contract + #4 first three Arc-1 scene packages, ACTIVE since #7 landed the stage schema and the STAGE_PRESENTATION registry), substrate-kit adoption (LANDED 2026-08-21, v1.21.0 — born-red cards, required checks ci + substrate-gate, land-it-yourself), Android shell MILESTONE A LANDED 2026-08-21 (#11 — Capacitor 8 over this exact build, app id com.menno420.couchlegend, debug-signed APK assembled in CI; no Play, no release keystore, no DESIGN § 7 handoff list — that is milestone B). The one milestone-B slice that needed no device LANDED 2026-08-22 (#14): a committed debug keystore + a CI assertion that every APK carries it, after FOUR CI runs were measured to mint four different signing keys — before it, successive builds were signature-INCOMPATIBLE, so replacing an installed build would need an uninstall first, which clears the save. Four certificates were measured; nothing has been installed on any device, so the refusal and the lost save are inferences, not observations. Anyone holding a pre-2026-08-22 build must export the in-game save code before the one-time uninstall that transition needs. NEXT = the REST of milestone B (DESIGN § 7's handoff list), still OWNER-GATED, not code-gated: it waits on him installing the APK and reporting force-stop save survival, crossfade/particle smoothness and offline cold launch. No agent container has an SDK, emulator or device, so real Android System WebView behaviour is UNMEASURED and cannot be closed from here — do not plan milestone B as though it can. No tuning change without sim evidence. The repo's docs/DESIGN.md is the binding mechanics map. Paired with the `repo-couch-legend` tool route so a Bash or Grep carrying the name cannot spend this route's once-per-session budget before your message does."
    },
    {
      "id": "error-review",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "caus[a-z]*\\s+(all\\s+)?these\\s+mistakes",
        "mistakes\\s+don'?t\\s+happen",
        "follow the rules and guides",
        "why the rules don'?t bind"
      ],
      "docs": [
        "docs/findings/2026-08-08-why-rules-dont-bind.md"
      ],
      "says": "You are asking why sessions keep repeating mistakes, or asking for a session to be reviewed for them. That question already has a MEASURED answer here, and it changes the method rather than adding to it: 16 incidents in one session, catchers owner 5 · Stop-hook 4 · gate/CI 3 · own tests 2 · after-the-fact 2 · documentation recalled at the right moment ZERO — against 116 committed statements of the verify-first rule across 66 files. So the thing NOT to produce is a 117th statement: a rule binds only if it ARRIVES at the moment of the action, and the actions that need one do not announce themselves. Read § 3 for the four failure classes before classifying anything, and § 4 for which delivery mechanism fits each (deterministic gate > moment-of-action injection > post-turn review). Count from observable events — owner corrections, hook firings, gate and CI reds — and attribute each to its catcher, because prevented errors are invisible and an uncounted claim about frequency is the thing this file exists to stop."
    },
    {
      "id": "repo-websites",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "menno420/websites",
        "\\bwebsites repo\\b",
        "\\bcontrol[- ]plane\\b",
        "\\bbotsite\\b",
        "\\breview[- ]site\\b"
      ],
      "docs": [
        "docs/repos/websites/README.md"
      ],
      "says": "Layer 2 entry point — read it BEFORE attaching the repo. websites is the four-service estate repo (control-plane · botsite · dashboard on Railway; review is a GitHub Pages static export since the 2026-08-20/21 cutover — NO Railway service behind it). Threads: keep-bot-only execution (LANDED — #508 crawler gate, #509 exporter, #510 cutover + serviceDelete) · open follow-ups (/queue gate-ready on the owner's word; OQ-WEBSITES-PAT = the Tier-1 READ-ONLY rate-limit token). Measured traps in the entry point: GITHUB_TOKEN pushes fire no push-event workflows (dispatch review-pages.yml explicitly); exporter exit 0 proves 200s, not link integrity; the owner-gate throttle leaks across test files. The repo's own tree wins over all of it."
    },
    {
      "id": "repo-websites-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "menno420/websites",
        "\\bwebsites repo\\b",
        "\\bcontrol[- ]plane\\b",
        "\\bbotsite\\b",
        "\\breview[- ]site\\b",
        "\\b(the|my) websites\\b"
      ],
      "docs": [
        "docs/repos/websites/README.md"
      ],
      "says": "You named websites (the estate's four-service web repo). Read docs/repos/websites/README.md BEFORE attaching the repo — it carries the landed keep-bot-only thread, the measured traps, and the open follow-ups."
    },
    {
      "id": "repo-superbot",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "menno420/superbot(?![\\w-])",
        "\\bsuperbot repo\\b",
        "\\bworker service\\b",
        "\\bsuperbot\\b(?![\\w-])(?! ?(next|idle|games|mineverse|world|plugin|2\\.0))"
      ],
      "docs": [
        "docs/repos/superbot/README.md"
      ],
      "says": "Layer 2 entry point — read it BEFORE touching the repo. superbot is the FROZEN repo behind the LIVE production Discord bot (reliable-grace worker — the estate's hardest rail: never stop/scale/disconnect/delete). Since W1 (2026-08-14) the worker has a watch filter (disbot/** + root build inputs): workflow/runbook-only merges produce SKIPPED deployments (live-tested on sb #2446 — the bot did not restart), while a push touching disbot/** or a build input still restarts it. Schedule retirements are the recurring work class (sb #2446/#2450 pattern: cron out, dispatch kept, header note) — they burn Actions runs, not the bot. Traps measured on #2450: the auto-merge enabler ARMS AT OPEN — disable before requesting review; the docs/operations runbook rows go stale silently. The repo's own tree wins."
    },
    {
      "id": "repo-superbot-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "menno420/superbot(?![\\w-])",
        "\\bsuperbot repo\\b",
        "\\bworker service\\b",
        "\\bsuperbot\\b(?![\\w-])(?! ?(next|idle|games|mineverse|world|plugin|2\\.0))"
      ],
      "docs": [
        "docs/repos/superbot/README.md"
      ],
      "says": "You named superbot (the frozen repo behind the LIVE production bot). Read docs/repos/superbot/README.md BEFORE touching it — the hard rail, the W1 watch-filter rebuild semantics, the poller-retirement pattern, and the auto-merge-enabler trap live there."
    },
    {
      "id": "repo-estate-backups",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "menno420/estate-backups",
        "\\bestate[- ]backups\\b"
      ],
      "docs": [
        "docs/repos/estate-backups/README.md"
      ],
      "says": "Layer 2 entry point — read it BEFORE working the repo. estate-backups is the PRIVATE Actions venue for Railway-Postgres work the container's 80/443-only egress cannot reach. Two one-shot workflows, each with its own sealed secret: dump.yml = the restore-verified PRE-DELETION archive of postgres-botsite (PGB_DSN, ran 2026-08-16); sizing.yml = read-only catalog sizing (BOT_DB_DSN). The recurring bot backup is a superbot workflow (weekly since sb #2446), NOT here. The credential pattern IS the venue: one-shot sealed-box secret (PUT without printing → dispatch → read log → DELETE). Read-only unless the owner directs otherwise — the worker/Postgres hard rail applies."
    },
    {
      "id": "repo-estate-backups-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "menno420/estate-backups",
        "\\bestate[- ]backups\\b"
      ],
      "docs": [
        "docs/repos/estate-backups/README.md"
      ],
      "says": "You named estate-backups (the private Actions venue for bot-DB work). Read docs/repos/estate-backups/README.md first — the sealed-box secret pattern and the read-only posture live there."
    },
    {
      "id": "repo-product-forge",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bproduct[- ]forge\\b",
        "\\bphone[- ]controller\\b",
        "\\bcontroller app\\b",
        "menno420/product-forge"
      ],
      "docs": [
        "docs/repos/product-forge/README.md"
      ],
      "says": "Layer 2 entry point — read it BEFORE attaching the repo. product-forge is the seat-era shell whose living asset is phone-controller: a shipped Android Bluetooth-HID controller app (signed releases v0.4.0 → v0.22.0). The active thread is the app's customization era; graduation to its own repo is program step R2 (the keystore edge and the fresh-keystore recommendation are in the entry point). The repo's own products/phone-controller/README.md wins on the app."
    },
    {
      "id": "repo-product-forge-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bproduct[- ]forge\\b",
        "\\bphone[- ]controller\\b",
        "\\bcontroller app\\b",
        "menno420/product-forge"
      ],
      "docs": [
        "docs/repos/product-forge/README.md"
      ],
      "says": "You named product-forge / the phone-controller app. Read docs/repos/product-forge/README.md BEFORE attaching — it carries the app's slice state, the R2 graduation plan, and the signing-keystore edge (the one sharp edge of the graduation). Paired with the `repo-product-forge` tool route so a Bash or Grep carrying the name cannot spend this route's once-per-session budget before your message does."
    },
    {
      "id": "repo-superbot-next",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bsuperbot[- ]next\\b",
        "\\bsuperbot 2\\.0\\b",
        "menno420/superbot-next"
      ],
      "docs": [
        "docs/repos/superbot-next/README.md"
      ],
      "says": "Layer 2 entry point — read it BEFORE touching the repo. superbot-next is the complete-parked bot rebuild: 533/533 golden parity green, but parity is NOT porting (capture-world literals; 60/66 help panels button-less — the 2026-08-05 live audit). Two competing plans are on record (cutover ladder vs server-first rebuild) and the entry point states that fork. Traps: substrate-gate is NOT required there and reds on a pre-existing break; the closeout's 'read-only 2026-07-22' claim is false at face value; control/ deletion is owner-sequenced (D6)."
    },
    {
      "id": "repo-superbot-next-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bsuperbot[- ]next\\b",
        "\\bsuperbot 2\\.0\\b",
        "\\bthe rebuild\\b",
        "\\bthe old rebuild\\b",
        "menno420/superbot-next"
      ],
      "docs": [
        "docs/repos/superbot-next/README.md"
      ],
      "says": "You named superbot-next (the bot rebuild). Read docs/repos/superbot-next/README.md BEFORE touching it — golden-parity green must not be read as ported (capture-literal caveat), two competing direction plans are on record, and the gate/closeout traps live there. Paired with the `repo-superbot-next` tool route so a Bash or Grep carrying the name cannot spend this route's once-per-session budget before your message does."
    },
    {
      "id": "repo-substrate-kit",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bsubstrate[- ]kit\\b(?![- ](?:app|dashboard))",
        "\\bthe[- ]kit\\b(?![- ]dashboard)",
        "menno420/substrate-kit(?![\\w-])"
      ],
      "docs": [
        "docs/repos/substrate-kit/README.md"
      ],
      "says": "Layer 2 entry point — read it BEFORE working the kit. The kit's next worklist (23 rows as of 2026-08-14 — count from the finding, not from here) lives in fleet-manager's findings, NOT in the kit's own tree; releases go ONLY via workflow_dispatch; dist/bootstrap.py is GENERATED from src/engine (never edit the dist); docs/adopters.md is GENERATED and its scan roster misses sim-lab, superbot-idle and product-forge (v1.7.0, the actual oldest adopter). The similarly named Substrate-kit-app is a frozen dashboard experiment, not the kit."
    },
    {
      "id": "repo-substrate-kit-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bsubstrate[- ]kit\\b(?![- ](?:app|dashboard))",
        "\\bthe[- ]kit\\b(?![- ]dashboard)",
        "\\bkit release\\b",
        "menno420/substrate-kit(?![\\w-])"
      ],
      "docs": [
        "docs/repos/substrate-kit/README.md"
      ],
      "says": "You named substrate-kit. Read docs/repos/substrate-kit/README.md BEFORE working it — the v1.21.0 follow-up worklist (23 rows as of 2026-08-14) lives on the fleet-manager side and is unreachable from the kit's own tree; the release, generated-file and registry traps are in the entry point. Paired with the `repo-substrate-kit` tool route so a Bash or Grep carrying the name cannot spend this route's once-per-session budget before your message does."
    },
    {
      "id": "repo-venture-lab",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bventure[- ]lab\\b",
        "\\bstripe webhook test kit\\b",
        "\\bnight kiln\\b",
        "\\blull/dreamline\\b",
        "\\bdreamline\\b",
        "\\bultramarine\\b",
        "menno420/venture-lab"
      ],
      "docs": [
        "docs/repos/venture-lab/README.md"
      ],
      "says": "Layer 2 entry point — read it BEFORE acting on anything in venture-lab. The governing fact: OD-11 ('let it sit', 2026-07-26) SUPERSEDES the repo's own top closeout threads — the T+14 kill-clock call and the publish wave read as live in the repo and are not. Do not action delist/publish from repo docs alone. Hard rails: no spend, no account creation, no publishing without explicit owner action."
    },
    {
      "id": "repo-venture-lab-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bventure[- ]lab\\b",
        "\\bstripe webhook test kit\\b",
        "\\bnight kiln\\b",
        "\\blull/dreamline\\b",
        "\\bdreamline\\b",
        "\\bultramarine\\b",
        "menno420/venture-lab"
      ],
      "docs": [
        "docs/repos/venture-lab/README.md"
      ],
      "says": "You named venture-lab (or one of its products). Read docs/repos/venture-lab/README.md BEFORE acting — OD-11 supersedes the repo's own kill-clock/publish threads, and the entry point carries what is actually live (1 SKU), what waits (19 SKUs, 12 books), and the hard rails. Paired with the `repo-venture-lab` tool route so a Bash or Grep carrying the name cannot spend this route's once-per-session budget before your message does."
    },
    {
      "id": "estate-superbot-world",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bsuperbot[- ](games|idle|mineverse)\\b",
        "\\bmineverse\\b",
        "\\bsuperbot[- ]world\\b",
        "\\bsuperbot[- ]plugin[- ]hello\\b",
        "\\bplugin[- ]hello\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You touched one of the SuperBot-World satellites (superbot-games · superbot-idle · superbot-mineverse) — all complete-parked, no Layer-2 folder yet. Their rows in docs/ESTATE.md carry the load-bearing facts: mineverse is OFF Railway since 2026-08-20/21 and its own go-live checklist is moot (and its coordinator baton tells you to delete a trigger — [D-0015] forbids that); games' kit hop is owner-paced ('no adopter yet', owner 2026-08-14); idle's plugin is pinned in superbot-next's lockfile and its kit version is invisible to the adopter registry. mineverse's closeout is the family MASTER. superbot-plugin-hello (the contract exemplar, never archived) is the fourth name here: a manifest edit there is a TWO-REPO change — the host pin in superbot-next must re-land or the host boots FAILED_STARTUP."
    },
    {
      "id": "estate-superbot-world-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bsuperbot[- ](games|idle|mineverse)\\b",
        "\\bmineverse\\b",
        "\\bsuperbot[- ]world\\b",
        "\\bsuperbot[- ]plugin[- ]hello\\b",
        "\\bplugin[- ]hello\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You named a SuperBot-World satellite. Read its row in docs/ESTATE.md first — the family is complete-parked, every continuation is owner-gated, and the mineverse row carries two booby-trap warnings (moot go-live docs; a [D-0015]-violating trigger baton). For superbot-plugin-hello: a manifest edit is a two-repo change (host pin re-land or FAILED_STARTUP). Paired with the `estate-superbot-world` tool route so a Bash or Grep carrying a name cannot spend this route's once-per-session budget before your message does."
    },
    {
      "id": "estate-codetools",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bcodetool[- ]labs?\\b",
        "\\bcodetool-lab-(?:opus4\\.8|fable5|sonnet5)\\b",
        "\\bmdverify\\b",
        "\\benvdrift\\b(?!\\.py)",
        "\\bcfgdiff\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You touched the codetool labs (mdverify / envdrift / cfgdiff — repo names codetool-lab-*). Their docs/ESTATE.md rows carry the R3 release mechanics that differ per repo and live only satellite-side: opus4.8 already released (never archive while install URLs pin it); sonnet5's release tag is v0.1.1@0b1eb60 — v0.1.0 predates release.yml and fires NOTHING; fable5 has NO release workflow at all (API-created Releases needed). Their in-repo 'agents policy-blocked on tags' notes are stale seat walls — the direct-PAT path works."
    },
    {
      "id": "estate-codetools-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bcodetool[- ]labs?\\b",
        "\\bcodetool-lab-(?:opus4\\.8|fable5|sonnet5)\\b",
        "\\bmdverify\\b",
        "\\benvdrift\\b(?!\\.py)",
        "\\bcfgdiff\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You named a codetool lab or its product (mdverify / envdrift / cfgdiff). Read the docs/ESTATE.md rows first — the per-repo R3 release traps (which tag, which SHA, which repo has no release workflow) are there, and the labs' own tag-push walls are stale. Paired with the `estate-codetools` tool route."
    },
    {
      "id": "estate-shiftlife",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bshiftlife\\b",
        "menno420/shiftlife"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You touched shiftlife. It is PAUSED by owner word (OD-15, 2026-08-10: not active — do not pick it up on the old D2 pointer). Its docs/ESTATE.md row carries the stop state: free core 7/8 with reminders DELIVERY unbuilt, a standing apps/api code-vs-live deploy gap, and the open owner asks (OQ-SHIFTLIFE-PHASE0, OQ-RAILWAY-SHIFTLIFE-SCOPE). Its own docs/current-state.md + docs/plan-conformance.md are the repo-side truth; PRODUCT.md is binding product law there."
    },
    {
      "id": "estate-shiftlife-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bshiftlife\\b",
        "\\bshift calendar\\b",
        "menno420/shiftlife"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You named shiftlife. It is paused by OD-15 (not active); read its docs/ESTATE.md row before doing anything — the stop state, the deploy gap, and the owner asks that would resume it are there. Paired with the `estate-shiftlife` tool route."
    },
    {
      "id": "estate-gba-pokemon",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bgba[- ]homebrew\\b",
        "\\bpokemon[- ]mod[- ]lab\\b",
        "\\blumen[- ]drift\\b",
        "\\bwickroad\\b",
        "\\bbrineward\\b",
        "\\bunderroot\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You touched the game-lab pair (gba-homebrew / pokemon-mod-lab). Their docs/ESTATE.md rows carry the traps: gba's required 'NDS ROM build' check reds on every cold-cache PR (BlocksDS 1.21.1 pin unrecoverable; the 1.22.3 migration branch is retained, its PR closed per [D-0017]) — do not 'fix' the ruleset; pml is PRIVATE with a source-only rail (R22) and one owner letter (OQ-PML-EMERALD-LETTER) unblocks it. Both repos' best cold-start doc is their own docs/PROJECT-CLOSEOUT.md, better than their READMEs."
    },
    {
      "id": "estate-gba-pokemon-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bgba[- ]homebrew\\b",
        "\\bpokemon[- ]mod[- ]lab\\b",
        "\\bgba (project|game|games|rom|roms)\\b",
        "\\bpokemon (mod|hack)\\b",
        "\\blumen[- ]drift\\b",
        "\\bwickroad\\b",
        "\\bbrineward\\b",
        "\\bunderroot\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You named the GBA project or the pokemon mod lab. Read their docs/ESTATE.md rows first — the gba cold-cache CI trap and the pml private/source-only rails are there, and both repos' real entry point is their own PROJECT-CLOSEOUT.md. Paired with the `estate-gba-pokemon` tool route."
    },
    {
      "id": "estate-research",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bidea[- ]engine\\b",
        "\\bsim[- ]lab\\b",
        "\\btrading[- ]strategy\\b",
        "\\btrading[- ]lab\\b",
        "\\bcurious[- ]research\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You touched the research family (idea-engine · sim-lab · trading-strategy · curious-research). Their docs/ESTATE.md rows carry the routing facts: the Ideas Lab pair is on-demand (OD-4/OD-10), at rest, resume recipe in idea-engine's PROJECT-CLOSEOUT.md §3 (HANDOFF.md is the older layer); 'sim-lab' can now also mean the 4-gate METHOD run inside a target repo, not commits to the sim-lab repo; trading-strategy is closed (0 promoted, holdout SPENT) with only the archive decision open; curious-research is the parked gift notebook."
    },
    {
      "id": "estate-research-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bidea[- ]engine\\b",
        "\\bsim[- ]lab\\b",
        "\\btrading[- ]strategy\\b",
        "\\btrading[- ]lab\\b",
        "\\bcurious[- ]research\\b",
        "\\bideas lab\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You named a research-family repo (or the Ideas Lab). Read its docs/ESTATE.md row first — the pair's on-demand doctrine, the resume recipe, the sim-lab vocabulary drift, and trading-strategy's closed verdict are there. Paired with the `estate-research` tool route."
    },
    {
      "id": "estate-kit-app",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bsubstrate[- ]kit[- ](?:app|dashboard)\\b",
        "\\bkit dashboard\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You touched Substrate-kit-app. It is NOT the kit: a frozen one-shot Gemini (AI Studio) dashboard experiment committed onto a partial v1.20.2 kit snapshot — its README, CONSTITUTION and docs tree are substrate-kit's VERBATIM, so every in-repo surface misidentifies it, and its copied CI reds by construction. The only honest self-descriptions are metadata.json and package.json. The kit itself is menno420/substrate-kit; its entry point is docs/repos/substrate-kit/README.md."
    },
    {
      "id": "estate-kit-app-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bsubstrate[- ]kit[- ](?:app|dashboard)\\b",
        "\\bkit dashboard\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You named Substrate-kit-app (or the kit dashboard). It is NOT the kit — a frozen Gemini dashboard experiment whose in-repo docs are the kit's verbatim, so everything inside it misidentifies itself. Its docs/ESTATE.md row is the honest description; the real kit's entry point is docs/repos/substrate-kit/README.md. Paired with the `estate-kit-app` tool route."
    },
    {
      "id": "estate-proxybench",
      "tools": [
        "Bash",
        "WebFetch",
        "Read",
        "Glob",
        "Grep"
      ],
      "when": [
        "\\bproxybench\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You touched proxybench. Its docs/ESTATE.md row is the whole story: a single-file proxy benchmark built mostly as a joke; OD-12 parks it — no action required, disposition open and unimportant. Do not invest a session in it without the owner asking."
    },
    {
      "id": "estate-proxybench-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bproxybench\\b"
      ],
      "docs": [
        "docs/ESTATE.md"
      ],
      "says": "You named proxybench. Read its docs/ESTATE.md row — OD-12 parks it (no action required). Paired with the `estate-proxybench` tool route."
    },
    {
      "id": "repo-disposition-2026-08-22",
      "tools": [
        "Edit",
        "Write",
        "Bash"
      ],
      "when": [
        "\\barchiv(e|ed|ing)\\b",
        "\\bdispositions?\\b",
        "\\bstart(ing)? fresh\\b",
        "\\brework(ed|ing)?\\b",
        "\\bwhich repos to keep\\b",
        "\\bkeep[- ]?vs[- ]?archive\\b",
        "\\bOD-1[78]\\b"
      ],
      "docs": [
        "docs/findings/2026-08-22-owner-direction.md",
        "docs/planning/2026-07-26-consolidation-program.md",
        "docs/ESTATE.md"
      ],
      "says": "You are working the repo-disposition question (OD-18, owner 2026-08-22). His verbatim words and the three asks that are NOT directives are in docs/findings/2026-08-22-owner-direction.md. Two things that change the answer: archiving is REVERSIBLE and blocks writes only, so archive is a cheap undoable call and DELETE is the only one needing certainty; and the estate has TWO start-fresh precedents, not one — the 2026-08-21 game-community plan AND superbot-next, which reached 533/533 parity and still ended parked as a donor rather than a replacement. Read the pair, not the success story alone. Deliverable is a table with a reason per row.",
      "authored_only": true
    },
    {
      "id": "exit-code-after-a-pipe",
      "tools": [
        "Bash"
      ],
      "when": [
        "\\|[\\s\\S]{0,400}\\$\\?"
      ],
      "docs": [
        "docs/traps.md"
      ],
      "says": "TRAP-002 — this command pipes, and then reads `$?`. After `a | b`, `$?` is **b's** exit code, not a's. The failure is silent and inverted: the real command fails, your check reports success, and the session records a pass. MEASURED 2026-08-23 in this very estate: `pip install … | tail -5; echo \"PIP_EXIT=$?\"` printed PIP_EXIT=0 — that was `tail`'s exit code, and the install's real status was unknown. FIX, pick one: redirect instead of piping (`cmd > out.txt 2>&1; echo $?`) and read the code directly, or use `${PIPESTATUS[0]}` when the pipe is unavoidable. fm #915 counted 26 of 389 session cards restating this trap and 0 routes delivering it — this route is the fix for that. Register: docs/traps.md TRAP-002."
    },
    {
      "id": "stamping-a-measured-claim",
      "tools": [
        "Edit",
        "Write",
        "Bash"
      ],
      "when": [
        "`?MEASURED`?\\b",
        "\\bmeasured\\s+(live|2026-)",
        "\\bverified live\\b"
      ],
      "docs": [
        "docs/traps.md"
      ],
      "says": "TRAP-001 — you are stamping something MEASURED. One check: is your basis a **live surface**, or a document that describes one? A citation is not a measurement, and this estate's most recent failure was exactly that laundering — a session wrote 'measuring the live state instead' and then cited an audit doc, having already retracted a CORRECT finding on the strength of a code comment written two months earlier. The credential was in the environment the whole time: $GITHUB_PAT over direct egress for GitHub, $RAILWAY_API_KEY against backboard.railway.com/graphql/v2 for anything deployed. If you truly cannot read the live surface, the sentence must say so: 'per <doc> dated <date>, not re-verified.' MAP.md's own closing rule — the live surface wins over any document here — did not prevent three instances in one conversation. Register: docs/traps.md TRAP-001.",
      "authored_only": true,
      "repeat": true
    },
    {
      "id": "absence-claim",
      "tools": [
        "Edit",
        "Write",
        "Bash"
      ],
      "when": [
        "\\bno (hits|results|matches|consumers|references|dependents)\\b",
        "\\bnothing (in|else|found)\\b",
        "\\bzero hits\\b",
        "\\bnot (indexed|referenced|present anywhere)\\b",
        "\\bnone of the (other|remaining)\\b"
      ],
      "docs": [
        "docs/traps.md"
      ],
      "says": "TRAP-003 — you are about to record that something is NOT there. An empty result proves your QUERY RAN; it does not prove the world is empty. Run the positive control FIRST: make the same query, in the same form, find something you know IS present. If it can't, the query is broken, not the world. MEASURED 2026-08-23: a 26-repo index sweep using `path:README.md` with no search term returned 0 for EVERY repo including fleet-manager — it measured nothing, and was caught only because a known-good repo also came back empty. Same day, spider-swing was recorded as having 'no next-step line anywhere in 662 lines' from a heading regex; reading the file found it at lines 512-514, in prose. For a 'not in file F' claim, OPEN F — a keyword search is not a read. Register: docs/traps.md TRAP-003.",
      "authored_only": true,
      "repeat": true
    },
    {
      "id": "claim-beyond-the-sample",
      "tools": [
        "Edit",
        "Write",
        "Bash"
      ],
      "when": [
        "\\bonly \\d+ of \\d+\\b",
        "\\b\\d+ of (the )?\\d+ (repos|repositories|files|entries|cards|commits|sessions|PRs|pull requests|notifications|findings|adopters|routes|hooks|skills|docs|documents)\\b",
        "\\bnone of the other \\d+\\b",
        "\\bevery (repo|repository|file|entry|card|commit|session|PR|pull request|notification|finding|adopter|route|hook|skill|doc|document) in\\b",
        "\\ball \\d+ (repos|repositories|files|entries|cards|commits|sessions|PRs|pull requests|notifications|findings|adopters|routes|hooks|skills|docs|documents)\\b",
        "\\b\\d[\\d,]*\\s+(?:[a-z-]+\\s+)?(repos|repositories|files|entries|cards|commits|sessions|PRs|pull requests|notifications|findings|adopters|routes|hooks|skills|docs|documents)\\b(?!\\s*(that|which)\\b)"
      ],
      "docs": [
        "docs/traps.md"
      ],
      "says": "STOP — you are about to write a COUNT or a UNIVERSAL over a population. State N MEASURED of M TOTAL and name what the unmeasured remainder is. TRAP-004, and it fired four times on 2026-08-26 in one session, twice inside the very document warning about it: a kit-version census taken from TEN repos was published over all of them and contradicted by the estate's own records (five live versions across 16 adopters, not three); a commit count taken from a SHALLOW container clone (52 here, 966 on main) was written as the repository's history; 'all 51 notifications were replays' described a set by its majority when four were new; and a card count was reported as a session count when one session can land two cards. THE COUNT-ONLY FORM IS THE ONE THAT KEEPS GETTING THROUGH — 'X cards', 'Y commits' with no ratio and no qualifier reads as a census and is usually a sample. Two checks before you write it: (1) did you enumerate the WHOLE population, or the first page / the default depth / the ten you sampled? (2) does a record already in this tree contradict it? Re-measuring the full set is nearly always cheaper than the argument about whether the sample generalises — the ten-repo shortcut above cost a round of review to undo, and the full sweep took two minutes.",
      "authored_only": true,
      "repeat": true
    },
    {
      "id": "card-flip-before-push",
      "tools": [
        "Bash"
      ],
      "when": [
        "(?:^|[;&|]|\\Bcd\\s)\\s*git\\b(?:\\s+(?:-[cC]\\s*\\S+|-[pP]\\b|--\\S+))*\\s+push\\b"
      ],
      "docs": [
        "docs/traps.md"
      ],
      "says": "TRAP-006 — before this push: does the in-diff .sessions/*.md card still say in-progress? The born-red card IS the merge hold. Two guards read it: the required check is red while it says in-progress, and merge-on-green.yml:190-213 parses each in-diff card and skips the merge on in-progress. Pushing a card already flipped to complete defeats both at once — the PR is born green and the sweep lands it before review can answer. MEASURED 2026-08-23 (fm #915): opened 08:24:29Z, auto-merged 08:25:06Z — 37 seconds, inside the ~335s Codex relay, 0 reviews. Verify right after opening: the required check must be RED and name the born-red hold. Escape hatch if a PR must open green: the do-not-automerge label. Register: docs/traps.md TRAP-006. ALSO TRAP-007 (the neighbour this route did not name): the other losing flip is flipping to complete while a review you REQUESTED is still unanswered. The lander sees card=complete + gate=green and cannot see a pending re-review, so it merges the head it has and later findings land against a PR that no longer exists. MEASURED 2026-08-23 fm #937: merged at 775f1c8; the round-2 commits 4a80bf6/4ea3962 reached main in neither, so main carried five known-wrong statements. Before flipping, confirm the verdict exists AT THE CURRENT HEAD on BOTH surfaces (/pulls/{n}/reviews AND /issues/{n}/comments — a clean pass creates no review object). If you re-request after flipping, apply do-not-automerge BEFORE PUSHING THE COMPLETED CARD — the push, not the request, is what makes the PR mergeable, so a label applied after it loses the race. To establish the head was reviewed, match PER SURFACE. A REVIEW object carries `commit_id` — compare it directly. A CLEAN pass creates NO review object and arrives as an ISSUE COMMENT whose SHAPE VARIES. Whether it carries a `Reviewed commit:` line is an INDEPENDENT variation the headline does NOT predict: fm #938 and fm #939 share the `Approved - no blocking findings` headline and differ on it (#938 none, #939 yes); the `Hooray!` form had one. Some bodies name the head only as a 40-hex SHA inside blob/<sha>/ URLs, beside the merge-preview SHA. So never branch on the headline. Try `Reviewed commit:` FIRST; when absent, extract every 40-hex string and test whether your head is AMONG them - presence, not position. If none matches, re-request rather than guessing. Checking only commit_id reads a clean verdict as absent.",
      "code_only": true,
      "repeat": true
    },
    {
      "id": "card-flip-to-complete",
      "tools": [
        "Edit",
        "Write",
        "MultiEdit",
        "Bash"
      ],
      "when": [
        "Status:\\**\\s*`?complete`?"
      ],
      "docs": [
        "docs/traps.md"
      ],
      "says": "TRAP-006/007 — you are flipping a session card to `complete`. THIS IS THE MERGE-ELIGIBILITY EVENT, not bookkeeping: the born-red card is the only thing holding merge-on-green off the PR, so everything you still owe it happens BEFORE this edit. Check, in order: (1) has the reviewer answered AT THE CURRENT HEAD? Match PER SURFACE: a REVIEW object carries commit_id — compare it; an ISSUE COMMENT (the clean pass) has NO commit_id — use the body rule below. Matching commit_id on both rejects every clean verdict. (2) did you change anything after that verdict? then push, re-request on the NEW head and wait again — a review binds the SHA it ran on (.claude/skills/session-close/SKILL.md:116-129, MEASURED fm #827). MEASURED fm #937: flipped after round 1, re-requested, and merge-on-green landed 775f1c8 while round 2 was still being written — six findings landed against a PR that no longer existed. If a review is outstanding, apply do-not-automerge BEFORE pushing the completed card — the PUSH, not the request, is the merge-eligibility event, so a label applied after it loses the race. To establish the head was reviewed, match PER SURFACE. A REVIEW object carries `commit_id` — compare it directly. A CLEAN pass creates NO review object and arrives as an ISSUE COMMENT whose SHAPE VARIES. Whether it carries a `Reviewed commit:` line is an INDEPENDENT variation the headline does NOT predict: fm #938 and fm #939 share the `Approved - no blocking findings` headline and differ on it (#938 none, #939 yes); the `Hooray!` form had one. Some bodies name the head only as a 40-hex SHA inside blob/<sha>/ URLs, beside the merge-preview SHA. So never branch on the headline. Try `Reviewed commit:` FIRST; when absent, extract every 40-hex string and test whether your head is AMONG them - presence, not position. If none matches, re-request rather than guessing. Checking only commit_id reads a clean verdict as absent.",
      "code_only": false,
      "path_when": [
        "(^|/)\\.sessions/[^/]*\\.md$"
      ],
      "authored_only": true
    },
    {
      "id": "card-status-write",
      "tools": [
        "Edit",
        "Write",
        "MultiEdit",
        "Bash"
      ],
      "when": [
        "\\.sessions/.*\\.md"
      ],
      "docs": [
        "docs/traps.md"
      ],
      "says": "TRAP-006 (card lifecycle) — a session card's Status is a merge control, not paperwork. Commit it `in-progress` as the FIRST commit and push it red; flip to `complete` as the deliberate LAST commit, after review has answered. A card that reaches the remote already complete opens the PR green and it auto-merges unreviewed (fm #915: 37 seconds, 0 reviews). If a review is outstanding, apply do-not-automerge BEFORE PUSHING THE COMPLETED CARD — the push, not the request, is the merge-eligibility event. Register: docs/traps.md TRAP-006 + TRAP-007. ALSO TRAP-007 (the neighbour this route did not name): the other losing flip is flipping to complete while a review you REQUESTED is still unanswered. The lander sees card=complete + gate=green and cannot see a pending re-review, so it merges the head it has and later findings land against a PR that no longer exists. MEASURED 2026-08-23 fm #937: merged at 775f1c8; the round-2 commits 4a80bf6/4ea3962 reached main in neither, so main carried five known-wrong statements. Before flipping, confirm the verdict exists AT THE CURRENT HEAD on BOTH surfaces (/pulls/{n}/reviews AND /issues/{n}/comments — a clean pass creates no review object). If you re-request after flipping, apply do-not-automerge BEFORE pushing the request.",
      "path_when": [
        "(^|/)\\.sessions/[^/]*\\.md$"
      ],
      "authored_only": true
    },
    {
      "id": "codex-verdict-poll",
      "tools": [
        "Bash"
      ],
      "when": [
        "pulls/(?:\\d+|\\{n\\}|\\$\\{?\\w+\\}?)/reviews",
        "issues/(?:\\d+|\\{n\\}|\\$\\{?\\w+\\}?)/comments",
        "pulls/(?:\\d+|\\{n\\}|\\$\\{?\\w+\\}?)/comments"
      ],
      "docs": [
        "docs/CAPABILITIES.md"
      ],
      "says": "You are polling for a review verdict. A CLEAN Codex pass creates NO review object: /pulls/{n}/reviews returns 0 and so does /pulls/{n}/comments, while the verdict sits in /issues/{n}/comments. Polling only /reviews cannot tell 'passed clean' from 'never answered'. MEASURED twice: websites #511 (2026-08-22) and fm #924 (2026-08-23, verdict at 11:23:57Z ~3 min after open) — on #924 a session polled /reviews and /pulls/{n}/comments for 24 minutes, concluded no review arrived, and wrote that into a committed card. It had passed clean. CHECK BOTH SURFACES, and match on SHA rather than recency — a stale clean verdict from an earlier head otherwise reads as covering the current one. HOW to match differs per surface: a REVIEW object carries `commit_id`. A CLEAN pass does NOT, and its shape VARIES: one observed form carries a `Reviewed commit:` line (websites #511, fm #924), another carries none at all (MEASURED 2026-08-24, fm #938) and names the head only as a 40-hex SHA inside blob/<sha>/ URLs beside the merge-preview SHA. Try `Reviewed commit:` first; when absent, extract every 40-hex string and test whether your head is AMONG them. If none matches, re-request rather than guessing. Also filter by the Codex login: your own reply to a review thread creates a review record under your account and a naive 'is there a review at head?' counts it as the reviewer's answer. Canonical: docs/CAPABILITIES.md § \"Codex's CLEAN verdict is an issue comment\"."
    },
    {
      "id": "gemini-notebook",
      "when": [
        "\\bnotebooklm\\b",
        "\\bgemini[- ]notebook",
        "\\bnotebook (bundle|corpus|sources)\\b",
        "build_notebook_bundle",
        "OQ-GEMINI-NOTEBOOKS"
      ],
      "docs": [
        "docs/providers/gemini-notebook.md"
      ],
      "says": "Gemini Notebook IS NotebookLM, renamed (owner screenshot of the in-app rename banner, 2026-08-23) — one product, two entry points, his account is PRO. THE RULE THAT GOVERNS EVERY BUNDLE: partition, never concatenate. A corpus over the cap becomes MORE notebooks, never fewer files — merging collapses the citation granularity that is the entire point of the upload. THE CAP IS PER NOTEBOOK: confirmed 2026-08-23 from Google's own FAQ (\"Up to 50 sources per notebook\", \"100 notebooks per account\" on free); the PRO figure of 300 is the owner's splash reading and is NOT on that page, so treat it as OWNER not MEASURED and do not assume it holds on the standalone surface. IT DOES NOT INGEST SOURCE FILES — .ino/.scad/.py/.sh/.yml/.json are not uploadable and .html is accepted only as a web URL; convert them. THE FILENAME IS THE CITATION LABEL, so flatten the path into the name (guides__first-layer__guide.md), and never leave a leading dot — a dotfile source is hidden from the upload picker and missed by select-all. Do not hand-roll any of this: tools/build_notebook_bundle.py does it, is corpus-agnostic, and idea-engine is next. API reachability is UNVERIFIED and a failed probe is one call's result, never a wall."
    },
    {
      "id": "gemini-notebook-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bnotebooklm\\b",
        "\\bgemini[- ]notebook",
        "\\bnotebook (bundle|corpus|sources)\\b",
        "OQ-GEMINI-NOTEBOOKS"
      ],
      "docs": [
        "docs/providers/gemini-notebook.md"
      ],
      "says": "You named Gemini Notebook (= NotebookLM, renamed). Read docs/providers/gemini-notebook.md BEFORE building or advising anything — it carries the rule that governs every bundle (partition, never concatenate: a corpus over the cap becomes MORE notebooks, never fewer files, because merging collapses the citation granularity the upload exists for), what the cap actually is and which half is confirmed (per notebook: MEASURED from Google's own FAQ; the PRO 300 is the owner's splash reading, NOT established for the standalone surface), what it refuses to ingest (.ino/.scad/.py/.sh/.yml/.json; .html only as a URL), and the two build findings that cost a session to learn — the filename IS the citation label, and a leading dot makes a source invisible to the upload picker. tools/build_notebook_bundle.py already does all of it and is corpus-agnostic. Paired with the `gemini-notebook` tool route so a Bash or Grep carrying the name cannot spend this route's once-per-session budget before your message does."
    },
    {
      "id": "session-card-venue",
      "tools": [
        "Write",
        "Edit",
        "Bash"
      ],
      "when": [
        "📊\\s*Model:",
        "\\.sessions/\\d{4}-\\d{2}-\\d{2}-"
      ],
      "docs": [
        "docs/activity/README.md"
      ],
      "says": "You are writing a session card. Add the venue line directly under the 📊 Model: line — `- **📍 Venue:** <token>` — from the closed set: local-desktop · local-cli · cloud-container · codex-cloud · chatgpt-work · other. The Model line answers WHO ran the session; nothing answered WHERE until 2026-08-26, and MEASURED that day: 0 of 418 dated cards in this repo carried it (the directory README is not a card), so a session reading another session's card could not tell laptop work from container work — which is exactly the question the owner asked. It is a separate line, never a fourth Model segment: that line has a kit-validated three-segment taxonomy (model · effort · task-class) and a local convention does not belong inside a gated grammar. OMIT IT RATHER THAN GUESS — the generator reports an absent line as `unstated` and prints the coverage count, so an honest null stays visible and a wrong venue does not. And if this session did anything that produced NO COMMIT ANYWHERE (a laptop change, a ChatGPT or Gemini sitting, a Drive reorganisation, an install), that is the one thing nothing can derive: log it with `python3 tools/estate_activity.py log --venue <token> --title \"...\"`. Full design: docs/activity/README.md.",
      "authored_only": true
    },
    {
      "id": "cross-session-visibility",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\blocal session",
        "\\bcross[- ]session",
        "\\bwhat (have|has|did) (the |my |his )?(other |local |cloud )?(sessions?|ai|ais)\\b",
        "\\bwhile I was away\\b",
        "\\bwhat happened (since|while|yesterday|overnight)\\b"
      ],
      "docs": [
        "docs/activity/README.md",
        "docs/findings/2026-08-26-cross-session-visibility.md"
      ],
      "says": "You are being asked what other sessions have been doing. DO NOT answer from this repo's .sessions/ alone — that is only fleet-manager's own work, and it is the exact gap measured on 2026-08-26: in the seven days to that date the estate wrote 74 cards across six non-archived repositories and a fleet-manager cloud session could reach 54 of them; the other 20 were unreachable from here. Run `python3 tools/estate_activity.py refresh` for the current estate-wide picture, then read docs/activity/estate-log.md — including its INVISIBLE WORK section, which names repositories that moved and left no card (that is how creator-kit surfaced at all). Work that touched no repository (laptop setup, a ChatGPT or Gemini sitting, Drive) is never derivable and lives only in docs/activity/off-repo-log.md — read it too. The log also reads OPEN PR BRANCHES, so a born-red card in flight is listed before it merges — check that section before starting work in a repo. Venue coverage started at 0 of 74, so a card with no 📍 Venue: line means UNSTATED, never 'cloud'; say unknown rather than inferring the machine."
    },
    {
      "id": "shallow-clone-commit-counts",
      "tools": [
        "Bash"
      ],
      "when": [
        "git\\s+(log|rev-list|shortlog)\\b",
        "git\\s+log[^|]*\\|\\s*wc\\b"
      ],
      "docs": [
        "docs/traps.md"
      ],
      "says": "This container's clone is SHALLOW — `.git/shallow` exists, and on 2026-08-26 it held 52 commits while `main` held 966. So any count, author list or 'every commit' claim taken from `git log`, `git rev-list` or `git shortlog` here describes THE CLONE, not the repository, and a squash-merged history makes the sample look uniform (one author, one committer) exactly when it is least representative. Check with `test -f .git/shallow` before quoting a number, and either scope the claim to the sample in the same sentence or get the real figure from the API (`GET /repos/{o}/{r}/commits?per_page=1` + the Link header). MEASURED 2026-08-26: 'git history | 50 commits, one author, every one squash-merged' was published as a property of fleet-manager and was a property of a shallow clone (TRAP-004; @codex, fm #947)."
    },
    {
      "id": "execution-packets-prompt",
      "tools": [
        "UserPromptSubmit"
      ],
      "when": [
        "\\bPKT-[A-D]\\d",
        "\\b(execution|work) packets?\\b",
        "\\bpacket plan\\b"
      ],
      "docs": [
        "docs/planning/2026-08-26-estate-execution-packets.md"
      ],
      "says": "You are naming the estate execution packets (2026-08-26, OD-22's frame). Read docs/planning/2026-08-26-estate-execution-packets.md before acting: § 5 is the packet catalog (target, executor, steps, acceptance, verify, non-scope per packet), § 4 the venue block every packet inherits — run its precheck first on a local session (python --version · gh auth status; if Python is missing, the PR's required check is the binding gate, never merge on red). The first sitting is PKT-C1 (spider-swing's front door); the pair that must not slip is A1/A2 (gate the next-agent contribution); the owner-lane letters are § 5's closing table. The doc's figures carry the command that produced each — re-derive before building on one, never quote. The owner accepted the plan provisionally ('not read every word yet'), so expect his line-level corrections to outrank the prose."
    }
  ]
}
